# Heroes' Fate 共用腦

> 給**任何接手的人或 AI**：`PROJECT_NOTES.md` 的變更日誌已經 137 條，沒人讀得完。
> 這份是**濃縮過的、只留還會咬人的東西**。動工前讀這份就夠；要查某一版的細節再去翻日誌。
>
> 規則層看 `CLAUDE.md`（每個 session 自動載入）。這份補的是**經驗層**。

---

## 30 秒版

聚會用的抽籤遊戲，**純靜態 HTML/CSS/JS，無 build 無框架**。
睿哥**只在 iPhone Safari 上用 4G 玩**，`push` 到 `main` → GitHub Pages 自動上線。

```
正式網址   https://longxia7hao-dev.github.io/heroes-fate/
本機目錄   /Users/longxia7hao/Heroes_Fate
```

**他的實際連線約 120〜150 KB/s（約 1 Mbps）。** 這個數字決定了一切效能決策 ——
任何「在桌機上很快」的方案都要先換算成他的速度再判斷。

---

## 現況速覽

| | |
|---|---|
| 版本 | 見 `index.html` 的 `.build-stamp`（＝線上真正的版本） |
| 角色 | 14 位。**`id` 從不改名**，70 條影片路徑與音效全靠它 |
| 影片 | `assets/videos/mobile/` 約 46MB，71 支。一場 4 人約 6.9MB |
| 音效 | `assets/audio/` 約 4.6MB，Web Audio（不是 `<audio>` 元素） |
| 程式 | `js/game.js` 3348 行是主體，`js/videoPlayer.js` 1066 行管所有影片 |

**五個 JS 檔完全沒被 `index.html` 引用 —— 是死的，別照著它們推論架構**：
`characterStage.js`／`frameAnimator.js`／`spritePlayer.js`／`victory.js`／`viewer3d.js`。
（尚未刪除，刪之前問睿哥。）

---

## 鐵則（違反會出事）

1. **改 `js/rng.js` 前先問睿哥。** 目前 SHA-256
   `9ab55a96f19f162c1380a06cc7b3f2d496fb088aa393d570afeef7ef662f021a`
2. **先 RNG 定案，再播演出**（seed-first）。演出不可以影響結果。
3. **影片絕對不要走 Service Worker**（`sw.js` 的 `VIDEO_CACHE = false`）。詳見下面地雷 ①。
4. 一局只有一位勝利者；同局角色不可重複；不需輸入名字。
5. 不要引入打包工具或前端框架。
6. **收工一定要 `git push`** —— 不 push 睿哥的手機看不到任何東西。

---

## 地雷區

### ① 影片走 Service Worker ＝ 選角畫面直接死掉

`sw.js` 裡有一整套 range-aware 的影片快取（存完整 200、自己切 206），
**在 Chromium 上驗證完全正常**。但 2026-09-05 在睿哥 iPhone 上：
**卡片完全不翻面、待機影片完全不出現**（翻牌綁在 `playing` 事件，
影片沒變成可播就永遠停在卡背）。關掉立刻恢復，他親自確認。

程式碼還留著，**很容易被下一個人「看起來能加速」就打開**。要重開必須先在真 iPhone 上驗過選角翻牌。

### ② 快取有四層，少做一層手機就看到舊的

```
改 JS/CSS      → index.html 對應的 ?v=N +1
換影片         → videoPlayer.js 的 MEDIA_VERSION +1（改 manifest 再加 MANIFEST_VERSION）
換素材         → python3 tools/gen_asset_versions.py（逐檔雜湊表）
CSS 裡的 url() → 手動改 css 檔內的 ?v=（吃不到 JS 的雜湊表）
```

**最陰的是 `index.html` 自己**：頂端那三個 `<meta http-equiv="Cache-Control">`
**對瀏覽器完全沒有作用**（只有真的 HTTP header 算數），而 Pages 給 HTML 的是 `max-age=600`。
所以每次改版都要**更新 `.build-stamp` ＋ 跑 `tools/sync_build.py`**，玩家才收得到更新提示。

**而且「更新」按鈕本身曾經是壞的**：原本只做 `location.reload()`，
但 reload 出去的導覽還是會經過 SW、被快取裡的舊 `index.html` 接走 ——
睿哥為此卡在 v1.65 卡了五個版本。現在改成「清 shell 桶 ＋ `registration.update()` ＋ 換帶時間戳的網址」，
而且主選單會自動更新（演出中只掛提示）。

### ③ 影片播放：`playing` 不代表播得動

`playing` 只表示**第一幀解出來了**。在他的 4G 上一就緒就開播 ＝ **播一下就餓死**
（兩支錄影逐幀量到靜止 1.67s／1.87s）。

現在的規則：**等到整支緩衝完才讓影片上場，還沒好就顯示已在快取裡的頭像。**
逾時也**不放行沒緩衝完的影片** —— 一張清楚的靜圖，永遠好過一段一直頓的影片。

判斷「緩衝完」要用 `buffered` **單一連續段**（`b.length === 1`）。
只看 `start(0)` 與 `end(length-1)` 的話**中間有洞也會判成完整**，
Chromium 永遠只有一段所以測不出來，但 Safari 用 Range 很常產生多段。

**不要用 `canplaythrough` / `readyState >= 4`** —— 規格上那只是「估計」，
實測它跟 `playing` 每次都在同一毫秒觸發，等於完全沒等到。

### ④ 預抓不會被 `<video>` 重用 —— 要自己握著 Blob

2026-09-07 實測（限速 130KB/s、伺服器有送 ETag／Last-Modified）：

```
fetch() 預抓完      → <video> 播同一支，又跟伺服器要 2 次
<video preload> 預熱 → 第二顆 <video> 仍重抓 1641ms
```

**v1.80〜v1.87 的預載因此完全沒有效果，只是在搶頻寬。**
現在改成 `fetch()` → `blob()` → `createObjectURL()`，播放時餵 `blob:` URL
（實測 460ms 上場、零網路請求、零卡頓）。

**`blob:` 跟地雷 ① 的 SW 完全不同**：SW 是**合成**媒體回應，blob 是瀏覽器自己持有的位元組。
但退路**必須用逾時而不是 `error`** —— iOS 的失效模式是「不報錯也永遠不會變成可播」。

### ⑤ 背景預抓會跟玩家眼前的影片搶頻寬

v1.69 曾經在播放中背景補快取，實測「Range 送出 64K」之後緊接著「無 Range 送出 335K」——
**邊播邊多抓一整支**。預抓只能在真的閒著的時候做，而且要能立刻讓路。

**預抓順序要跟選角格一樣**（照 `HEROES`，不是 manifest 順序）。
順序錯了等於白預載：那 7 秒只抓得完 2 支，抓錯人就完全沒幫助。

**⚠️ 「讓路」不等於 `abort()` —— 但改掉它 v1.90 也炸了，見下方警告。** 這條咬了 v1.80〜v1.89 整整十個版本：
睿哥的節奏是**每 1.3〜1.5 秒換一個角色**，而一支片要 1.3 秒才抓得完 ——
每次點卡片就中止在飛的那支，等於**每一支都在快抓完時被砍掉，下一輪還從 0 重抓**，
預抓從頭到尾一支都沒完成過。實測（模擬他的節奏連點 8 位）：
**中止版 8 位全走 `net`、1304〜2919ms；不中止版 8 位全走 `blob`、343〜399ms。**

規則：**只有真的離開選角（進入演出）才中止；選角中換角色只是「不再開新的」。**
已經抓一半的東西丟掉是純虧損。要不要開新的交給 `warmLineFree()` 問
「眼前那支緩衝完了沒」—— **用問的，不要用猜秒數的。**

睿哥 2026-09-07 的 `?debug=1` 截圖證實這條在他手機上有效：
14 角全走 `blob`、**8〜59ms 上場**（knight 16／ranger 8／axeman 9）。

> ⚠️ 歷史：v1.91 曾因為一支「卡片不翻面」的錄影把這個修正整個退掉，
> **那是誤判** —— 同一分鐘的截圖顯示同一版本其實是好的，真正的病灶是
> 下面地雷區 ⑩ 的 `play()` 掛住。v1.92 已放回。**看到單一異常先比對時間軸，
> 不要急著退掉有數據支持的修正。**

### ⑤-2 瀏覽時不要預抓 confirm 片

每點一個角色就順手預抓該角的確定片（約 160K、佔線 1.1 秒），但那是他
**只是路過、根本沒按「決定」**的角色。瀏覽 10 個就白花 1.6MB ——
**比整個等待片庫（14 支共 2.3MB）還多**，而那些頻寬本來該拿去預抓下一支等待片。

優先順序寫死在 `primeConfirmWhenSafe()`：**等待片永遠排在 confirm 前面。**
自己這支要整支緩衝完、且背景預抓沒事做了，才准去碰 confirm；等不到就不抓。
代價很小：按「決定」時現抓（實測 22ms 發出請求），而那段畫面上本來就有「鎖定中…」。

### ⑥ 換片會先畫出「上一支的最後一幀」

`#stage-video` 是魔王降臨／命運排序／命運分隊共用的**同一顆 `<video>`**，
換 `src` 之後 Safari 常常繼續畫上一支的最後一幀。
而為了避免「全黑」又刻意提早顯示 —— 兩個各自合理的決定湊成 bug。

現在等待期間**不讓 `<video>` 露臉**，改用 `.stage.poster-hold::after` 圖層頂著新片自己的 poster。

### ⑦ 換素材的三個坑

- **換影片一定要重製 poster**，否則會先閃一張舊角色圖。用 `tools/replace_hero_videos.py --finish` 會自動做。
- **角色 `id` 跟顯示名對不起來**：`武鬥宗師` 是 **`dark_fighter`**，`monk` 是**僧侶**。
  這是 v1.10 改名留下的陷阱，**換素材前一定要抽幀跟現行檔案並排比對**
  （v1.8 兩隻弓箭手配對顛倒也是同一類錯誤）。
- Sora 原片常夾一軌 mjpeg 封面圖，轉檔要明確 `-map 0:v:0 -map 0:a:0?`。

### ⑧ 演出流程有 early return

`presentBossRaid()` 曾因為 `if (isDoom) { … return; }` 排在播 final 之前，
導致命運審判模式**整段最後一擊從來沒播過**。加新 ACT 前先確認它在所有 return 之前。

### ⑨ CSS 的小陷阱

- `.m-ico` 用 `font-size: 0` 藏住 RAID／ORDER／TEAM 這幾個字，符號是 `::before` 畫的。
  **一改字級那幾個字就會跑出來蓋住卡片。**
- `background-clip: text` ＋ `color: transparent`：底圖一旦不畫就**整個字消失，沒有中間狀態**。
  v1.66 的勝利字樣就這樣在 iPhone 上不見了。（模式頁標題用的同款寫法**經睿哥截圖證實可用**，
  差別可能在 `filter` 疊了幾層 —— 要用就照抄已知可用的那組。）
- 結果頁要「一頁塞得下」靠的是**立繪 `flex: 0 1 auto` ＋ `aspect-ratio: 1/1` ＋ `width: auto`**
  （高度被壓時寬度跟著縮，才不會變扁）。**不要加 `overflow: hidden`** ——
  命運排序 6 人的名單會被切掉 37px 而且捲不到。

---

### ⑩ `await el.play()` 會把整條揭露流程吊死

**`play()` 回傳的 promise 規格上沒有保證會 settle。** iOS 的失效模式又剛好是
**不報錯、也永遠不會變成可播** —— 接 `error` 接不到，`await` 就永遠不回來，
**後面的頭像後備一行都跑不到，卡片永遠停在卡背**（睿哥 2026-09-07 21:09 的
12 秒錄影就是這個）。

而 `setSource()` 的 blob 逾時退路會**主動把那個 promise 作廢**：blob 2.5 秒內
給不出第一幀就 `disableBlobs()` + 換網址 `load()`。舊版換完不會再 `play()`，
等於沒有人叫它動。

規則：**任何 `await el.play()` 都必須有時限**（`PLAY_SETTLE_MS = 1200`），
而且**任何換 src + `load()` 之後都要補一次 `play()`**。
實測（把 blob 來源的 `play()` 改成永遠不 settle）：**修補前 8024ms、修補後 1514ms**。

### ⑪ 背景工作不要壓在翻牌／演出的那一刻

睿哥 2026-09-08 的三張 `?debug=1` 截圖：等待片是 blob、rs4、整支緩衝完、
**9ms 就在播、一次 `⚠ STALL` 都沒有** —— 但他還是覺得卡。
因為**確定片的預抓每一次都跟翻牌落在同一毫秒**（誤差 0〜2ms），
手機要在跑翻牌動畫的同時下載＋**解碼**第二支影片（`loadeddata` 代表真的在解），
畫面就掉格。

規則：**條件成立不等於現在就該做。** 背景預抓／預熱一律推到演出結束、
畫面靜下來之後（`CONFIRM_PRIME_DELAY_MS = 1500`）。

⚠️ **這個錯誤犯過兩次。** v1.93 把 confirm 的預抓移開翻牌之後，
v1.95 的無縫循環又把**夥伴那顆的載入＋解碼**放回同一個位置 ——
睿哥的截圖量到翻牌後**近 0.4 秒沒有新畫格**（`379ms`／`398ms`），
序列就是 `293 A playing（揭露）` 緊接 `294 B loadstart`。
v1.98 補上 `LOOP_PARTNER_DELAY_MS = 1200`。
**加任何背景工作之前，先問「它會不會落在翻牌的那 400 毫秒裡」。**

⚠️ 連帶的教訓：**「影片沒卡」不等於「畫面沒卡」。** 診斷面板一開始只量
`<video>` 的 `currentTime`，量不到主執行緒被佔住 —— 現在有
`🧊 畫面凍住 Nms`（`requestAnimationFrame` 間隔 >120ms）。

### ⑫ 3 秒的 `loop` 每繞一圈就重新緩衝一次（v1.95 已修）

等待片是 `video.loop = true` 的 3 秒片。**每次繞回開頭 WebKit 都會掉回 rs1、
丟一個 `waiting`** —— **就算整支都在本機 blob 裡也一樣**，跟網路完全無關。
實測（雲端）`循環頓 4 次 @349／3364／6392／9425ms`，間隔正好 3030ms＝片長，
單次約 12ms（桌機看不出來，手機未知）。

**這條的意義**：它是**結構性的、只要盯著一個角色就每 3 秒重複一次**，
所以 v1.80〜v1.93 那十幾個版本的載入修正**再怎麼對也不會讓人覺得變順**。
查「還是會卡」時，先問一句：**「是點下去那一次，還是每隔幾秒重複一次？」**

**v1.95 的解法與量到的數字**（同一支片、同一顆 blob、跑 13 秒）：

    A) loop = true              4 次餓死   seeking→waiting ×4
    B) ended → currentTime = 0  4 次餓死
    C) 提早 0.15s 就 seek        4 次餓死
    D) 兩顆交棒                 **0 次**

三種寫法一模一樣，因為**兇手是 seek 本身，不是 `loop` 這個屬性** ——
別再試「換個方式 seek」，沒用。唯一的解法是讓可見的那顆**從頭到尾不 seek**：
`startSeamlessLoop()` 讓夥伴那顆提前 0.25s 接上來，seek 留給藏起來的那顆。

三個一定要記得的坑：
1. **不要用 `activateVideo()` 交棒** —— 它會呼叫 `opts.onShown`，
   game.js 在那裡面重啟角色 BGM，變成每 3 秒重啟一次音樂。
2. **夥伴那顆會被 confirm 預熱佔住** —— 所以 confirm 改用 `storeBlob()`
   只抓位元組，不佔任何 `<video>`。
3. **沒有 blob 就不要交棒**（`hasBlob(src)` 護欄）—— 夥伴載同一支會
   跟伺服器**再抓一次整支**，在 130KB/s 上等於雙倍流量。

### ⑬ 診斷工具不能要求人「在對的時機截圖」

第一版面板是會滾動的日誌，結果睿哥的四張截圖裡三張沒有面板、一張只剩尾巴。
**摘要要用釘住的**：永遠顯示「上一次點擊」的結論，隔多久截圖都帶著答案。
日誌放畫面最下面，中間讓出來給要觀察的東西。

### ⑭ iOS 會回收沒在播的 `<video>` 的解碼資源

睿哥 2026-09-08：「**有些第一次不卡，但選到別人後換回來卻卡了。**」

`setSource()` 有一條快路：`if (target.dataset.src === src) return;` ——
「網址一樣就不必重設」。第一次選某角色時網址不同，會走完整流程含 `load()`；
**換走再換回來時網址一樣，直接 return**。

但 **iOS Safari 會回收沒在播的 `<video>` 的解碼資源**：`readyState` 掉回 0
（HAVE_NOTHING），`src` 屬性卻還在。於是媒體已經被收走、**卻再也沒有人
呼叫 `load()`**，只剩 `play()` 自己慢慢把整套弄回來。

規則：**「網址一樣」不代表「媒體還在」。** 走快路之前先看 `readyState`，
是 0 就 `load()` 一次。

⚠️ **Chromium 不會這樣回收**，所以雲端完全測不出來
（回訪實測 353〜411ms 跟第一次一模一樣）。這條是照睿哥的描述在程式裡
找出來的 —— **「第一次好、回來就壞」這種描述本身就是很強的線索，
它幾乎一定代表某個「已經做過就跳過」的快路踩空了。**

### ⑮ 「多久翻牌」和「掉格」是兩回事，別再只量前者

睿哥 2026-09-08：「你能不能認真測試」—— 他是對的。當時所有測試量的都是
**多久翻牌**與 **`currentTime` 有沒有前進**，而這兩個**都抓不到掉格**：
影片可以 `currentTime` 正常前進、主執行緒也不忙，而合成器在丟畫格。

要量的是這兩項（面板已內建）：
- `getVideoPlaybackQuality()` → `droppedVideoFrames`
- `requestVideoFrameCallback()` → **真的被畫到螢幕上**的畫格間隔
  （30fps 正常約 33ms，超過 100ms 就是肉眼看得到的頓）

⚠️ **兩項在雲端都量不出來**（headless Chromium 不做真正的合成，
`totalVideoFrames` 回 -1／0），**但 iOS Safari 上是真數字**。

### ⑯ 「第一次不會、之後開始」＝有東西在累積

這種形狀幾乎一定是每次操作留下一點殘留。已經抓到一個：
`audioDirector.js` 的 `crossfadeMusic()` 淡出後只 `stop()` 了來源，
**那顆 `GainNode` 卻永遠掛在 `musicBus` 上** —— 角色 BGM 是第一次點角色
才啟動、之後每換一個角色就換一次曲子，等於**每點一個角色就留一顆死節點**。
（v1.97 補了 `disconnect()`。）

**最大的一個是解碼後的音訊**（v1.99 修）：角色 BGM 每支 12 秒 44.1kHz 立體聲，
**解碼成 `AudioBuffer` 是 4.2MB**，而 `bufferCache` 原本**沒有上限也沒有淘汰** ——
每點一個新角色就多留一支。14 支＝**56.6MB**，連場景 BGM 共 **90.4MB**。
現在是 `HERO_BGM_CACHE_MAX = 3` 的 LRU（只淘汰角色 BGM，正在播的不丟；
壓縮原檔留著，換回去是本機重解不上網）。

⚠️ **算一下再說。** 「一支 145KB 的 mp3」聽起來很小，
解碼後是 **29 倍**（12s × 44100 × 2ch × 4 bytes）。快取音訊一定要設上限。

排查順序：先數會累積的東西（解碼後的音訊、解碼中的 `<video>` 數、音訊節點、
JS 堆），再去看單次操作的耗時。診斷面板有 `音訊快取` 那一行可以直接看。

### ⑰ 量測工具自己也會騙人

`requestVideoFrameCallback` **暫停中不會觸發**。無縫循環會把不在畫面上的
那顆暫停，於是基準值停在三秒前，一交棒回來就算成一次 2833ms 的假大跳
（20 秒生出 7 筆全是假的）。基準值要**存在元素上、由輪詢在「不是 active」
時清掉** —— 不能只靠回呼自己清，它在該清的時候正好不會被呼叫。

同理，翻牌期間影片是 `opacity:0`，本來就沒有畫格，也要排除；
否則「翻牌期間的空白」和「播放中掉格」會混成同一個數字。

**加一個量測欄位時，先想清楚它在什麼情況下會給出假數字。**

### ⑱ 揭露與交接不能把「準備中」當成「已完成」（v1.100）

- `revealPrepared` 前已在隱藏層暫停／歸零，揭露時不要再設 `currentTime=0`；同值賦值也會觸發非同步 seeking。本機 WebKit 的 19 次換角由 19 次可見 seek 降到 0。
- 冷載入先用頭像翻開時，影片接手前也要在隱藏層等倒帶／播放就緒；超時保留頭像，不硬露出停住的影片。
- 循環夥伴 `readyState>=3` 不代表 `play()` 成功。等 playing／Promise 成功才切 is-active；延遲、拒播、無回應時舊片繼續 loop，切角須取消在途交接並檢查來源／token。
- `onShown` 是翻牌中點，不是翻牌完成；角色 BGM 的解碼也要等完整翻牌後再開始，過期角色排程要取消。
- 預抓「不中止」與「單工」要一起做：重啟排程前等前一個 controller 釋放，否則換頁 800ms 後可能重抓還沒完成的同一支。
- 音效自然 ended、主動 stop、啟動失敗都要 disconnect source／gain；不能只清 BGM 淡出那條路徑。回歸測試須模擬 stop 不送 ended，並驗證延遲 ended 不會誤清新群組。

測試：`node tools/test_audio_cleanup.cjs`；`HF_PLAYWRIGHT=<已安裝 Playwright 的絕對路徑> node tools/test_pick_runtime.cjs`。後者使用 WebKit 真實 H.264、預設連 8888，靜音但不略過音訊解碼；`HF_TEST_URL=http://127.0.0.1:8901/ HF_COLD=1` 可搭配 `tools/slow_server.py --port 8901 --kbps 130`，`HF_RAID=1` 另跑完整魔王討伐。這不是實體 iPhone／Tesla 的掉格保證。

### ⑲ 卡頓跟畫質無關；壓縮只買到「等待變短」

**證據**：v1.78／v1.82 兩次重壓之後睿哥照樣回報卡，而卡的當下面板是
`blob rs4 1段 0.0-3.0/3.0` —— **整支在本機、根本沒動用網路**。
真正的成因（多餘的 seek、循環接點、翻牌中點解碼 BGM、音訊快取 90MB）
**換成 1080p 或 240p 都一樣會發生**。

壓縮唯一買到的是**第一次點未預抓到的角色要等多久**（睿哥約 130KB/s）：

    等待片平均      第一次點      預抓 14 支
    CRF 29  400KB     3.1 秒         43 秒   ← 實測不能用，聖騎士 687KB 超時
    CRF 32  272KB     2.1 秒         29 秒   ← 現行
    CRF 36  168KB     1.3 秒         18 秒   ← v1.82，為追卡頓而壓，已收回

**別再為了「卡」去降畫質。** 要降只為了縮短等待，而且要先量。

還原影片畫質時的兩個檢查：
1. **還原點會不會把換掉的素材帶回來**（武鬥宗師是 v1.74〜v1.76 換的，
   早於 v1.78 重壓，所以還原到重壓前是安全的 —— 但要抽首幀目視確認）。
2. **poster 要不要重製**：比對 poster 與還原後首幀的平均像素差，
   跟「poster vs 現行影片」的差值一樣就不必重製（實測 15.02 vs 15.05）。

### ⑳ 舞台片（開場／降臨）本來完全吃不到預抓

`playStageClip()` 直接設 `video.src = versioned(url)`，**不經過 `blobSrc()`**；
而它的預抓是隱藏的 `<video preload="auto">` —— 地雷區 ④ 早就量過**那種預抓
不會被真正播放的元素重用**。所以「預抓過的」魔王降臨片上場時還是從頭下載
（程式裡那句「點進入戰場後約 4〜5 秒全黑」就是這個）。

修法（v1.102）：`videoPlayer` 匯出 `resolveSrc()`，舞台片走它；
預抓改用 `storeBlob()`，並**接進選角畫面那條單工佇列、排在 14 支待機片之後**
（模式頁只停留兩三秒，1386KB 在 130KB/s 要 10.7 秒，非得更早開始）。
實測：降臨段 13 秒 → **6041ms（＝影片長度，零等待）**。

**通則：任何「直接設 `video.src`」的地方都要問一句「它吃得到 blob 嗎」。**

### ㉑ 還原舊素材前先確認那是不是同一段影片

v1.102 想把分隊開場拉回高畫質，git 裡最大的版本是 1540KB —— 但那是 v1.53
的舊片，還原出來是 **800×450 橫式**，而現行是 720×1280 直式。**不同的影片。**

檢查方式：抽首幀比對平均像素差（同一段重壓過的片約 2 以下；不同片會很大），
並且看解析度。同理，`final` 現行已是 10.04s／1584kbps，重壓幾乎沒動它
（+5%）—— **重壓已壓過的檔案不會還原畫質**，要更好只能回睿哥 Mac／Drive
上的原始素材重編。

### ㉒ 畫質的硬上限是「解析度」，不是碼率

**所有影片素材都只有 720 寬**，而睿哥的 iPhone 約 **1170 實體像素** ——
影片一直被**放大 1.6 倍**。碼率拉再高也救不了放大造成的糊。

判斷方法：先看 `kb/s` 再決定要不要加碼率。
- `attack 641 kb/s`（720×1264）→ 明顯偏低，拉到 912 有感（v1.103 做了）
- `final 1573 kb/s`（720×1080）→ 碼率已經夠，糊的是解析度，加碼率沒用

**要真的更清楚只有一條路：用原始素材重編成 1080 寬。**
原始檔只在睿哥的 Mac／Google Drive（repo 只有 `mobile/` 與 `poster/`），
**雲端 session 做不到，這件事要在 Mac 上做。** 重壓已經壓過的檔案
**不會**還原畫質 —— `tools/recompress_videos.py` 只會把它壓更小。

工具（v1.104 新增，**在 Mac 上跑**）：

    python3 tools/upgrade_from_source.py --src <原片> --dest boss/arrival
    python3 tools/upgrade_from_source.py --src <資料夾> --kind final --dry-run

high profile／faststart，自動重製 poster 並跑 `gen_asset_versions.py`＋
`sync_build.py`。**來源比目標窄會自動不放大。**

**⚠️ CRF 21 是陷阱，工具預設已改成 32（2026-09-11 在真實原片上量的）**：
final 1080 寬 CRF 21 → **16〜25MB／支**，130KB/s 的 4G 要 170 秒，逾時後
final 根本不會播。paladin 的階梯：1080/CRF26 11.0MB、28 8.2MB、30 6.1MB、
**32 4.6MB**；**900/32 3.4MB**；現行 720 是 1.9MB。**final 只有戰鬥開始後
17〜25 秒的前置時間還要跟攻擊片搶頻寬，1080 寬任何 CRF 都抓不完** ——
睿哥拍板 **final 用 900 寬、魔王降臨用 1080 寬**（它在選角階段就預抓，1.8MB 夠）。

**能升的只有原片本身夠寬的**：14 支 final 原片只有 5 支是 1152 寬
（assassin／paladin／dark_fighter／amazon／orc_archer）；其餘 9 支原片就是
720〜768，重做只會變大不會變清楚，其中 3 支 `copy_*.mov` 還帶 CapCut 浮水印
（現行版本是裁掉的），重做會把浮水印帶回來 —— **不要碰**。

**抽牌與卡片分組的原片在 `角色圖/魔王降臨/` 夾裡**（睿哥 2026-09-11 補的，跟魔王降臨放同一夾，檔名是 UUID 認不出來 —— **用首幀差對現行檔比對**：`e2c2e0c2`＝抽牌 1152×1728、`b649f998`＝卡片分組 720×1280）。抽牌已升 1080×1620；卡片分組原片只有 720 寬升不了，從原片重做只把碼率 743→918 kb/s。**Drive 剛上傳的檔案，這台 Mac 的清單可能好幾小時後才更新**；`find -mmin` 看不到不代表沒有，直接 `ls` 該資料夾強制重新列舉，或請睿哥貼路徑。

### ㉓ 還原舊素材：一定要逐版量「解析度」

`teams/intro` 的歷史版本看起來一個比一個大，實際上是**不同的影片**：

    8a2108f  720x1280   537KB   ← 現行（重壓過）
    bcd858c  720x1280   547KB   ← 同一支的最佳版
    e1c0fa7  800x450    554KB   ← 橫式，不同影片
    e87e53c  544x966    636KB   ← 不同影片
    584ec61  640x1136  1540KB   ← 不同影片（最大，但完全不能用）

**只看檔案大小會挑到最糟的那一個。** 逐版 `ffmpeg -i` 量解析度，
再用首幀平均像素差確認（同一支重壓過的約 2 以下）。

## 雲端 session 測不到的四件事

**別把「Chromium 上沒問題」當成沒問題。** 這四項每一項都造成過誤判：

| 測不到什麼 | 怎麼繞過 |
|---|---|
| **H.264 解碼** —— 所有 `.mp4` 都 `ERR_FAILED` | 用 ffmpeg 轉一支 VP9/WebM 當替身；`tools/slow_server.py --webm-dir` 會自動替換 |
| **Range 回 206** —— `python3 -m http.server` 回的是 200 | 用 `tools/slow_server.py`（會正確回 206，並可 `--kbps` 限速） |
| **安全區 `env(safe-area-inset-*)`** —— headless 全是 0 | 注入 `inset: 59px 8.8px 34px` 之類的值模擬 iPhone |
| **快取行為** —— 少了 `ETag`／`Last-Modified` 就不會快取 206 | `slow_server.py` 已補上；自寫測試伺服器要記得 |

**還有一個最基本的**：先看睿哥截圖上的 `.build-stamp` 對不對。
他曾經卡在舊版五個版本，而我一直在查程式。

---

## 工具速查

```bash
python3 tools/replace_hero_videos.py <id> --finish --wait/--confirm/--attack/--final/--victory <檔案>
        # 換角色影片：轉檔＋重製 poster＋版本號全套。--scan 可列出 Drive 素材夾
python3 tools/recompress_videos.py --crf 32 [--kinds wait]   # 整批重壓（壓不贏會保留原檔）
python3 tools/gen_asset_versions.py                          # 重算逐檔雜湊表
python3 tools/sync_build.py                                  # 版本印記同步到 build.txt
python3 tools/slow_server.py --port 8901 --kbps 130 --webm-dir <dir>   # 測試伺服器
```

**雲端讀不到 Google Drive**（連接器登入的是別的帳號）。素材要嘛睿哥貼到對話裡，要嘛在 Mac 上做。

---

## 還沒解決的

- **v1.100 已修正可重現的揭露倒帶、循環搶先交接、BGM 翻牌衝突與資源清理，暖／冷載入回歸通過；仍待睿哥實體 iPhone／Tesla 驗收。** 若仍卡頓，帶 `?debug=1` 截圖或錄影，區分「載入等待」與「播放中掉格」，看 A／B 事件與畫格間隔，不再以桌面結果宣稱實機已解決。
- **要查手機上的影片問題，先開 `?debug=1`**（`js/debugHud.js`，平常完全不啟動）。
  它把 `<video>` 的來源（blob／net）、readyState、緩衝段數、`waiting`／`stalled`、
  真正的 STALL 與實測網速畫在畫面上。**沒有它就只能靠螢幕錄影反推，v1.80〜v1.88
  八個版本都是這樣猜錯的。** 看的時候務必認 A／B 標記：舞台是兩顆 `<video>` 交替，
  不分清楚就會把「另一顆在預抓」誤讀成「這顆在重抓」。
- 五個沒被引用的 JS 檔要不要刪。
- `AGENT_HANDOFF.md` 的數字是 2026-08-01 的（當時 13 角、v0.94），**當背景讀，別當現況**。

---

## 維護這份文件

**只寫「還會咬人」的東西。** 已經修掉而且不會再犯的，留在 `PROJECT_NOTES.md` 的日誌就好。
每次踩到新的坑、或推翻上面某一條時，就更新這裡 —— 這份文件的價值在於**短**。
