# 專案共享筆記：Heroes' Fate 可玩原型

## 專案是什麼

依 GDD 實作的瀏覽器可玩原型（HTML/CSS/JS），展示聚會抽籤＋命運之輪演出。

## 目前狀態

- 最後更新：2026-08-17（Claude Code）
- 進度：可玩 **v1.55**；手機維持 14 張選角卡 7×2，平板（≥700px）改為最大 920px 的直／橫向自適應版面，選角採左側 Sora 舞台＋右側 4 欄英雄；角色卡、玩家圓點、戰鬥 HUD 與分隊英雄節點改為持久 DOM 更新；選角與魔王戰媒體採有上限的序列預熱，低階／省流量裝置自動降載高成本裝飾；14 角色影片與角色 BGM／招式／勝利音效均完整保留
- 路徑：`/Users/longxia7hao/Heroes_Fate/`
- **開工必讀（會自動載入，跨裝置都有效）：`CLAUDE.md`**；架構長篇說明：`AGENT_HANDOFF.md`（內文現況數字是 2026-08-01 的舊值，只當背景讀）
- **GitHub（2026-08-13 建立）**：`longxia7hao-dev/heroes-fate`（公開），版本控制 265 檔約 73MB；`assets/anim`（128MB）與舊高碼率原片 `assets/videos/{wait,confirm,victory}`（272MB）由 `.gitignore` 排除，只留本機與 Drive
- **正式公開網址（取代 cloudflared 隧道）**：<https://longxia7hao-dev.github.io/heroes-fate/> —— push 到 `main` 約 1 分鐘後自動上線，**不需要 Mac 開機**。隧道網址每次重開都會變，已不再是主要入口
- **收工必 `git push`**，否則睿哥手機看不到改動；雲端與本機動工前都先 `git pull`，避免兩邊各改一份
- 本機：`http://127.0.0.1:8888/index.html`（Cmd+Shift+R）
- 區網手機：`http://<Mac-IP>:8888/index.html`（勿用 127.0.0.1）
- 公開隧道：見 `PHONE_ONLINE.txt`（需 cloudflared + server 同時開）
- 影片主路徑：`assets/videos/mobile/{wait,confirm,attack,final,victory}/` 與 `mobile/boss/arrival.mp4` + `manifest.json`（Sora H.264 行動版）
- 聲音主路徑：`assets/audio/{bgm,sfx,heroes/{bgm,attack,victory}}/`（約 4.44MiB，v1.50 新增抽牌曲 `bgm/order.mp3`）；由 `js/audioDirector.js` 以 Web Audio 處理 iOS 解鎖、角色／場景淡入淡出、分組停止與音量控制；v1.47 起先啟動眼前 BGM／cue，再以 idle 序列暖核心音效，角色音效只序列暖參戰者 attack＋勝者 victory，不再同時發出最多 26 個請求
- 公開網址（使用者角色 BGM 精華版）：`https://vocational-brush-org-exceptional.trycloudflare.com/index.html?build=character-bgm-20260811`
- 素材來源（單一事實）：既有 13 位英雄來自 Google Drive `我的雲端硬碟/英雄命運抽（heroes fate)/角色圖/`（folder id `1EZhAFT1mTkOagNvm2iqkqDV3hIjAFfNd`）五個子夾；第 14 位龍騎士由 Codex 內建 ImageGen 產生一致角色關鍵畫面，再轉為現行手機影片、海報、頭像與專屬音效
- 待選片：14 支全為約 3.04s 直式新片（**2026-08-12 全部換成使用者修正版**，720×~1270，CRF 29 共 5.8MB）
- 攻擊魔王動畫：`assets/videos/mobile/attack/*.mp4`（14 支約 3.04s，CRF 29 共 8.1MB）— 魔王戰的英雄切入用這個，缺片才退回 confirm；v1.47 起只預熱下一支，背景 attack `<video>` 永遠最多 1 個，勝者 final 延到第一支攻擊交棒後才預熱
- 選角卡縮圖：`assets/heroes/portraits/*.jpg`（14 張 240×322 頭像，共 188,335 bytes）；舊的 `assets/heroes/thumbs/` 已停用
- 播放倍率：確定選擇片 **1× 原速**（3.00–3.04s 完整播完，硬上限 4200ms）；攻擊／勝利短片維持 **1.3×**（完整播完不截斷）
- 魔王戰演出順序：集結→**魔王降臨影片（7s，缺片才退回立繪動畫）**→全軍突擊→**各角色攻擊切入連播**→魔王怒吼→**命運一擊（全員按住 3 秒）**→**勝者的打敗魔王專屬影片 final（1.3×，等待 8s／播放上限 14s）**→角色勝利短片→結果頁；**命運審判也會播 final**（只是收在「審判降下……」而非勝利片）；**攻擊輪播時點一下切入層可跳下一位（`click`＋前 600ms 防誤觸），不點則自然播完**；首幀 poster 見 `assets/videos/poster/{attack,victory,final}/` 與 `poster/boss/arrival.jpg`（43 張 1.3MB，2026-08-13 由現行影片全數重製）
- 素材快取：`tools/gen_asset_versions.py` 會為影片、音檔、立繪、選角頭像與 poster 產生逐檔內容雜湊（`js/assetVersions.js`）；查不到才退回 `ART_VERSION=4`／全域媒體版本。換素材後必跑工具，不要回退成整包加版號
- 設定生效時機：`命運卡`／`命運一擊`／懲罰題庫是在按設定視窗的**「關閉」**時才寫回 `state.opts`（`#settings-close` handler），不是勾選當下
- 缺片狀態：**無**（wait／confirm／attack／final／victory 各 14 支；manifest 共 70 條引用）
- 模式：**魔王討伐／命運排序／命運分隊(2-4)**（2026-08-15 依睿哥指示移除命運配對／命運審判／命運淘汰的卡片；**doom 的程式碼仍在**，因為命運卡「逆命」會把魔王討伐換成審判）；每局開場翻**命運卡**（4 張，可關；已移除「雙重命運」，一局只有一位勝利者）；揭曉前有**命運一擊**（可關）；結果頁可抽**懲罰任務**、看**今晚戰績**、產**命運卷軸**圖卡
- 影片體積：`mobile/` 58.71MiB＋ `poster/` 1.59MiB；舊高碼率原始檔仍在 `assets/videos/{wait,confirm,victory}/`，不在現行 manifest 主路徑
- 戰鬥素材：`assets/bg_battle_arena_v2.jpg` + 延遲載入的 `assets/boss_model_v3.{webp,png}` + `assets/ref_battle_mobile.mp4`；後者是遊戲實際只播的前 1.67s 手機版（125,278 bytes、無音軌、faststart），完整 9.93s 原片仍保留於本機忽略檔 `assets/ref_battle.mp4`
- 角色選角 BGM：來源為 Google Drive `我的雲端硬碟/英雄命運抽（heroes fate)/伴奏/`；首頁 1 首＋角色 14 首皆由約 30.8s 原片擷取 12s 最強辨識段，輸出 44.1kHz stereo 96kbps、約 -14 LUFS、TP 不高於 -1.5dB，短淡入／淡出後循環；重建工具 `tools/extract_character_bgm.py`
- 聲音來源：使用者製作的開始選單伴奏與 14 角選角 BGM 使用上述 Google Drive 來源；原有場景 BGM／UI／魔王共用聲效由 `tools/generate_audio.py` 原創程序化生成；既有 13 角攻擊與勝利音效由使用者 Google Drive 對應 Sora 原片抽出；龍騎士兩支專屬音效由專案既有原創 attack／chime 素材重新混音
- GDD：`/Users/longxia7hao/Heroes_Fate_GDD/GDD.md`
- 勝利片：14 支全齊（2026-08-11 新增 `dragon_knight`）

## 睿哥的指示

- 按 GDD 企劃製作；參考片強化魔王討伐
- 角色選擇要有建模立繪；不需要輸入名字

## 變更日誌

- 2026-08-17｜Claude Code｜**v1.55 分隊開場影片換成第二版（法師排卡陣），走 `contain`**：①睿哥換了一支新的：法師施法把一張張卡牌排成方陣，比第一版的金幣更貼「分隊」。原片 1280×720／15.2Mb/s／11.5MB／6.04s。②**⚠️ 這支是橫式的（16:9），所以用 `fit: "contain"`。** 舞台是直向全螢幕、預設 `cover`；16:9 塞進 393×660 的直式框，cover **只會留下中間約三成的寬度** —— 左邊的法師與右邊的卡陣剛好各被切掉一半，構圖整個毀掉。這支的重點橫跨整個畫面寬度，**裁不得**。`fit` 這條路正是 v1.46 拿掉橫式魔王時**刻意留著**的後路（BOSS_ARRIVALS 上方註解寫「下次再收到橫式素材就不用重寫」），這次就是那個下次 —— 一行 `fit: "contain"` 就接上了，沒有改任何機制。③**代價要講清楚**：contain 之後影片只佔畫面中間約三分之一（393×221），上下是黑的。**要滿版就得請睿哥給直式版**（他在 v1.46 為了岩石巨獸做過同樣的事，有前例）。我沒有用「模糊的第二層影片去填黑邊」那種常見手法 —— 那要再開一個 `<video>` 解碼器，他才剛抱怨過卡。④壓成 **800×450 CRF 30 共 555KB**（顯示寬度 393pt＝786 實體像素，800 剛好不必放大），比上一版的 637KB 更小，而且**片長從 10.04s 縮到 6.04s**，等待也短了。演出的逾時同步從 11000ms 調成 7000ms。⑤`-map 0:v:0` 不能省：這支還夾了一軌 **mjpeg 封面圖**，不指定會被一起帶進輸出。⑥**音軌拿掉，但這支跟上一支不一樣**：上一支量到 -46.9 LUFS（等同無聲），這支是 **-20.1 LUFS，是有東西的**。舞台影片一律靜音播放（iOS 自動播放的硬性條件），所以照樣拿掉；**睿哥想要的話可以像抽牌那首一樣抽出來當專屬 BGM**，那要另外做。⑦快取：game.js?v=77→78、版本表 ab98391009；印記 `v1.55 · 0817-2358`。⑧**實測**：poster 與影片都套到 `object-fit: contain`（`objectFit: "contain"`，框 393×660），截圖確認法師與卡陣**兩邊都完整沒被裁**；分隊四組人數×隊數的結果頁全部維持零捲動。
- 2026-08-17｜Claude Code｜**⚠️ 雲端 session 的 git 有過一次「commit 推上去了、本機卻看不到」**：v1.54 推送時終端顯示 `584ec61..e87e53c main -> main` 成功，但**本機 reflog 完全沒有那筆紀錄、`git cat-file` 也說物件不存在**，`git log` 停在 v1.53。查 `git ls-remote` 才確認**遠端兩個分支都確實是 e87e53c，v1.54 有上線**，是本機這份 clone 掉了那個 commit（當時剛用過 `git worktree add` 開了一份 v1.53 的副本來做 A/B 比對，事後 `git worktree remove --force`）。**教訓：雲端 session 用 `git worktree` 做比對之後，下一次動工前一定要先 `git ls-remote` 對一下遠端真正的 HEAD**，不要只信本機的 `git log` —— 差一點就在 v1.53 的基礎上重做一版，把 v1.54 的修正整個蓋掉。復原方式：`git fetch` 把物件抓回來，再 `git reset --hard origin/<branch>`。

- 2026-08-17｜Claude Code｜**v1.54 修「影片超卡、選角影片跑不出來」——兩支影片在搶同一條 4G**：①睿哥回報 v1.53 之後影片超卡，連選角的等待／確定影片都跑不出來。②**根因一（真正的兇手）：用不到的魔王降臨片會跟開場影片同時下載。** `show("mode")` 會**投機預抓**一支魔王降臨片（實測 `arrival.mp4` 是 **1,419,794 bytes**），但原本要等**整段演出跑完**的 `finally` 才釋放。於是玩命運分隊／命運排序時，那支**完全用不到**的 1.4MB 會跟該模式自己的開場影片**整段並行搶頻寬** —— 分隊那 10 秒影片播多久，它就搶多久。修法：`revealFateCard()` 之後只要 `result.mode` 不是 boss／doom，就**立刻** `releaseArrivalPrefetch()`（`removeAttribute("src") + load()` 會中止進行中的下載）。**放在命運卡之後**是因為「逆命」會把 boss 換成 doom，那兩個模式都還要用降臨片；判斷也必須用 `result.mode` 而不是 `mode`（前者才是命運卡生效後的真正模式）。③**根因二：分隊開場影片太大。** 1.54MB 對弱訊號 4G 太重。**從原始上傳檔重壓**（不是拿我自己壓過的再壓，避免二次失真）成 544×966 CRF32：**637KB（-59%）**，poster 一併重製。抽三個候選在手機顯示尺寸下並排比對過，畫質看不出差別。④**這兩件加起來**：選分隊時「開場影片播出來之前必須到齊的位元組」從 ~2.9MB 降到 ~0.65MB。⑤**查證過但不是原因的**：(a) 版本表只有新增、既有素材雜湊全沒變，**不是把整包影片的快取沖掉**；(b) 選角頁仍是需求驅動（Codex v1.47 的設計沒被破壞），開機到選角只抓 1.86MB，JS 錯誤 0；(c) 預抓元素**沒有洩漏** —— `presentPlay` 的 `finally` 每一種模式都會釋放。⑥**沒有動、也建議先別動的**：`videoPlayer.js` 選角影片的 `canplay` 等待是 1400ms，看起來像「跑不出來」的兇手，但它**不是硬放棄** —— 後面用 `playing` 事件當真正開演的訊號，晚幾秒就緒一樣換得掉立繪。貿然拉長只會讓立繪停更久。⑦**`assets/videos/mobile/order/intro.mp4` 還是 1.09MB，這次沒動**：雲端只有壓過的版本，再壓一次是二次失真；原片（1152×1728／18MB）在睿哥 Mac 上，要縮的話在本機從原片重壓才乾淨。⑧**驗證方法本身踩到的坑**：一開始想用「限速 + 看 `transferSize`」證明中止有效，量出來修前修後都是 0KB —— **那是 headless Chromium 不支援 H.264、自己把請求砍掉**，跟我的修改無關，兩版根本分不出來。改成**直接盯預抓那個 `<video>` 元素的 `src` 何時被清掉**（不依賴解碼能力）才量得準：修前是按下分隊後 2914ms 才放掉，修後 1622ms。⚠️ 而且 **headless 會嚴重低估這個修正的效果** —— 那裡影片根本播不起來，整段分隊演出只有 2.9 秒就結束；真機上影片會實播 10 秒，被搶的就是那整整 10 秒。⑨快取：game.js?v=76→77、版本表 07fa58847d；印記 `v1.54 · 0817-2304`。⑩**回歸**：分隊（4 組人數×隊數）、魔王討伐、命運排序 BGM 進出全部正常，console error 0。

- 2026-08-17｜Claude Code｜**v1.53 命運分隊：睿哥提供的開場影片＋大頭圖呈現分隊結果**：①**「請修正成有 4 隊的選項」—— 這個本來就有，而且是對的。** 模式頁三顆按鈕（2／3／4 隊）都在、都可按，`rng.js` 也已經 `Math.min(4, …)`；實測 4 人要 4 隊 → `1/1/1/1`、6 人要 4 隊 → `2/2/1/1`、13 人要 4 隊 → `4/3/3/3`，人數不足時自動降到人數（3 人要 4 隊 → 3 隊），全部正確。②**睿哥真正踩到的是別的**：他附圖寫「3 隊」卻只看得到紅隊與藍隊 —— 那局是 **4 人 3 隊（2/1/1）**，第三隊被捲出 `.result-detail` 的可視範圍了（它是會捲的，而且沒有任何捲動提示）。所以「4 隊選項不見了」的感覺，其實是**看不到第 3、4 隊**。這由第③點一起解決。③**分隊結果改用角色大頭圖**（睿哥：「最後用角色大頭圖直覺的展現分配的結果」）：一隊一排頭像，外框吃該隊顏色 `--tc`，不必讀字就看得出誰跟誰同隊。**同時把上方那張大立繪關掉** —— 它本來顯示「第一隊第一個人」，位置又跟魔王討伐的勝利者一模一樣，很容易被誤會成「這個人贏了」，但分隊根本沒有勝利者；空間全部讓給隊伍。⑤**隊名從「頭像上面一行」改成「頭像左邊一欄」**：只收間距只省得到 11px，離四隊一頁還差 70px —— 真正貴的是每隊都多佔一整行的隊名（約 20px × 4）。並排之後那四行就沒了，也不必縮頭像。隊名欄固定 3.5em，讓四隊的頭像左邊對齊，一眼比得出各隊人數。⑥**開場影片換成睿哥提供的**（一雙手把發光金幣分成幾堆，正好是分隊的意象），取代原本「英雄小圖洗牌 8 幀」。原片 640×1424／13.3MB，**先裁掉上方空帘幕與下方墨水瓶**（`crop=640:1136:0:288`）再壓 —— 原片比例 0.449 非常瘦長，直接讓舞台的 `cover` 去裁會把手跟金幣切掉；裁成 0.563 之後 `cover` 只需再修上下各約 19px。CRF 30 共 **1.54MB（-88%）**，poster 一併產出，可一鍵跳過。音軌拿掉：量到只有 **-46.9 LUFS**（等同無聲），留著純粹是負重。⑦**⚠️ 這次最值得記的坑 —— `<img>` 的 `width`／`height` 屬性會贏過 `aspect-ratio`**：頭像寫了 `width="240" height="322"`，那兩個屬性會被瀏覽器當成「呈現層提示」寫進 UA 樣式（等同 `height: 322px`），**壓過 CSS 的 `aspect-ratio: 1`**。結果頭像變成 47×322 的長條，四隊把名單撐到 **1512px**。更陰的是**這個提示不在 `document.styleSheets` 裡** —— 我逐條掃過所有樣式表找設 height 的規則，一條都沒找到，只能從「computed height 322px 但沒有任何規則設它」反推。修法是補一行 `height: auto`。⑧快取：revamp.css?v=29→31、ornate.css?v=16→18、audioDirector.js?v=20→21、game.js?v=75→76、版本表 f9c47b1644；印記 `v1.53 · 0817-2142`。⑨**實測**（跑完整局，4 種組合）：393×660 下 **4 人 4 隊、4 人 3 隊、13 人 4 隊全部「所有隊伍完整看得到、零捲動」**（修之前 13 人 4 隊要捲 1180px）；360×600 的 13 人 4 隊仍需捲 31px（最小螢幕＋最多人，可接受）；影片與 poster 都回 200，大立繪 `display: none` 且**勝利短片確認沒有被載入**（藏起來也照載的話等於白吃流量）；魔王討伐與命運排序回歸正常，console error 0。

- 2026-08-17｜Claude Code｜**v1.52 命運魔法陣中央的六芒星跟著符文一階一階亮起**：①睿哥：「這個魔法陣中心的六芒星要一樣的跟著越來越亮，最後整個發光。」原本中央那顆星只存在於暗版圖裡，整段蓄力都是同一個亮度（亮版整體只推到 0.6），四顆符文在亮、星卻不動。②**手法沿用四顆符文那一套** —— 同一張亮版圖 `fate_circle_lit.webp`、同一個框，只是遮罩換成中央那顆星，所以位置一定對得上，不必另外切圖，也沒有新素材。③**遮罩尺寸是量出來的**：把亮版圖的「金色程度」（`R*0.6 + G*0.4 - B`）取出來，中央那團金色的 99 百分位半徑是 **17.5%**、中心在 **50% / 49.2%**（略高於幾何中心）。所以實心畫到 16.7%（22% × 76%）剛好包住星體，再羽化到 22% 讓光暈自然散出去。**量完有把偵測結果塗紅疊回原圖確認過** —— 這個專案吃過「以為量到瞳孔、其實量到眉毛」的虧，凡是自動量出來的座標都要畫出來看過再用。④**亮度是四階，門檻跟四顆符文一模一樣**（0.22／0.45／0.68／0.90）：每亮一顆符文，星就亮一階（0.22 → 0.44 → 0.66 → 1），第四顆亮完星整個發光，正好接上揭曉。用四個 `clamp` 相加而不是單一線性值，才會是「跟著符文一階一階」而不是平順淡入。⑤**光暈的模糊半徑刻意寫死、不跟著 `--p` 變**：blur 半徑每幀改變會逼瀏覽器每幀重新柵格化，那正是 v1.43 掉幀的成因；強弱交給 opacity 就夠了。閒置時整個 `.strike-gate` 是 `visibility: hidden`，子樹完全不繪製，所以蓄力以外零成本。⑥**命名有先查過**：`.fc-*` 這個前綴是**魔法陣與命運卡共用的**（卡片那邊有 `.fc-name`／`.fc-desc`／`.fc-kicker`），這個專案已經被同類撞名咬過三次；`.fc-star` 確認全專案唯一才用。⑦快取：revamp.css?v=28→29；印記 `v1.52 · 0817-2028`。⑧**實測**（393×660，手動掃 `--p` 讀 computed opacity）：`--p` 0→1 時六芒星 **0 → 0.22 → 0.44 → 0.66 → 1.00**，每一階都**正好落在對應符文亮滿的那一刻**；截圖三張（p=0／0.55／1）確認暗版、半亮、全亮的樣子；完整跑一局魔王討伐流程無異常，console error 0。

- 2026-08-17｜Claude Code｜**v1.51 名單型結果頁的立繪改成「先吃滿剩下的空間」**：①睿哥附圖（兩人的命運排序結果頁）說「照片太小了，排版要修正一下」。根因：`.result-list` 的立繪高度**寫死 `min(17dvh, 132px)`**，那是照**最壞情況**（13 人 4 隊、名單很長）訂的，於是人少時名單只有兩行、底下空一大片，立繪卻還是被釘在 132px。②修法：把 `flex-shrink` 打開（`flex: 0 1 auto`）＋給一個大的預設高度 `min(40dvh, 290px)`。**flex 的收縮只有在裝不下時才發生**，所以人少時完全不收、立繪拿滿；人多時先縮立繪、名單再自己捲。③**地板取原本寫死的 132px**（正方立繪那版取 120px），保證「再多人也不會比改版前更小」—— 這條是刻意的，不然 13 人時會從 132 掉到 118，變成修一頭壞一頭。④**⚠️ 尺寸一律改用 height 定、width 交給 `aspect-ratio`**：`:not(.has-video)` 原本是用 `width` 定的，那樣 flex 收縮吃不到（收縮作用在主軸＝高度）。⑤**實測**（393×660／360×600 × 2／4／6／9／13 人，共 10 組）：393×660 下 **2 人 75×132 → 130×228（面積約 3 倍）**、4 人 104×184、6 人 87×154、9 人以上回到地板 75×132；**十組全部面板零溢出、按鈕列都在畫面內 45px、零水平溢出**。⑥**量測踩到的坑，下次別再中**：第一版數字全是錯的 —— `#screen-result.active .result-model` 掛著入場動畫 `hf-result-model-in`，**有 90ms 延遲又是 `fill: both`**，所以注入畫面後立刻量，量到的是 `scale(.82)` 變形後的尺寸（computed height 224px 但 rect 只有 187px）。我一度以為是 flex 在亂縮，還去查 max-height。**量有入場動畫的元素一定要先等它跑完**。⑦**試過但放棄的作法**（已在 CSS 留註解，不要再試）：把長名單的列高收窄想讓 13 人多露幾列 —— 沒有用，名單是 `flex: 0 1 auto` 只縮不長，擠出來的空間全被立繪吃掉，看得到的列數一列都沒變。要多露得讓名單 `flex-grow`，但那會在人少時把立繪壓扁，跟這次要的正好相反。⑧快取：ornate.css?v=15→16；印記 `v1.51 · 0817-1946`。

- 2026-08-17｜Claude Code｜**v1.50 命運排序的抽牌演出換上專屬管弦樂**：①睿哥給了一支 Gemini 生成的影片（30.73s，內嵌標題 "The Sigil Unbound"／專輯 "Overture of the Lost Kingdom"／類型 Orchestral Film Score），指定「抽牌的動畫放這音樂」。只取音軌，存成 `assets/audio/bgm/order.mp3`。②**音量有對齊過，不是直接丟進去**：原素材 **-11.6 LUFS**，而現有的 `battle.mp3`／`pick.mp3` 是 **-15.2／-15.4** —— 直接用會比全場其他 BGM 大將近 4dB，一進抽牌段就突然變大聲。用兩段式 `loudnorm`（`linear=true`，先量測再套 `measured_*`）壓到 **-15.8 LUFS／真峰 -4.2dBTP**，跟 battle 差 0.6dB，聽不出來。編碼照現有 BGM 的規格：44.1kHz 立體聲 MP3 112kbps，431KB（battle 是 403KB）。③**新增一對 API `playMusicTrack(key)` / `restoreSceneMusic()`**（`js/audioDirector.js`）：概念跟 `playHeroMusic()` 一樣是「暫時蓋掉場景曲」，差別是**不綁死場景** —— 選角那組寫死 `currentScene !== "pick"` 就退出，演出中途要換得另外一對。下載／解碼／循環／0.68s 交叉淡入都由既有的 `crossfadeMusic()` 處理，沒有重造輪子。④**曲子在開場影片之前就下**：儀式從影片就開始了，音樂晚進來會像配錯段；而且這支 431KB 在 4G 上要抓一下，早一點下才來得及在牌陣出現前接上（`crossfadeMusic()` 會自己等，不會卡住演出）。⑤**⚠️ 改到流程結構，接手的人注意**：`presentOrderCards()` 原本有一行 `if (!wrap) return;` **排在 try 之前**，那條路徑會跳過 `finally` —— 音樂就換不回來、整局都會掛著抽牌的曲子。已經把它移進 try 裡面，`finally` 也改用 optional chaining（wrap 可能是 null）。換回場景 BGM 就寫在那個 `finally`，**不要搬走**。⑥快取：audioDirector.js?v=19→20、game.js?v=74→75、版本表 936f37fd6f；印記 `v1.50 · 0817-1858`。⑦**實測**（393×660 跑完整一局命運排序，每 100ms 取樣 `HF_Audio.getStatus().music`）：`… → battle → **order**（act=cards）→ home` —— **抽牌時確實換成 order 曲，結束後確實換回場景 BGM，沒有殘留**；`order.mp3` 回 200；API 兩支都掛得上去。

- 2026-08-17｜Claude Code｜**v1.49 移除 ACT 3A「全軍突擊」，魔王降臨直接接英雄攻擊**：①睿哥看了畫面直接說「這個畫面不要了，魔王降臨動畫後直接接英雄攻擊畫面」。`presentBossRaid()` 的 ACT 3A 整段拿掉 —— 包含 `setAct("clash")`、「命運之輪 · 全軍突擊！」橫幅、`battle.clash` 音效，以及那支全螢幕戰場空景 `assets/ref_battle_mobile.mp4`。②**注意這裡的來龍去脈，免得後人以為誰刪錯**：那支影片是 **Codex 在 v1.47 才剛納入版控**（為了修 404），結果 v1.48 隔天睿哥就說這個畫面不要了 —— 於是照專案鐵則「版本控制只收遊戲實際在用的素材」把它刪掉，素材數 216→215、版本表雜湊回到 `b2033a7e0a`。**要復原的話原片還在睿哥 Mac 的 `assets/ref_battle.mp4`**（那支是 `.gitignore` 的）。③**HUD 不用補建**：ACT 3A 原本有一次 `renderBattleHud(players)`，但 ACT 2 已經建好、`playAttackSequence()` 每位攻擊時還會再更新 active，拿掉不影響。④**`revamp.css` 的 `[data-act="clash"]` 三條規則變成死規則，先留著沒清** —— `clash` 這個 act 名稱沒有別人在用，留著不影響任何畫面；真要清應該連 `audioDirector.js` 的 `battle.clash` 對應一起處理，另外開一次比較乾淨。⑤程式裡留了一段明確的「不要加回來」註解，寫清楚一併清掉了哪些東西，免得下一個人找不到 `ref_battle_mobile.mp4` 以為是漏刪。⑥快取：game.js?v=73→74、版本表 b2033a7e0a；印記 `v1.49 · 0817-1806`。⑦**實測**（393×660 跑完整一局，MutationObserver 同時記錄 `data-act` 與 `#cut-banner` 文字）：ACT 走 `gather → arrival → attack → fate → reveal → attack → …`（**中間沒有 clash**）；橫幅序列 `魔王降臨 → 玩家 1 出擊 → 玩家 2 → 玩家 3 → 魔王怒吼 → …`，**「全軍突擊」不再出現**；整局沒有任何 `ref_battle` 請求。⑧**驗證踩到的坑記一下**：`setBanner()` 寫的是 **`#cut-banner`**，不是 `#stage-banner` —— 第一次探針選錯元素，橫幅紀錄回傳空陣列，差點被當成「橫幅都沒出現」的通過。選錯選擇器的空結果跟真的通過長得一模一樣，這種檢查要先確認元素抓到了。

- 2026-08-17｜Claude Code｜**v1.48 補上 Codex v1.47 沒動到的兩點：進場全黑、結果頁水平捲動**：①**先講一件事，免得後人重工**：睿哥實測回報的三點裡，第 ① 點（`assets/ref_battle_mobile.mp4` 404）**Codex 在 v1.47 已經修掉了**，而且修得比我原本的作法好 —— 我本來要把那一拍改成純程式演出（等於放棄那段素材），Codex 是**把影片本身壓成 125KB 正式納入版控**。線上實測那支現在回 200。我原本寫在分支上的 v1.47 因此整個作廢，改成只在 Codex 的成果上補剩下兩點。②**順帶更正睿哥的診斷**：他把 404 歸因成「騎士攻擊影片路徑錯誤」，其實那支是 **ACT 3A『全軍突擊』的通用戰場空景**，不是騎士的攻擊片 —— 它剛好排在騎士切入之前，所以看起來像騎士出場時的空白。`assets/videos/mobile/attack/knight.mp4` 一直是好的。③**點「進入戰場」後約 4～5 秒全黑**（Codex v1.47 未處理）：`playStageClip()` 把揭幕（`show` ＋ `video-active`）排在 `waitMediaReady()` **之後**，4G 上等 1.35MB 的降臨片要好幾秒，那幾秒舞台全暗、看起來像當掉。改成**有 poster 就先揭幕**，那段等待顯示的是影片首幀而不是黑畫面，再無縫接上真正播放。沒 poster 就不先揭幕（那只是把黑畫面換個地方黑）。三條離開路徑（沒就緒／被略過／`play()` 失敗）都會走到 `finally` 的 `stopStageVideo()`，`show` 與 `video-active` 都收得乾淨，不會卡住。④**結果頁的水平捲動條**（Codex v1.47 未處理）：兇手是裝飾層 `.result-rays`（revamp.css:2126）—— `position: absolute`、**`width: 150%`**、還一直在轉。關鍵在 **`overflow-y: auto` 一旦設下去，x 軸的 `visible` 就會被瀏覽器改判成 `auto`**，捲動條因此跑出來。面板補上 `overflow-x: hidden`。⑤**另一件不用做的事**：睿哥建議的「統一 14 名角色的攻擊與勝利動畫」**本來就是齊的** —— 逐項清點 wait／confirm／attack／final／victory 各 **14/14**，音效 attack／victory 也各 **14/14**，沒有缺口。⑥快取：game.js?v=72→73、ornate.css?v=14→15；印記 `v1.48 · 0817-1712`。⑦**實測**（393×660 跑完整一局，改用 MutationObserver 記錄，不用取樣 —— **第一次用 120ms 取樣完全漏掉降臨階段**，差點誤判成「這局沒播降臨」）：揭幕當下 `{act:"arrival", shown:true, rs:0, poster:"arrival_golem.jpg"}` —— **`readyState` 還是 0 就已經揭幕**，正是這次要的行為；結果頁 A/B 對照 **修之前 `overflowX:auto`、有捲動條，修之後 `hidden`、沒有**；`ref_battle_mobile.mp4` 回 200。⑧**雲端驗證的已知限制**：headless Chromium 沒有 H.264 解碼，所有 `.mp4` 都會以 `ERR_FAILED` 收場（curl 實測同一批檔案全部 200，是編解碼器不是缺檔）。所以**降臨片在雲端只活了 24ms 就被判定沒就緒** —— 影片實際播放的手感一定要在睿哥手機上驗。

- 2026-08-17｜Codex｜**v1.47 架構／卡頓整體優化＋平板直橫向版面修正**：①**選角載入改為需求驅動**：刪除完整名冊 14 支 wait 的背景掃描（合計 5.44MiB），人數頁只暖 manifest＋14 張小頭像，不再任意抓騎士 wait/confirm 約 726KiB；卡片 `pointerdown` 改走 `HF_VideoPlayer.prime()` 的安全 standby 槽，不會暫停目前角色，`click` 仍只預覽，唯一寫入 `state.players[state.pickIndex]` 的地方維持 `applyPick()`（按「決定」）。②**DOM 不再反覆拆建**：14 張角色卡與玩家圓點初次建立後只切 class／disabled／內容；魔王戰 HUD 相同名冊只建立一次、逐位攻擊只更新 active/winner；分隊洗牌重用每個 slot 的節點，避免 13 人時重解碼頭像與大量 layout。③**魔王戰媒體有上限**：攻擊片由「所有參戰者＋final 同時 preload」改為目前播放時只暖下一支，背景 attack `<video>` 上限 1；final 延到第一位攻擊交棒後才暖；所有 prefetch 有 generation token，skip／cleanup 會中止並釋放 attack/final，poster 改為上限 6 的去重 Map，降臨預抓在實播第一幀就釋放。影片與招式聲都等 `play()` 真正成功後才揭幕／下 cue。④**音訊排程**：首次手勢優先啟動眼前 BGM，11 顆核心 SFX 改 idle 逐檔下載／解碼；參戰角色音訊由最多 26 個並發改為可取消的序列，只暖參戰者 attack＋勝者 victory。⑤**主執行緒／GPU 降載**：一般等待由逐幀 RAF 與 32ms polling 改成 timeout＋單一 skip event；全螢幕影片不再套 filter，影片／hero-cut 顯示時隱藏底下舞台合成樹；煙霧移除動態 blur、魔王 aura／勝利子樹只在可見時運作、轉場移除逐格 clip-path、結果頁只留一套旋轉光芒；省流量／2G／≤2GB／≤2 核裝置自動掛 `perf-lite`。⑥**平板修正**：≥700px 將 `#app` 由 480px 放寬到 920px；選角採左側 Sora 舞台＋右側 4 欄英雄，header/footer 跨滿整列，另有 ≤720px 高度橫向壓縮與 safe-area；一般面板維持 580–760px 適讀寬。⑦**首載延遲資源**：不可達公開模式的魔王立繪改到 survival 真要用時才設 src；勝者預設 279KB 騎士圖、兩張命運陣（134KB）與 720×1080 卷軸 canvas（約 3.1MB bitmap）均改為需要時才配置。⑧**修正式素材 404**：`assets/ref_battle_mobile.mp4` 正式納入版控；遊戲只播前 1.5s，因此輸出 1.67s／464×848／24fps／H.264／無音軌／faststart 的 125,278-byte 版本（原本 2,120,334 bytes，-94.1%），完整 9.93s 原片仍在本機忽略檔 `assets/ref_battle.mp4`。⑨清除 `css/game.css` 檔尾多餘 `}`；逐檔版本表共 216 條、雜湊 `c4935b4002`，快取：game.css?v=4、ornate.css?v=14、videoPlayer.js?v=29、audioDirector.js?v=19、game.js?v=72，印記 `v1.47 · 0817-1642`。⑩**驗證**：指定 `python3 -m http.server 8888 --bind 0.0.0.0` 下入口＋12 個關鍵資源全 200；14 英雄／70 manifest 影片與程式內 231 個靜態素材引用零缺檔；JS 語法、CSS/HTML 結構、125KB 影片完整解碼、300 次 RNG smoke 均通過；`js/rng.js` 未修改且 SHA-256 保持 `9ab55a96f19f162c1380a06cc7b3f2d496fb088aa393d570afeef7ef662f021a`，confirm 仍為 1×。
- 2026-08-16｜Claude Code｜**v1.46 岩石巨獸換成直式版，拿掉 `contain` 的權宜作法**：①睿哥重做了一支 **1088×1920（直式 9:16）** 的岩石巨獸，取代原本 1728×1152 的橫式版。②原本那支是全場唯一的橫式素材，得靠 `fit: "contain"` 才不會被裁掉身體 —— 代價是上下兩條黑邊。**換成直式之後就跟另外兩隻一樣滿版**，那個權宜作法拿掉了。③`fit` 的機制**保留但目前沒人用**：註解已改寫成「下次再收到橫式素材時的後路」，免得後人以為是死碼順手刪掉。④壓成 720×1270／CRF 30：**1425KB（舊的橫式版是 573KB）** —— 直式的像素多了一倍多，體積跟著上去是正常的。三支降臨片合計約 4.3MB（第一次各自被抽到時才下載）。poster 一併重製。⑤快取：game.js?v=70→71、版本表 b2033a7e0a；印記 `v1.46`。⑥**實測**（固定抽中岩石巨獸）：`{act:arrival, src:arrival_golem.mp4, **fit:cover(預設)**, tap:true}`，預抓與實播一致，console error 0。
- 2026-08-16｜Claude Code｜**v1.45 更新提示改成畫面最上方的整條橫幅**：①睿哥回「一樣都沒有改變，還是同一隻魔王」。查證：`af7caa2`（v1.44）**部署成功、線上 `build.txt` 就是 `v1.44 · 0816-1106`**，本機實測 7 局經「再來一局」重玩也確實在換魔王 —— **程式是對的，問題在他手機拿到的還是舊的 `index.html`**。②**這個坑已經吃掉四輪以上**（v1.11／v1.24／v1.25，加這次）。根因是提示長成「首頁最下面一顆小藥丸」，**要捲到底才看得到**，等於沒有用。改成 **`position: fixed` 的整條橫幅，固定在畫面最上方**、紫色呼吸動畫，不可能錯過。③**按鈕從首頁面板搬到 `<body>` 直屬層** —— 原本放在 `#screen-home` 裡，一切換頁面那個 section 就被隱藏，玩到一半根本看不到提示。現在每一頁都在。④**⚠️ 而且必須排在 `js/game.js` 之前**：`watchForNewBuild()` 在腳本載入當下就去 `$("#build-update")`，我第一次把它插在 `</body>` 前（腳本之後），那時還沒進 DOM、抓到 null 直接 return，**整個提示永遠不會出現**（量到 hidden 一直是 true）。⑤**順帶釐清一件事**（差點又講錯）：**魔王討伐全程不顯示魔王立繪** —— `presentBossRaid()` 裡沒有任何 `boss.classList.add("show")`（睿哥 2026-08-13 要求拿掉的），所以那一局唯一的魔王畫面就是 6 秒的降臨影片。我一度以為他看到的是不會變的立繪，還照著做了兩張去背立繪，查清楚後已全部還原、素材也刪掉，沒有留下沒人用的檔案。⑥快取：ornate.css?v=12→13；印記 `v1.45`。⑦**實測**（攔截 `build.txt` 回傳假版本）：首頁／人數頁／選角頁**三頁橫幅都在**、`position: fixed`、寬度滿版 393px、`top: 0`，點下去確實重新載入，console error 0。
- 2026-08-15｜Claude Code｜**v1.44 修「魔王永遠不換」——抽籤掛錯地方了**：①睿哥：「測試了好幾輪，怎麼都沒有新增的魔物出現。」三選一跑好幾輪全中同一隻，機率上（(1/3)^n）說不通，一定是 bug。②**根因**：v1.42 把抽籤寫在 `prefetchArrivalClip()`，而它只掛在 `show("mode")`（進模式頁）上。但**「再來一局」`#btn-replay` 是直接呼叫 `startMode()`、根本不經過模式頁的** —— 於是重玩幾次都還是第一次抽到的那隻。睿哥就是用「再來一局」連續測，所以永遠看到同一隻。③**修法**：加一個 `arrivalUsed` 旗標。進模式頁照樣先抽（爭取預抓時間），`startMode()` 發現上一隻已經被用掉就**重抽**。這樣不管從模式頁進來還是「再來一局」，每一局都會重抽。④**驗證方法也修了**：前兩次探針都讀 `#stage-video.src`，一直讀到空字串 —— `playStageClip()` 會**先 `stopStageVideo()` 清掉 src 再設新的**，時機非常難抓。改用**網路請求**判定（每局要播哪一隻，一定會去要那一隻的 poster／mp4），一次就準。⑤**實測**：連續 7 局全部用「再來一局」重玩 → `ember → golem → ember → ember → ember → ember → golem`，**確實每局重抽**（修之前是 7 局同一隻）。快取：game.js?v=69→70；印記 `v1.44`。
- 2026-08-15｜Claude Code｜**v1.43 修掉 v1.42 自己造成的掉幀（整段演出）**：①睿哥回報「剛測試很卡」。**根因是我在 v1.42 加的 `#stage-tap-hint`**：它是 `.stage-world` 的**常駐**子元素，我只給了 `opacity: 0`，但**`opacity: 0` 的元素仍然會被繪製與合成**；而它帶著 `backdrop-filter: blur(6px)`，背後又正好是**整片全螢幕、正在播放的舞台影片** —— 等於整段演出每一格畫面都要對整個背景做一次模糊。②**為什麼 `.hero-cut .cut-tap-hint` 同樣有 blur 卻沒事**：它的父層 `.hero-cut` 在非作用時是 `visibility: hidden`，整棵子樹都不畫。我照抄樣式卻沒照抄這一點。③修法兩件：(a) 提示改成 `visibility: hidden` ↔ `visible` 切換，不只是 opacity；(b) **直接拿掉 `backdrop-filter`**，背景本來就夠暗，改用不透明一點的純色，看起來一樣但每格零成本。④順手把「略過動畫」鈕的 `backdrop-filter: blur(7px)`（revamp.css:2063）也拿掉 —— **它同樣整段演出都壓在播放中的影片上**，是同一個問題，只是不是這次才有的。⑤快取：ornate.css?v=11→12；印記 `v1.43`。⑥**實測**（`el.checkVisibility()` 會把祖先的 display／visibility 一起算，才不會像第一次那樣誤判）：演出中**仍在合成的 backdrop-filter 由 2 個變成 0 個**，提示層在非作用時 `visibility: hidden`（完全不畫）、`can-tap` 時 `visible / opacity .9`正常顯示，console error 0。⑦**另一個「卡」的來源不是 bug，是設計上的代價**：三隻魔王隨機出場，**每隻第一次被抽到時要在模式頁下載 0.6–1.4MB**（4G 上會有感），三隻都進過快取後就不會了。
- 2026-08-15｜Claude Code｜**v1.42 新增兩隻魔王（隨機出場）＋ 降臨可一鍵跳過**：①睿哥給了兩支影片：熔岩魔女（1248×1664 直式）與岩石巨獸（**1728×1152 橫式**）。壓成 720 寬 CRF 30：`arrival_ember.mp4` 1434KB、`arrival_golem.mp4` 573KB，poster 一併重製；音軌照慣例拿掉（舞台影片一律靜音播放，iOS 自動播放的硬性條件）。②**橫式那支要 `object-fit: contain`**：舞台是直向全螢幕、預設 `cover`，1728×1152 塞進去只會看到中間約四成，魔王的身體會被切掉。`playStageClip` 新增 `fit` 選項，`stopStageVideo` 一定要把它還原 —— 不還原的話下一支直式片會莫名被加上黑邊。③**隨機用 `Math.random()`，不走 seedRun。** 魔王是誰對「誰被抽中」毫無影響，純粹演出；專案鐵則管的是**結果**必須 seed-first，這裡不在它的射程內。④**抽籤放在 `prefetchArrivalClip()`（進模式頁時）**：先抽才知道要預抓哪一支。若抽到跟上次不同，會先把舊的預抓元素釋放掉，不讓它佔記憶體與流量。⑤一鍵跳過沿用既有的 `waitClipEndOrTap()`（`click`＋前 600ms 防誤觸），`playStageClip` 新增 `tapTarget`；提示 `#stage-tap-hint` 樣式與攻擊切入層那顆一致 —— 同一個手勢就該長同一個樣子。`can-tap` 在 `presentPlay` 的 finally 也要收，否則中途跳過時提示會一直掛在畫面上。⑥快取：game.js?v=68→69、ornate.css?v=10→11、版本表 1f061b9937；印記 `v1.42`。⑦**實測**（注入 log 記錄 `#stage` 的 act／影片／can-tap）：
   熔岩魔女 → `{act:arrival, src:arrival_ember.mp4, fit:cover(預設), tap:true}`；
   強制抽中岩石巨獸 → `{act:arrival, src:arrival_golem.mp4, **fit:contain**, tap:true}`；
   兩次都**預抓與實播一致**，三支 mp4 皆 200，console error 0。⑧**已知不一致（未處理）**：ACT 3A 之後出場的魔王立繪仍是 `boss_model_v3.png`，不會跟著降臨影片換。要一致的話需要另外三張去背立繪。
- 2026-08-15｜Claude Code｜**v1.41 武鬥宗師與龍騎士的曝光對齊**：①睿哥：「武鬥宗師和龍騎士大頭圖的顏色偏暗，能調整盡量與其他一致嗎？」②**量法的關鍵：只量背景，不量整張。** 背景那片城堡天空 14 張是**同一套美術**，用它當基準才分得開「這張曝光偏暗」與「這個角色本來就是暗色調」（例如 assassin 整張只有 50.7，但背景 91.1 完全正常 —— 黑兜帽是設計，不是曝光）。③量出來背景亮度中位數 90.7：**dragon_knight 76.5（0.84×）、dark_fighter 80.4（0.89×）正是全場最暗的兩張**，睿哥看得沒錯。④**用 gamma 不用線性增益**：線性乘法會把本來就接近 255 的高光推爆（天空與盔甲反光先死白），畫面反而變平；gamma < 1 抬的是中間調與暗部，高光幾乎不動。gamma 由背景反推：`g = ln(目標/255) / ln(現值/255)` → dragon_knight 0.86、dark_fighter 0.90。⑤順帶各補一點飽和（×1.05／×1.10），因為提亮會讓彩度看起來變淡。**沒有再往上加** —— 這兩個角色本來就是低彩度的設定（暗甲、灰膚），過度加彩會變假。⑥`tone()` 併進 `tools/fix_portrait_framing.py`，取景之後才套；仍然只讀 `_orig/`，**連跑兩次 md5 相同**。⑦**實測**：改後背景亮度 dark_fighter 89.4（0.98×）、dragon_knight 90.0（0.99×），已落在中位數上；並排比對確認沒有被洗白，角色的暗色調仍在。版本表 9292c4e5c4；印記 `v1.41`。
- 2026-08-15｜Claude Code｜**v1.40 再放大四張頭像（斧戰士／巫毒薩滿／僧侶／公主）**：①睿哥圈出這四張：「頭有比較小一點，可以稍微等比例放大一點嗎？」②**這四張的共通點是頭周圍的東西多** —— 斧戰士的斗篷、巫毒薩滿的毛領＋頭冠＋豎髮、僧侶的僧袍、公主的皇冠＋大量頭髮。**瞳距量起來其實跟別人差不多**，但視覺上就是小一號 —— 這種「畫面佔比」的感受量不出來，**以睿哥看到的為準**。③四張統一 **1.14×**（等比例，長寬同時放大不變形），dy 各自算回眼線 y≈210：公式 `dy = eye − 161 − 49/zoom`；axeman +6、dark_elf +4、monk +6、princess 0。④公主的皇冠與四張的邊界都確認過沒有被裁掉（裁切框皆在原圖範圍內，不需補邊）。⑤`_orig/` 現有 9 張原圖（72KB→更多），工具仍然只讀那裡，**連跑兩次 md5 相同**。⑥版本表 7efc7037b0；版本印記 `v1.40`。⑦**實測**：選角格 4x 截圖，四張明顯變大且與其餘十張同一級距，console error 0。
- 2026-08-15｜Claude Code｜**v1.39 14 張頭像整體對齊（比例大小與高低）**：①睿哥：「盡量讓每個角色比例大小、位置高低一致。」②**自動量測連試三種都失敗，全部丟掉**：(a) 全域膚色偵測 —— 金髮與亮盔甲被判成皮膚，量出來 paladin 反而最寬；(b) 暗塊左右重心 —— 抓到臉旁的髮絲，archmage 量到 188px；(c) 連通區塊配對 —— **把偵測結果疊回原圖一看，抓到的多半是眉毛不是瞳孔**。**教訓：這種量測一定要把結果畫出來確認，只看數字會被騙。**③改用**目視但可比較**的方法：把 14 張裁同一段 y 帶（150–285）並排、每 20px 畫格線，瞳距直接用格子數讀。讀出來瞳距中位數約 **58px**、眼線目標 y≈210。④**發現上一輪做錯一件事**：v1.38 把 archmage 放大 1.10 —— 但它本來就是**全場最大**的一張（瞳距約 70）。這輪改回 1.02（幾乎不放大），只保留「往上移」。⑤這輪的設定：amazon 1.22/+6（瞳距 44，全場最小）、archmage 1.02/+12、dark_fighter 1.08/+4（眼線 216 偏低）、dragon_knight 1.16/−44、paladin 1.24/+4。⑥`extend_top()` 擴充成 `extend(top, bottom)` —— 縮放接近 1 又要平移時，裁切框會**同時**超出上下兩端，只補上緣的話 PIL 會丟 `box can't exceed original image size`。⑦原圖仍全部留在 `portraits/_orig/`，工具永遠只讀那裡，**連跑兩次 md5 相同**。⑧版本表 1e3d150713；版本印記 `v1.39`。⑨**實測**：改後同段 y 帶並排，14 張的臉大小與眼線都落在同一帶；選角格 4x 截圖確認、console error 0。⑩**沒有動的**：assassin（兜帽只有一對發光眼，沒有臉可對齊）；knight 的臉因為在頭盔裡本來就偏小，那是美術本身，不是取景問題。
- 2026-08-15｜Claude Code｜**v1.38 三張選角頭像重新取景（頭的大小與高低對齊）**：①睿哥：「聖騎士的頭明顯比大家小；大魔導士頭的位置太低；龍騎士頭的位置太高。」②**量法**：14 張排成 montage、在 **y=210** 畫基準線，再逐張看**瞳距**（瞳孔中心的 x 距離 —— 這是頭部大小最穩的代理值，不受髮型與盔甲干擾；**膚色偵測不能用**，金髮與盔甲會被判成皮膚，量出來 paladin 反而變最寬）。量到：多數人瞳距 60–64px、眼線 205–218；**paladin 瞳距只有 44**（別人的 0.7）、**dragon_knight 眼線 150**（高了 60px）、archmage 眼線 207 但頭頂留白比別人多。③新增 `tools/fix_portrait_framing.py`，以 `zoom`／`dy` 重新取景：paladin 1.30／+6、archmage 1.10／+14、dragon_knight 1.10／**−46**。④dragon_knight 的髮梢本來就頂到上緣，往下移代表**裁切框要伸出畫面外**，上方不足處用**鏡射＋高斯模糊**補 32px（那一帶是暗色天空與城堡剪影，補完看不出來）。⑤**避開重跑就毀掉的陷阱**：這支會覆蓋 `portraits/<id>.jpg`，若來源就是輸出檔，重跑一次就再縮放一次。所以原圖另存 `portraits/_orig/`（44KB），**本檔永遠只讀那裡** —— 已實測連跑兩次 md5 完全相同。（`generate_audio.py`／`gen_sfx_v2.py` 都被同類陷阱咬過，這次一開始就擋掉。）⑥這三張同時用在**選角格**與**命運排序的牌面**，兩邊都吃 `heroThumb()`，逐檔雜湊會自動換網址。⑦版本表 068e3636fb；版本印記 `v1.38`。⑧**實測**：改後 montage 三張的眼線都落在基準線上、頭的大小與鄰居相當；選角格 4x 截圖確認，console error 0。
- 2026-08-15｜Claude Code｜**v1.37 卡片正面：整張臉跟肩膀完整露出**：①睿哥：「卡片的正面能讓角色的整張臉跟肩膀露出來嗎？現在都被切掉。」**是兩件事疊起來造成的**：(a) `object-fit: cover` 把 **0.745 的頭像**塞進 **0.667 的卡**（卡片為了配合卡背素材是 2:3），左右各裁掉 5%，肩膀先斷一截；(b) 名次條用漸層**壓在圖上**、蓋住下面三成，剩下的肩膀也沒了。②頭像素材（240×322）本身就是很緊的「臉＋肩膀」構圖，**肩膀就貼在下緣**，所以只要裁一點或蓋一點就會不見。③改成上下兩段的 flex：圖用 **`object-fit: contain` 完整顯示不裁切**，名次條**自己佔一條**放在圖下方（不再是絕對定位疊上去）。④`min-height: 0` 一樣不能省 —— flex 子項預設 `min-height: auto`，不歸零的話圖會把名次條擠出卡片外。⑤左右各留約 5px 的黑邊（0.745 的圖放進 0.667 的框），底色是深色所以幾乎看不出來；要完全沒有黑邊就得重切一套 2:3 的頭像，那要動素材，先不做。⑥快取：ornate.css?v=9→10；版本印記 `v1.37`。⑦**實測**：`object-fit` 為 `contain`、六張牌名次 1..6、翻面數等於總數；5x 放大截圖確認每張都看得到完整的臉與肩膀。
- 2026-08-15｜Claude Code｜**v1.36 命運排序換上睿哥的開場影片與卡背**：①**卡背**：`assets/ui/card_back.webp`（六芒星魔法陣＋金框，320×480／44.6KB）。**卡片比例從 3:4 改成 2:3** —— 那是**素材本身的比例**（1152×1728）；維持 3:4 的話那張圖不是被裁掉外框就是被拉扁，所以是卡片配合素材。英雄頭像（240×322）在 2:3 框裡用 `cover` 只裁掉一點左右，角色是置中的沒問題。②**開場影片**：`assets/videos/mobile/order/intro.mp4`，取代先前借用的 `boss/arrival.mp4`。**原片 1152×1728／24Mb/s／18.3MB，4G 上根本不可能**；壓成 720×1080、CRF 30、**1.09MB**（掃過 CRF 28／30／32 得 1473／1093／812KB，30 與既有的 `boss/arrival.mp4` 1386KB 同級）。③**音軌拿掉**：原片帶 AAC（-32 dBFS 的低量環境音），但**舞台影片一律靜音播放**（iOS 自動播放的硬性條件），留著只是負重。要用那段聲音的話得另外抽成音效 cue。④poster 一併重製：`assets/videos/poster/order/intro.jpg`（720×1080／113KB）。⑤**`CLAUDE.md` 那條「雲端做不到 ffmpeg 轉檔」已更正** —— 這條從 v1.22 起就被發現是錯的，拖到現在才改。`pip install imageio-ffmpeg` 帶的是靜態 ffmpeg 7.0.2，H.264／AAC 都能解能編。⑥快取：game.js?v=67→68、ornate.css?v=8→9、版本表 5ea9c51bfd；**卡背走 CSS `url()`，吃不到逐檔雜湊表，換圖要手動 +`?v=`**（目前 `?v=1`）。⑦**實測**：`order/intro.mp4` 與 `order/intro.jpg`、`ui/card_back.webp` 皆 200；卡片實測比例 **0.667**（＝2:3）、六張一列無溢出、翻面後名次仍是 1..N，console error 0。
- 2026-08-15｜Claude Code｜**v1.35 命運排序改成「開場影片 ＋ 牌陣同時翻面」**：①睿哥指定的演出：先播一段影片（跟魔王討伐同一個排面），然後每位角色化成一張牌，**卡背朝上**排出來（一眼數得出有幾張），**同時翻面**，正面是英雄的樣貌，由左至右就是第一名到最後一名。取代原本「一位一位站出來」的版本。②開場片沿用 `boss/arrival.mp4` —— **目前唯一的全螢幕開場片**。若要排序專用的片再說。③**名次仍然是 seed-first**：`result.order` 早在 `seedRun()` 就定案，`presentOrderCards()` 只是照索引排牌，不做任何抽樣（鐵則沒有被動到，rng.js 也沒碰）。④**踩到撞名，查了半天**：第一版把類別取名 `.fate-card`／`.fc-*`，但 **revamp.css 2415 行的 `.fate-card` 是「命運卡揭曉」那一層**（還有 `.fc-name`／`.fc-desc`／魔法陣的 `.fc-rune`）。結果卡背正常、**卡片正面被那邊的樣式接管，量出來只有 20×23px**，變成一顆小圓點。全部改名為 **`rank-*`／`rk-*`** 並在 CSS 就地寫下警告。**這是這個專案第三次因為「重用既有名字」出事**（前兩次是 `::after`）。⑤**第二個坑：自訂屬性被自己蓋掉**。JS 把算好的卡片寬度設在容器上，但 `.fate-card` 自己也宣告了一次 `--fc-w: 56px` —— **自己的宣告會蓋掉繼承來的值**，所以算出 70px 卻永遠畫成 56px。預設值改放容器上。⑥**第三個坑：寬度沒扣 gap**，五張就從一列擠成兩列；改成 `min(calc((100% - (n-1)*gap) / n), 78px)`，由 CSS 算才知道容器實際多寬。⑦翻面用標準 3D 兩面（`preserve-3d` ＋ `backface-visibility: hidden` ＋ 背面預轉 180°），**不要改成 opacity 互換** —— 那看起來是淡出淡入不是翻面。整段包 try/finally，跳過或出錯牌陣一定收掉。⑧快取：game.js?v=66→67、ornate.css?v=7→8；版本印記 `v1.35`。⑨**實測**（393×660 與 360×600，2／4／5／6／7／13 人）：牌數正確、**由左至右名次 1..N**、同時翻面（flipped 等於總數）、13 人折成 7＋6 兩列、**無橫向溢出**、console error 0；排序結果頁的名單也正常（沿用 v1.34 的 `.result-list`）。
- 2026-08-15｜Claude Code｜**v1.34 刪掉三個模式；名單型結果頁也塞進一個螢幕**：①睿哥：「命運配對、命運審判、命運淘汰，與其他類別類似，沒必要存在 直接刪除」——`index.html` 的三張模式卡已移除，剩 **魔王討伐／命運排序／命運分隊(2-4)**。②**⚠️ 但 doom 的程式碼不能刪**：`js/rng.js` 的命運卡「逆命」會把 boss 換成 doom（`if (mode === "boss") effectiveMode = "doom"`），魔王討伐開到那張卡仍會走審判流程。要連那張卡一起拿掉得**先問睿哥**（鐵則：改 rng.js 前先問）。pair／survival 的分支留著沒人走，而且 **teams 模式仍沿用 pair 的 ACT 舞台**，那段不要順手清掉。已在 index.html 就地註記。③**名單型結果頁（分隊／排序）本來會整頁捲**。單人勝利那版（v1.30）是把每塊都收小，但名單長度隨人數變（最多 13 人 4 隊），**再怎麼收都有裝不下的一天**。改成**面板 flex 直排、只讓名單那塊伸縮**：徽章／立繪／名字／按鈕保持自然大小，名單吃掉剩餘空間，真的放不下才在名單自己裡面捲 —— 「按鈕永遠看得到」對任何人數都成立。④`min-height: 0` 不能省：flex 子項的 `min-height` 預設是 `auto`，不歸零 `.result-detail` 根本不會縮。⑤名單型的頁面主角是名單不是立繪，所以由 `setResultPortrait(id, { list: true })` 掛上 `.result-list`，立繪縮到 `min(17dvh, 132px)`、每個隊伍區塊也收緊。**不用 CSS `:has(.team-block)` 判斷** —— 排序模式的名單是一串 `<p>`／`<ol>`，沒有共通標記可選，由產生內容的那一端直接說「我是名單」最不容易錯。⑥模式從六個減到三個之後，模式頁在 393×660 本來就一頁裝得下；360×600 還差 90px，再收內距與敘述字級後也過了。⑦快取：game.js?v=65→66、ornate.css?v=6→7；版本印記 `v1.34`。⑧**實測**（320×560／360×600／393×660／430×720）：首頁／人數／選角／模式／結果／結果+懲罰**全部 0 溢出**；分隊 4 隊 13 人與排序 13 人**整頁不捲、按鈕全可見**，只有名單自己內捲；393×660 的 3 隊 3 人**完全不捲**。console error 0。⑨**未達成的**：320×560（SE 一代）的模式頁仍超出 44px、360×600 的 3 隊 3 人名單仍會小捲一段 —— 那個尺寸下資訊量本來就裝不完，整頁與按鈕都沒問題。
- 2026-08-14｜Claude Code｜**v1.33 符文音效放大（睿哥：實測有點小聲）**：①**只調增益不夠**。量過同級的 cue：符文原本的「峰值×增益」是 0.67，其實已經跟 `strike.release`（0.75）、`reveal.winner`（0.64）同級 —— 問題不在峰值，在**密度**：素材 RMS 只有 -15.4 dBFS，是寬頻鬆散的光暈聲，跟同樣寬頻的討伐曲疊在一起會被**遮蔽**。②**所以三件事一起做**：(a) 素材加柔和飽和（`tools/import_scene_bgm.py` 新增 **`--drive`**，tanh 把波峰壓圓、中間拉起來），RMS **-15.4 → -11.0 dBFS**、峰值 0.81、波峰因數 9.1dB，起音仍在 0.05s、尾端 -40 dBFS，四發之間照樣不疊。(b) cue 增益 0.9 → **0.98**（峰值×增益 0.79，與全場最響的 `boss.roar` 0.80 同級）。(c) **`strike.charge` 開始就把音樂 duck 到 0.62**（-4.2 dB），整段按住都讓四記符文出得來。③drive 掃過 1.8／2.6／3.4：**1.8 是甜蜜點** —— 2.6 以上 RMS 不再上升（已被 target 卡住），只是把峰值愈壓愈低、聲音愈扁。④綜合相對響度約 **+9.3 dB**（素材 +4.4、增益 +0.7、音樂讓開 +4.2）。⑤`getStatus()` 新增 `duckFactor`，調音量平衡時看得到才好驗。⑥快取：audioDirector.js?v=17→18、版本表 1c0a8bc9c2；版本印記 `v1.33`。⑦**實測**：實跑一局按住 3.4 秒，`strike.rune` 仍準時四次（+662／+1363／+2046／+2712ms）；`duckFactor` 1 → **0.62** → 3.8 秒後回到 1。
- 2026-08-14｜Claude Code｜**v1.32 魔法陣每亮一個符文就響一次（共四次）**：①素材由睿哥的 3.01s 影片抽音軌（`imageio-ffmpeg` 的靜態 ffmpeg），新增 `assets/audio/sfx/rune_light.mp3`（0.63s／9.8KB），cue 名 `strike.rune`。②**門檻必須跟 CSS 一致**：`css/revamp.css` 的 `.fc-rune[data-rune="n"]` 是 `opacity: clamp(0, calc((var(--p) - 門檻) * 18), 1)`，四個門檻是 **0.22／0.45／0.68／0.90**，`playFateStrike()` 的 tick 就在 `--p` 越過這四個值時各放一次。**改一邊就要改另一邊。**③用 `while` 不用 `if` —— 畫面卡頓時一個 frame 可能跨過兩個門檻，那也要補放，不能讓某一顆符文默默沒有聲音。放開手 `--p` 歸零時 `runesLit` 也要歸零，否則重按不會再響。④**這顆一定要短**：四次間隔只有 690ms（3000ms × 0.23）。原素材是 3 秒的持續光暈，直接用就會四發疊在一起 —— **v1.21 的「魔法陣結束還在噹」就是這樣來的**。切成 0.63s，尾端衰到 -38 dBFS。⑤**第一版切錯了**：從 0.46s 起切，能量峰落在 **0.30s**，聽起來是「慢慢浮起來」不是「亮起來」，而且第四顆的峰剛好壓在 `strike.release` 上。改從 **0.72s**（原曲能量峰 0.76s）起切，並替 `tools/import_scene_bgm.py` 新增 **`--attack`**（開頭快速上升，避免從樂句中間切進去的喀聲）。現在峰值落在 **0.05s**。⑥快取：game.js?v=64→65、audioDirector.js?v=16→17、版本表 eb6c293dbc；版本印記 `v1.32`。⑦**實測**（實跑一局魔王討伐，按住 3.4 秒）：`strike.charge → strike.rune ×4 （+664／+1364／+2048／+2714ms，對應 0.22/0.45/0.68/0.90 × 3000ms）→ strike.release @3014ms`，`rune_light.mp3` 200。⑧**順帶發現一個既有的 404**：`assets/ref_battle_mobile.mp4?v=14` —— 那支檔案還沒從睿哥的 Mac 加進版控，雲端補不了，**要在本機 `git add`**。
- 2026-08-14｜Claude Code｜**v1.31 攻擊魔王的音樂換成睿哥給的 `crown_of_thunder`**：①取代 `assets/audio/bgm/battle.mp3`。**這一支同時涵蓋魔王降臨與討伐**（v1.19 之後降臨就沿用討伐曲，沒有獨立的 arrival 曲）。②走 `tools/import_scene_bgm.py`：原曲 30.8s，尾端 2.0s 摺回開頭做**無縫循環**（`crossfadeMusic()` 是 `source.loop = true` 的硬循環，**接縫沒有任何交叉淡化**，不處理的話每循環一次就「啪」一聲），輸出 28.8s／393.9KB。③**響度刻意對齊舊檔的 -16.9 dBFS，不讓它變大聲** —— v1.29 才把最後一擊的音效放大並把音樂 duck 到 0.42，討伐曲若順手調響就會把那件事抵消掉。**注意 MP3 編碼會掉約 0.5dB**：`--target-db` 要給 **-16.4** 才會在解碼後量到 -16.9（第一次給 -16.9，實測只有 -17.4，比舊檔還小聲）。④原曲峰值是 **1.00（滿刻度）**，正規化後降到 0.75，不會削頂。循環接縫的頭尾 50ms 能量差 2.6dB，與舊檔的 2.1dB 相當。⑤快取：版本表 319c4e0887，`battle.mp3?v=712fbb782a`；版本印記 `v1.31`。⑥**實測**：解鎖音訊後切到 `play` 場景，確認載入的就是新的 `battle.mp3?v=712fbb782a` 200，console error 0。
- 2026-08-14｜Claude Code｜**v1.30 結果頁塞進一個螢幕，不用再往下滑**：①**兩個各自獨立的原因**。(a) **`vh` 在 iOS Safari 是「大視窗」** —— 它算的是網址列與工具列都收起來時的高度，不是當下看得到的那塊。手機上 `46vh` ≈ 392px（被 330px 上限接住），但實際可見區只有約 660px，立繪框一個人就吃掉半個螢幕。**這一層所有 `vh` 已一律改成 `dvh`**。(b) **面板自己是捲動容器**：`#screen-result .result-panel` 有 `max-height: calc(100dvh - …)`＋ `overflow-y: auto`，內容超出時不是整頁變長，而是**在框裡面捲**。②**所以我上一輪的驗證是錯的** —— 只量了 `document.scrollHeight`，看起來「無溢出」，但面板內部 393×660 超出 **147px**、360×600 超出 **199px**。**以後量版面要連面板的 `scrollHeight − clientHeight` 一起量。**③**修法**：立繪框 46dvh→35dvh、命運卡說明框內距與行高收緊（185→93px）、三排按鈕 min-height 調小（167→130px）、面板內距收一點。④**抽了懲罰任務之後**會多一塊 `.doom-box`（約 60–90px），用 **`:has(#doom-box:not([hidden]))`** 偵測，把空間從立繪框拿回來（那是唯一縮了也不影響資訊的區塊）。`:has()` 是 iOS Safari 16.4 起支援，更舊的機器不生效就退回原本可捲的行為，不會壞。⑤**踩到 CSS 順序的坑**：無條件的 `:has()` 規則與 `@media` 裡的同強度，**同強度誰在後面誰贏**。我先把短螢幕的覆寫寫進檔案前面的 `@media`，結果被後面的無條件規則蓋掉、量出來完全沒變；已把那組覆寫移到**檔案最後**並註記。⑥320 寬的老機（SE 一代）revamp.css 會把按鈕改成兩欄，多出一整列要 **203px，比立繪框還貴** —— 在該寬度改回三欄、字縮小，並讓說明框自己捲。⑦快取：ornate.css?v=5→6。⑧**實測**：320×560／360×600／393×660／430×720 四種**Safari 實際可見高度**，每一頁都同時檢查整頁溢出與所有面板的內部溢出，**含「抽懲罰任務之後」的狀態，八種組合全部 0 溢出**，console error 0。
- 2026-08-14｜Claude Code｜**v1.29 最後一擊其實是「完全沒有音效」，以及兩位精靈的 BGM 互換**：①**最後一擊不是太小聲，是一顆音效都沒放。** `playFinalBlow()` 寫的是 `audioCue("hero.attack." + hero.id, …)`，但 **cueMap 裡根本沒有 `hero.attack.<id>` 這種名字** —— per-hero 的攻擊聲一直是走 `HF_Audio.playHeroAttack()`，不在 cue 表裡。`cue()` 查不到就 `return null`，所以那 12 秒只剩討伐曲在跑。已用 assert 確認：`cue('hero.attack.ranger')` **回傳 null**。②**修法**：改走跟攻擊輪播同一條路徑，並且**放大 1.45 倍 ＋ 把音樂壓到 0.42**（`duck`，5.2s）—— 這是整場高潮，跟輪播用一模一樣的音量沒有份量。`playHeroAttack()` 因此新增 `options`：`volume` 是**倍率不是絕對值**（1 ＝ 原本的 0.84），另有 `duck`／`duckMs`。實際增益 0.84×1.45＝1.218，乘上 sfxBus 預設（master 0.86 × effects 0.82＝0.705）約 0.86，不會削頂。③**兩位精靈的選角 BGM 互換**：暗夜精靈是 `ranger`、光之精靈是 `orc_archer`（**名字與 id 對不起來，很容易搞錯**），直接對調 `assets/audio/heroes/bgm/` 底下那兩支檔案的內容，程式的對應表不動。實測點選兩張卡：暗夜精靈 → `ranger.mp3?v=86e17f6b91`、光之精靈 → `orc_archer.mp3?v=d9e9187a34`，**逐檔雜湊都變了**，手機只會重抓這兩支。④快取：game.js?v=63→64、audioDirector.js?v=15→16、版本表 2f72e54bf4；版本印記 `v1.29 · 0814-1830`。
- 2026-08-14｜Claude Code｜**v1.28 勝利者立繪框：從「正方盾牌框 + 留白」改成「直式卡 + 填滿」**：①**起因**：睿哥把現況與目標稿並排，說「我要修正成圖二的樣式」。差別就在這個框 —— 現況是正方形的盾牌框，勝利短片（**400×704，比例 0.568**）用 `object-fit: contain` 塞進去，左右各留掉快三成空白，角色小小一張浮在中間；目標是一張直式卡，畫面填滿、外圈一道金光。②**根因**在 revamp.css：`aspect-ratio: 1 / 1` ＋ `object-fit: contain` ＋ `padding: 4% 4% 9%`（那個下緣留白本來是為了避開盾形的圓弧 `border-radius: 22px 22px 52% 52%`）。③**修法不是硬改成 `cover`** —— 框跟素材比例不合時 `cover` 會把角色的頭或腳裁掉。改成**讓框去配合素材**：有勝利短片時（`.has-video`）框用 **400 / 704**，跟影片完全一致，`cover` 於是零裁切也零留白；沒有短片、退回立繪時，立繪是 **512×512 的正方形**，框維持 1 / 1。④有影片時**改用高度定尺寸**（`height: min(46vh, 330px)`）—— 原本 210px 寬套上 0.568 會變成 370px 高，底下的名字與按鈕會被擠出畫面；矮螢幕再退到 `min(42vh, 260px)`。⑤邊框照目標稿做成四層：亮金細邊 → 一圈暗底 → 再一圈金 → 外擴的暖光；名字 1.85→2.1rem。⑥快取：ornate.css?v=4→5；版本印記 `v1.28 · 0814-1745`。⑦**實測**（393×852 與 360×640，以同比例的 victory poster 代打影片，headless Chromium 沒有 H.264）：框的比例量到 **0.568 對素材 0.567**、`object-fit: cover`、`padding: 0px`，**上下皆無溢出**；退回立繪的情況框為 204×204、512×512 的圖剛好填滿。
- 2026-08-14｜Claude Code｜**v1.27 版面（不只是皮膚）**：①**起因**：v1.26 換了框之後睿哥回「但一樣沒有改版面」—— 說得對，那一版是純皮膚，版面（字級、對齊、間距）完全沒動。這一版處理量出來的三項真差距。②**標題小了一倍以上**：稿子裡「編成冒險隊伍」「選擇角色」的字高約 80 個稿子像素＝33 CSS px≈2rem，現況 1.3rem。標題是每頁的視覺重心，小一號整頁就散。改成 **1.9rem**；矮螢幕（`max-height: 700px`）退到 1.5rem，免得把選角舞台擠掉。③**人數頁的標題與副標在稿子裡是置中的**（STEP 標籤仍靠左），現況整頁靠左 —— 這是「版面沒改」最直接的一項。人數頁與模式頁一起置中，兩頁頭部才一致。④**角色卡名字改成滿版底條**：稿子是一條深色帶壓在卡片下緣，四個字的名字完整讀得到；現況是直接疊在卡片底色上、還被 `text-overflow: ellipsis` 切成「武鬥宗…」。字級改成 **`clamp(7.8px, 2.3vw, 9.4px)` 隨視窗寬走** —— 卡片固定 7 欄，360 寬時每張只有 44.8px，固定字級一定頂到兩邊；已 assert 14 個名字**全部無溢出**。⑤**修掉 v1.26 自己造成的擠壓**：那版給按鈕統一 `padding-inline: 1.15rem`，但結果頁與選角頁那排小按鈕本來只有 0.35rem，斜面往內畫 18px，字就頂在斜面上（睿哥截圖裡「抽懲罰任務」看得很清楚）。改成按情境各給內距，小按鈕的斜面縮到 15px（**低於 14px 中央那塊會被拉太開，金面變得又淡又平**）。⑥結果頁的命運卡說明框改成金色調。⑦快取：ornate.css?v=3→4；版本印記 `v1.27 · 0814-1045`。⑧**實測**：360×640／393×852／430×932 三種尺寸逐頁截圖，三者 `body.scrollWidth` 皆等於視窗寬（**無橫向溢出**），**除 Google Fonts（此環境對外被擋，手機正常）外 console error 0**。
- 2026-08-14｜Claude Code｜**v1.26 框改成「圖片套疊」：直接切稿子的像素做九宮格**：①**起因**：睿哥看過 v1.24 的 CSS 版之後說「跟這個還是有點差異，能用圖片套疊的方式嗎？」。稿子那種框是手繪級的金屬立體感（斜面、亮邊、四角托架、上緣兩顆凸出框外的飾釘），CSS 只能逼近到「一條金線＋一顆菱形」，所以改成貼稿子本人。②**新增 `tools/cut_ui_frames.py`**，從 STEP 01 那張稿子（944×2096）摳出三個素材：`assets/ui/frame_panel.webp`（582×701，47.8KB）、`btn_gold.webp`、`btn_purple.webp`（各 3.3／1.6KB）。③**去背用「幾何遮罩 ∩ 亮度」**：只留外框到內框之間那一圈（四角用大圓角保留托架長臂），圈內剩下的深色淡出成透明，貼到任何底色上都不會出現一圈深色框。左右**鏡射對稱化**——稿子是 AI 畫的，左右差了兩三個像素，不修一邊會比另一邊粗。④**兩個坑**：(a) 飾釘凸出框外，背後是那群 Q 版英雄，直接留會在飾釘裡看到一塊膚色。判準**必須用 G−B 而不是 R−B** —— 金的最亮處接近白（255,239,201），R−B 只有 +54，跟膚色的 +44 分不開；但金的 G−B 有 +38，膚色是 −2。(b) 按鈕**不能整顆拿去當素材** —— 稿子上印著「進入英雄殿堂」，中央那塊會連字一起被拉伸，左右鏡射後變成「進入英英人斑」。改成重組窄長條：[左端]+[一小條沒有字的材質]+[左端鏡射]，中央只被橫向拉伸，直向仍照原比例，金屬漸層完整保留。⑤**border-image 的三組數字**：`slice 93 104 68 104`／`width 62px 70px 46px 70px`／`outset 11px`。**`border-image-width` 遠大於 `border-width`（11px）是刻意的** —— 版面上只吃 11px，多出來的往內畫、疊在 padding 上，角落托架才不會把內容擠窄。outset 對應素材四邊各留的 11px bleed，金線因此剛好落在版面邊界上，飾釘凸出去。⑥切角是圖片的 alpha，**`box-shadow` 只認方形 border box**，用它會在四個切角外露出一塊方形暗影 —— 陰影一律改走 `filter: drop-shadow()`。⑦套用範圍：首頁／人數／模式／結果／設定／戰績六個面板，以及 `.btn.primary`／`.btn.gold`／`.btn.ghost`。revamp.css 的 `:is(.card, .panel)::before/::after`（那兩顆 ◆ 飾釘）在有貼圖框的面板上**關掉**，否則會變四顆。⑧**CSS 裡 url() 的素材無法用 `HF_ASSET_V` 逐檔雜湊表**（那是 JS 用的），改素材時要**手動把 `?v=` +1**。⑨快取：ornate.css?v=2→3；版本印記 `v1.26 · 0814-1010`。⑩**實測**：393×852 與 360×640 逐頁截圖（首頁／人數／選角／模式／設定／結果／戰績），框與按鈕四角無變形、設定視窗的「關閉」仍可捲到並按下（`命運卡`／`命運一擊` 靠它寫回），**console error 0**。
- 2026-08-14｜Claude Code｜**v1.25 新版偵測：不再靠玩家自己硬重載**：①**起因**：睿哥回「看起來沒變」，截圖裡介面確實還是舊的 —— 但**版本印記寫著 `v1.21`，而線上早已是 v1.24**（22:50:10Z 部署成功，截圖是台北 08:33＝一小時四十分之後）。印記成功發揮作用，指出問題不在部署而在手機的 `index.html` 是舊的。②**這是第三次為同一件事誤判**（v1.11、v1.24、這次），所以改成自動偵測而不是再叮嚀一次「要重載」。③**做法**：新增 `build.txt`（內容就是版本印記），`game.js` 的 `watchForNewBuild()` 以 **`cache: "no-store"`** 抓它跟頁面內嵌的 `.build-stamp` 比對，不同就顯示「有新版本 vX · 點一下更新」的小鈕，點下去 `location.reload()`。**刻意不自動重載** —— 那會打斷正在進行的一局。除了開場 1.5 秒後查一次，**`visibilitychange` 回到分頁時再查**，那正是最常「錯過新版」的時機；離線或抓不到就安靜略過，不能因為加分功能而壞掉。④新增 `tools/sync_build.py` 從 `index.html` 的印記產生 `build.txt`，**改版後必跑**（忘了跑玩家就收不到提示）；已寫進 `CLAUDE.md` 的踩坑清單第 1 條，連同「`<meta http-equiv=Cache-Control>` 對 HTTP 快取無效」這個根因一起。⑤快取：game.js?v=62→63、ornate.css?v=1→2；版本印記 `v1.25 · 0814-0905`。⑥**實測三種情境**：版本相同→提示保持隱藏；攔截 `build.txt` 回傳 `v9.99`→提示出現且文字帶新版號；點下去確實觸發重新導覽；**console error 0**。⑦**注意**：這個機制要生效，手機必須**先拿到一次含有偵測程式的頁面**。睿哥這次仍需手動硬重載一次（關掉分頁重開最可靠），之後就會自動提示。
- 2026-08-14｜Claude Code｜**v1.24 依睿哥五張視覺稿提升整體介面（新增 `css/ornate.css` 疊加層）**：①**素材不用重做** —— 稿子裡那群 Q 版英雄就是既有的 `assets/bg_party.jpg`，模式卡的場景圖也都在，所以整件事是純 CSS。②**另開 `css/ornate.css` 疊在最後**，不動 revamp.css 那 2600 行（大量 `#screen-x .y` 的 ID 選擇器，直接改容易改壞）。要退版只要把 index.html 那行 `<link>` 拿掉。③**視覺語言四件事**：金色雙線框＋四角菱形飾釘、金屬質感的金色主按鈕（斜面／裂紋／光暈）、紫色發光的次要按鈕、金色漸層標題；另加青色 STEP 標籤、金色人數大字、模式卡徽章外環、六角形玩家順位點。④**踩到三個坑，都記在檔案註解裡**：(a) 用 `background:` 簡寫會把 `background-color` 重設成透明，面板變透明後 `backdrop-filter` 把背後光斑糊成一顆怪圓 —— 必須單獨給 `background-color`；(b) **`::before`／`::after` 在這個專案裡幾乎都另有用途** —— `.card/.panel` 的是角落圓形飾釘、`.mode-card` 的是**美術圖與壓在圖上的遮罩**，我先後劫持了兩次，第二次直接讓模式卡文字糊到看不清。內框線最後改用 **`outline` + 負的 `outline-offset`**，不佔偽元素也不覆蓋底下的漸層；(c) `-webkit-text-stroke` 與 `background-clip:text`＋`drop-shadow` 疊在一起會在字緣糊出重影，中文筆畫多的字尤其明顯，拿掉即可。⑤按鈕選擇器要對上 revamp.css 的 `:is(#screen-home, …) .btn.primary`（`:is()` 取最強引數，等於 ID 強度），純類別選擇器壓不過。⑥**實測**：393×852 與 360×640 兩種尺寸逐頁截圖比對稿子；360 寬 `body.scrollWidth` 等於視窗寬（**無橫向溢出**），**console error 0**；模式卡遮罩與左側色條恢復正常、文字可讀。⑦快取：新增 ornate.css?v=1；版本印記 `v1.24`。**改這層時務必先確認要動的偽元素有沒有既有用途。**
- 2026-08-14｜Claude Code｜**v1.23 全隊到齊的號角改用睿哥影片的真實錄音，並拆掉「重跑就蓋回合成版」的陷阱**：①睿哥傳來 3.04 秒影片，指定用在「最後一個選完角色確定後」（即 `pick.complete`）。**這支不需要擷取** —— 逐 0.5 秒的 RMS 全在 -13.8～-15.4 dBFS 之間，從頭到尾能量平均，整支本身就是重點。②**但結尾不是靜音**（末 5ms 振幅 0.0100），硬切會有喀聲，所以補 0.25s 淡出；響度對到 -15.0 dBFS 與 v1.22 的勝利號一致，峰值 0.60、47.8KB。③**拆掉我自己上一輪埋的陷阱**：`gen_sfx_v2.py` 的 `SOUNDS` 裡**還掛著 `victory_fanfare` 與 `party_horn`** —— 它們在 v1.22／v1.23 都已改用真實錄音，但**只要有人重跑 `python3 tools/gen_sfx_v2.py`（不帶參數＝全部），兩支真實錄音就會被合成版蓋回去**。已把兩者移出 `SOUNDS`（函式保留當歷史參考），並在原處註明不要接回去。**這與 v1.16 對 `generate_audio.py` 做的是同一件事** —— 素材一旦改用真實錄音，就必須從產生器的工作清單移除，否則遲早被覆蓋。`SOUNDS` 現為 15 個（純合成音效）。④快取：audioDirector.js?v=14→15、版本表 d8583881ed；版本印記 `v1.23 · 0814-0630`。⑤**實測**（4 人局逐位按決定）：第 1–3 位 `pick.lock`＋`pick.advance`、**第 4 位只有 `pick.complete`**，流程走到模式頁，`party_horn.mp3` 200，**console error 0**；並以 assert 確認 `SOUNDS` 已不含那兩支。
- 2026-08-14｜Claude Code｜**v1.22 勝利號改用睿哥影片抽出的真實音樂（`victory_fanfare` 是最後一個合成音樂）**：①睿哥傳來 12 秒影片，要求「擷取重點」。取代 `sfx/victory_fanfare.mp3` —— 它是 v1.15 之後**唯一還是合成的『音樂』**（合成音效沒問題，但銅管號角靠振盪器做終究像合成器）。②**雲端現在做得到 ffmpeg 了**：`pip install imageio-ffmpeg` 會帶一份**靜態 ffmpeg 7.0.2**，能解 AAC／H.264。**`CLAUDE.md` 說「雲端 session 做不到 ffmpeg 轉檔」這條已經過時**（下次整理時要更新）。影片音軌為 AAC LC 44.1kHz stereo 128kbps。③**擷取點不是用猜的**：先算每 0.5 秒的 RMS 找到能量高原在 **4.5–6.5s**（-16.6 dBFS，全片最強；7.5–9.5s 掉到 -25 是安靜段），再用**頻譜流量（spectral flux）偵測起音點**得到 `… 4.02 / 4.75 / 4.97 / 5.17 / 5.41 / 5.87 / 6.13 …`，**從 4.02s 這個拍點切入**取 3.2 秒（涵蓋 4.75–6.0 的峰值與 6.75 的再起），結尾 0.45s 淡出避免從樂句中間斷掉。輸出 -15.0 dBFS／峰值 0.58／50.6KB，與原本的合成版（3.24s／-16.9 dBFS）長度相當、略響一點。④`tools/import_scene_bgm.py` 新增 `--fade-out`（一次性音效用；`--crossfade 0` 則跳過無縫循環處理）。⑤快取：audioDirector.js?v=13→14、版本表 34da82d602；版本印記 `v1.22 · 0814-0627`。⑥實測 `victory_fanfare.mp3` 200、無 console error。⑦**這顆聲音由 `victory.crown` 使用**（`victoryFilm.js` 的加冕時刻，且 audioDirector 會 `duck(0.5, 3400)` 把音樂壓低讓它出頭）。
- 2026-08-14｜Claude Code｜**v1.21 修掉「魔法陣結束還在噹」—— 兩記鐘聲疊在一起**：①**根因**：命運一擊完成時下 `strike.release`，而 ACT 5 揭曉的 `reveal.winner` **只隔 6 毫秒**就跟著下（`playFateStrike()` 一 resolve，`setAct("reveal")` 就緊接著跑）。而**這兩個 cue 當時都指向同一顆 `reveal_chime`** —— 兩記 2 秒的鐘同時響、疊在一起拖尾，圓陣都收乾淨了鐘還在鳴。②**修法一：一擊改用衝擊音**。新增 `fate_strike.mp3`（1.6s／25.9KB）：低頻 90→38Hz 轟落 + 撞擊瞬態 + 由亮轉暗的下墜掃頻，**刻意不放任何長衰減共振** —— 那一刻要的是「一擊」不是鈴聲，尾巴全靠殘響收掉。頻譜重心 2585Hz。③**修法二：把那顆鐘本身收短**。`reveal_chime` 由 **2.0s→1.1s**，最長衰減砍掉一半以上，並拿掉最高的兩段泛音（3520／4692）—— 「噹」的刺耳感主要來自那裡；頻譜重心 4926→**3728Hz**。它仍然是鐘，但變成一記重音而不是長鳴。**這顆鐘另有 4 個 cue 在用**（`fate.cardReveal`／`pair.reveal`／`doom.roll`／已停用的 `pick.partyReady`），全部一起受益 —— 2 秒的鐘對任何 UI 重音都太長。④快取：audioDirector.js?v=11→13、版本表 208 條（be91d02e5b）；版本印記 `v1.21`。⑤**實測**（魔王討伐，按住魔法陣 3.6 秒）：時間軸為 `strike.charge 40ms → strike.release 3049ms → reveal.winner 3053ms → hero.attack 3720ms`，兩顆聲音仍然幾乎同時，但已是「衝擊 1.6s ＋ 短鐘 1.1s」而不是兩記 2 秒鐘疊加；兩支音檔皆 200，**console error 0**。
- 2026-08-14｜Claude Code｜**v1.20 最後一位選完角色時吹召集號角**：①睿哥指定「最後完成選角色的音效要變成號角響起」。新增 `party_horn.mp3`（`gen_sfx_v2.py` 的 `party_horn()`，33.5KB／2.4s）：**低音 G3 起、0.62s 後揚上五度 D4**，各自加低八度加厚 —— 典型的召集號。做了三個方向（單聲長號／雙聲呼喚／三聲齊鳴）給睿哥試聽，**先上 B 雙聲呼喚**。②**號角的三個關鍵**（少一個就會變回合成器的鋸齒）：起音從偏低滑上去（`bend`，真號角都會 scoop）、**亮度隨力度走**（用掃頻低通而非固定濾波）、以及全程都在的吹奏氣音。③**接線**：`audioDirector` 新增 `party_horn` 音檔與 `pick.complete` cue；`applyPick()` 改成 `audioCue(allPlayersPicked() ? "pick.complete" : "pick.lock")` —— **判斷放在寫入 `state.players` 之後**，`allPlayersPicked()` 才算得準。④**順手修掉一個會糊掉的疊音**：`#pick-next` 的 handler 原本在最後一位時會再補一記 `pick.partyReady`（→ `reveal_chime`，2 秒鐘聲），**跟 2.4 秒的號角同時響會糊成一團**。既然號角就是「全隊到齊」的訊號，改成只有還有人沒選時才放過場音。⑤快取：audioDirector.js?v=10→11、game.js?v=61→62、版本表 206→207 條（5f90544e2e）；版本印記 `v1.20 · 0814-0547`。⑥**實測**（4 人局逐位按決定）：第 1–3 位 `pick.lock`＋`pick.advance`，**第 4 位只有 `pick.complete`**（號角，無鐘聲），流程正常走到模式頁，`party_horn.mp3` 200，**console error 0**。
- 2026-08-14｜Claude Code｜**v1.19 拿掉魔王降臨專屬配樂，整場討伐一首到底**：①睿哥回報「魔王那段時間太短了，音樂剛出來就被切掉」。確實如此 —— 降臨段只有 6 秒影片（加集結約 7 秒），我卻在那裡插一首曲子再換掉，**任何曲子在 7 秒內都只來得及放前奏就被交叉淡化掉**。②提了三個方向請睿哥定奪（降臨曲延續到命運一擊／降臨也用討伐曲／整場都用降臨曲），**睿哥選「降臨也用討伐曲」**。③**整套中途換曲拆乾淨**：`presentBossRaid()` 移除開場的 `playRaidMusic("bossArrival")` 與 ACT 3A 的 `playRaidMusic("battle")`；`audioDirector` 移除 `bossArrival` 音軌與**已無人呼叫的 `playRaidMusic()`**（含 API export，不留死碼）；`assets/audio/bgm/boss_arrival.mp3` 刪除（原檔在 5e73340 的 git 歷史裡，要復原可取回）。現在整場魔王討伐就是 `setScene("play")` 的討伐曲一首到底。④**在原處留下註解記錄這個教訓**：要再加中途換曲之前，先確認那個段落有多長 —— 7 秒的段落放不下一首曲子。⑤`bgm/` 由 1.07MB 降回 **905KB**；版本表 207→206 條（03815da9f2）；快取 audioDirector.js?v=9→10、game.js?v=60→61；版本印記 `v1.19 · 0814-0543`。⑥**實測**：魔王戰全程的音樂呼叫只剩 `setScene(play)` 一次，`pick.mp3`／`battle.mp3` 皆 200，無 4xx、**console error 0**。
- 2026-08-14｜Claude Code｜**v1.18 「決定」的鎖定音改成木扣（睿哥從四個方案選 A）**：①睿哥回報 v1.15 的 `ui_lock` 是「噹，不好聽」。根因是我給的金屬 modal 共振**最長衰減 220ms**，變成拖尾的金屬鈴聲。**而且這顆聲音不只「決定」在用** —— `pick.lock`／`mode.select`（選模式）／`order.rank`（命運排序揭曉）／`team.reveal`（分組揭曉）共用同一支，一場下來會聽很多次，拖尾越聽越煩。②做了四個方向讓他試聽（木扣／悶金屬／封印／鎧甲扣帶），選定 **A 木扣**。③新配方：木頭 modal `[196, 431, 742, 1180]`，**時長 0.55s→0.30s、最長衰減 220ms→55ms、泛音全在 1.2kHz 以下、完全沒有金屬尾**；頻譜重心 1020Hz、RMS 0.094、峰值 0.61。④快取：只有 `ui_lock.mp3` 的雜湊改變（走逐檔版本表，其餘 40 幾支音檔不受影響）；audioDirector.js?v=8→9、版本表 2e65f92b7c；版本印記 `v1.18 · 0814-0537`。⑤實測 `ui_lock.mp3` fetch 200、無 console error。
- 2026-08-14｜Claude Code｜**v1.17 三首場景配樂換成睿哥的真實曲子，並新增「魔王降臨」專屬配樂**：①睿哥直接把三個 MP3 傳進對話（30.7s／30.7s／29.2s，44.1kHz 立體聲，約 -14.3 dBFS）：`summit_of_the_valiant`→選角、`the_maw_of_grolveth`→**魔王降臨（新的段落，原本完全沒有配樂）**、`gemini_music`→討伐。**至此全遊戲已無合成音樂**。②**匯入前先查播放端**，發現關鍵限制：`crossfadeMusic()` 用的是 **`source.loop = true` 的硬循環，接縫沒有任何交叉淡化** —— 直接丟 30 秒的曲子進去，每次循環到尾端都會「啪」一聲跳回開頭。新增 `tools/import_scene_bgm.py`：取末尾 2 秒淡出疊回開頭的淡入段、再切掉重疊的尾巴，**循環點因此變成原曲相鄰兩個取樣點**。實測接縫跳階 `0.00002`，比曲中一般的相鄰樣本差（0.011）小 500 倍。③**響度分兩級對齊**：選角曲壓到 **-18 dBFS 對齊 14 首角色 BGM** —— 點角色時是它們互相交叉淡化，不對齊會「一點角色就變小聲」；魔王兩首用 -16.5 dBFS（比選角略強，是高潮段）。峰值分別 0.63／0.80／0.74，無削頂。④**魔王降臨曲只取 14 秒**：那首能量從頭到尾平均（逐秒 dBFS 在 -13～-18 間無起伏），沒有「最戲劇性的段落」可挑，而降臨段實際只有 7 秒左右（4G 上最長約 14 秒），取整首等於多背 230KB。⑤**接線**：`audioDirector` 新增 `bossArrival` 音軌與 `playRaidMusic(key)`（守則同 `playHeroMusic` —— 只在 `play` 畫面有效，避免演出結束後殘留呼叫蓋掉結果頁音樂）；`presentBossRaid()` 在 **ACT 1 之前**切降臨曲、**ACT 3A 全軍突擊**時交棒討伐曲。**刻意不在 ACT 2 才切** —— 集結只有 0.5 秒，等到 ACT 2 會變成 8 秒內連換三次音樂。⑥`bgm/` 由 666KB 增為 **1.07MB**（+404KB）。快取：audioDirector.js?v=7→8、game.js?v=59→60、版本表 206→207 條（ce69c3cf22）；版本印記 `v1.17 · 0814-0448`。⑦**實測**：魔王戰的音樂切換序列為 `setScene(play) → playRaidMusic(bossArrival) → playRaidMusic(battle)`，三支音檔皆 200，**console error 0**。⑧**未動**：命運淘汰（survival）也有一段「魔王降臨」，但它只是 0.5 秒的立繪動畫，短到不值得換音樂，仍走 battle。睿哥要的話再說。
- 2026-08-14｜Claude Code｜**v1.16 刪掉沒人引用的 `bgm/home.mp3`，並關掉「重跑就把新音效蓋回電子音」的陷阱**：①**刪檔**：`assets/audio/bgm/home.mp3`（337KB）從睿哥改用自己的伴奏之後就沒有任何引用 —— `audioDirector.js` 的 `musicTracks.home` 指的是 `home_custom.mp3`。全 repo grep 確認只剩版本表與產生器提到它，屬純死檔，已 `git rm`。②**順帶關掉一個真的會咬人的陷阱**：`tools/generate_audio.py` 的 `jobs` 裡**仍然掛著全部 15 個 sfx**，也就是**任何人重跑一次那支腳本，就會把 v1.15 剛換上的音效整包蓋回舊的振盪器合成版**。已把 sfx 全部移出 `jobs`，只留 `bgm/pick.mp3` 與 `bgm/battle.mp3` 兩首仍由它產生；`sfx_*()` 函式保留當歷史參考，但在 `jobs` 上方寫明**不要再接回去**。同時移除已刪檔案的 `bgm_home()` 產生器。③版本表由 207 條降為 **206 條**（雜湊 ac2329b9bc，由工具自動更新到 index.html）。④版本印記 `v1.16`。⑤**實測**：`home_custom`／`pick`／`battle`／`sfx/boss_roar` 四支代表性音檔的網址都帶各自雜湊且 fetch 皆 200，`bgm/home.mp3` 已不在版本表中，全頁無 mp3 4xx、無 console error。⑥**待辦（等睿哥）**：`bgm/pick.mp3`（選角頁未點角色時的底曲）與 `bgm/battle.mp3`（魔王戰全程）**仍是合成的電子音**。睿哥的伴奏夾 `我的雲端硬碟/英雄命運抽（heroes fate)/伴奏/` 底下 `開始遊戲選單伴奏/`（1 首）與 `角色選擇伴奏/`（14 首）**已全部用完**，沒有現成曲子可用，需要**兩首新的**。Google Drive 連接器目前仍回 `requires approval`，睿哥說要授權但尚未生效；也可以直接把檔案傳進對話。**音效可以靠合成做到不像電子音，音樂不行** —— 管弦樂沒有真實採樣做不出來，這兩首一定要真曲子。
- 2026-08-14｜Claude Code｜**v1.15 15 個共用音效換上重做版（睿哥試聽通過）＋音檔改走逐檔雜湊**：①睿哥聽過 A/B 試聽頁後回「OK」，`tools/gen_sfx_v2.py` 的成品正式覆蓋 `assets/audio/sfx/`（15 個共 308KB，舊版 268KB）。做法見前一條：噪音瞬態 + modal 共振 + 卷積殘響，取代舊的 sine／saw／square 振盪器。②**沒有把 `REV` +1** —— `audioDirector.js` 原本所有音檔共用一個 `REV`，+1 會讓 **61 支音檔 4.13MB 在 4G 上整包重抓**，其中 4MB 是完全沒動過的 14 首角色 BGM。這正是 2026-08-13 影片那次踩過的坑。改為與影片／圖片同一套機制：`gen_asset_versions.py` 的 `PATTERNS` 加入 `assets/audio/**/*.mp3`（版本表由 151 條增為 **207 條**），`asset()` 改成 `window.HF_ASSET_V?.[path] || REV` —— **只有換掉的 15 個音效網址會變**，其餘照走快取。③快取：audioDirector.js?v=6→7、assetVersions.js?v=bea3d07cce（由工具自動更新）；版本印記 `v1.15 · 0814-0221`。④**實測**：`boss_roar`／`heroes/bgm/knight`／`bgm/pick` 三支代表性音檔的網址都帶各自的雜湊（`7c648612ee`／`65fc85e741`／`3784bcef25`）且 fetch 皆 200，全頁無 mp3 4xx。⑤**下一步**：睿哥已同意授權 Google Drive 連接器，接著處理 `bgm/` 那 3 首場景曲（home／pick／battle）—— 它們仍是合成的管弦樂，音效可以靠合成做到不像電子音，**音樂不行**，要換成睿哥伴奏資料夾裡的真實曲目。
- 2026-08-14｜Claude Code｜**v1.14 魔王降臨補上吼叫聲（整段原本是靜音的）**：①**根因兩個疊在一起**：(a) **降臨影片沒有音軌** —— 檢查 `boss/arrival.mp4` 的 atom，`soun`／`mp4a` 都不存在（Sora 匯出的片一律沒聲音），所以那 6 秒的聲音完全靠音效撐；(b) 而音效的時機全錯了 —— `boss.enter`（降臨重音）下在 `playStageClip()` **之前**，4G 上影片還要載好幾秒，等於「重音對著還沒出現的畫面響」；`boss.roar` 則排在整支**播完之後**，那時已經進 ACT 3A 全軍突擊。結果就是**中間 6 秒的降臨本身全靜音**。②**修法**：`playStageClip()` 新增 `opts.onPlay` 回呼，在 `video.play()` 之後、`await` 之前觸發 —— 聲音一律從「影片真的開播」這一刻起算，不受載入時間影響。ACT2 改成：第一幀下 `boss.enter`，再用 `setTimeout(BOSS_ROAR_CUE_MS = 3400)` 讓 `boss.roar` 在魔王走到鏡頭前時響（片長 6.04s、吼聲 2.2s，3.4s 下去剛好收在片尾之前）。**這個常數刻意不乘 `fast`** —— stage clip 一律 1× 播放，乘了反而對不上畫面。③**補了一個自己差點做出來的洞**：原本寫成「影片播完就 `clearTimeout`」，那麼**影片只要短於 3.4 秒（載不動、被跳過）就完全不會吼**，等於把同一個 bug 換個形式留下。改成 timer 還沒燒到就當場補吼一聲；連播都沒播成才把重音與吼聲一起補。④快取：game.js?v=58→59；版本印記 `v1.14 · 0814-0216`。⑤**實測**（攔截 mp4 改回可播的 webm，分別用 7 秒與 3 秒代打片）：7 秒片 → `boss.enter` 2607ms（第一幀）、`boss.roar` 6008ms（正好 3401ms 後，落在影片中段）、`battle.clash` 9606ms；3 秒片 → 吼聲由 fallback 在 5220ms 補上，趕在 clash（5222ms）之前，**降臨段不會再有靜音的情況**。console error 0。
- 2026-08-14｜Claude Code｜**新增 `tools/gen_sfx_v2.py`：重做 15 個共用音效（等睿哥試聽後才換上）**：①**診斷**：睿哥要「全面提升配樂及音效，不要再出現類似電子音的」。聲音其實分兩類 —— **14 角選角 BGM、14 角攻擊音、14 角勝利音、首頁伴奏都是真實錄音**（來自睿哥的伴奏檔與 Sora 影片），沒問題；**電子音全部出自 `tools/generate_audio.py`**，它用 sine／saw／square 振盪器加簡單包絡合成了 `assets/audio/sfx/` 15 個音效與 `assets/audio/bgm/` 的 3 首場景曲（home／pick／battle），本質上是 8-bit 合成器。②**新腳本的做法**：真實感的關鍵不在「用不用合成」，而在舊版一件都沒做的三件事 —— **噪音瞬態**（真實撞擊的能量幾乎都在最前面幾毫秒的噪音，不在音高）、**modal 共振**（金屬與鐘的音色是一組非諧波共振峰各自衰減，用單一正弦做不出來；實作是用敲擊噪音去激發共振器，不是直接播正弦）、**卷積殘響**（IR 由衰減噪音合成，這是「像真的」與「像嗶聲」最大的分水嶺）。另有時變 state-variable 低通掃頻、軟飽和、極短左右延遲做立體聲寬度。③**雲端可獨立產出 MP3**：`pip install numpy scipy lameenc`，**不需要 ffmpeg**（lameenc 直接編碼），15 個 3 秒跑完共 308KB（舊版 268KB）。這推翻了 `CLAUDE.md` 說「雲端 session 做不到 ffmpeg 轉檔」的部分前提 —— **音訊合成與 MP3 編碼在雲端完全可行**，做不到的只有讀 Google Drive 與轉既有影片。④**調校紀錄**：`attack_heavy` 第一版頻譜重心 5919Hz（聽起來會是「嘶」一聲、毫無重量），把碎屑噪音由 highpass 900 改為 bandpass 420、低頻 body 加重後降到 **389Hz**；`smoke_burst` 音量偏低已補 1.4×。⑤**狀態：尚未換上遊戲素材。** 音檔產在暫存區，另做了 A/B 試聽頁（15 組並排，舊／新各一顆播放鈕）給睿哥在手機上判斷，等他回覆再覆蓋 `assets/audio/sfx/`。⑥**3 首場景 BGM 未動**：音效可以靠合成做到不像電子音，**管弦樂不行** —— 沒有真實採樣就是做不出來。兩條路等睿哥決定：用他 Drive 伴奏資料夾的既有曲目（需要他授權 Google Drive 連接器，本次呼叫被擋下），或他直接提供音檔。
- 2026-08-14｜Claude Code｜**v1.13 魔王討伐全程移除魔王立繪**：①睿哥指定「魔王降臨完，後面就不要再出現小魔王被撞擊」，並選了「立繪整個拿掉」。`presentBossRaid()` 內所有 `boss.classList.add(...)` 全部移除：ACT 3A 全軍突擊後的 `show`、ACT 4 怒吼的 `show/roar`、ACT 5 揭曉的 `show/hurt`、命運審判收尾的 `show/roar`、ACT 6 的 `hurt/down`（「魔王倒下」沒有立繪可演，只留 `boss.defeat` 音效）；勝利片的 `bossEl` 改傳 `null`，免得它去操作一個永遠不顯示的元素。**保留**煙霧、震動、聚光與勝者揭曉閃光 —— 那些不是魔王立繪的演出。函式結尾的 `boss.classList.remove(...)` 留著當保險。②`assets/boss_model_v3.webp` 的 `<link rel=preload>` 由 **high 降為 low**：魔王討伐已完全不用它，只剩命運淘汰會用到，沒理由每次開頁都高優先搶 97KB 的頻寬。③**命運淘汰（survival）未動**：那個模式的魔王完全靠立繪演出（降臨 `enter` 撞擊、每次淘汰 `hurt` 閃、最後 `down` 倒下），拿掉等於整個模式沒有魔王。睿哥的指示是針對魔王討伐，所以我沒有順手改，**等他決定**。④快取：game.js?v=57→58；版本印記更新為 `v1.13 · 0814-0155`。⑤**實測**（393×760，2 人，魔王討伐）：ACT 序列 `gather→arrival→clash→attack→fate→attack[FINAL]→fate(命運卡逆命轉審判)`，**全程 `boss.classList.contains("show")` 皆為 false**；命運一擊按住實測 p 0.13→0.43→0.73、符文依序點亮、3 秒完成，**console error 0**。
- 2026-08-14｜Claude Code｜**v1.12 首頁加版本印記（解決「改了但手機看到舊的」的判讀問題）**：①**起因**：睿哥回報「沒變…？」，但對時間後發現 **v1.11 的 Pages 部署 17:21:42Z 就成功了，截圖是 17:44Z（手機 1:44am）—— 晚了 22 分鐘**。截圖裡的舊環形進度條與角色大頭圖分別是 v1.10.x 的行為，也就是**手機拿到的 index.html 是舊的**。②**根因**：`index.html` 頂端那三個 `<meta http-equiv="Cache-Control">`／`Pragma`／`Expires` **對瀏覽器的 HTTP 快取完全沒有作用**（只有真正的 HTTP header 算數），而 GitHub Pages 給 HTML 的是 `max-age=600`；更關鍵的是**分頁只要一直開著不重新整理，index.html 根本不會再被抓一次**，於是所有 `?v=` 的努力全部失效。這是本專案最容易誤判的坑：我們兩邊都在猜「到底上了沒」。③**修法**：首頁面板下方加一行 `.build-stamp`（`v1.12 · 0814-0130`），淡色小字、可選取。**看得到印記＝新版；看不到整行＝舊版**，一眼分辨，不必再靠猜。**改版時要記得同步改這行**（就在 `index.html` 首頁區塊，旁邊有註解說明）。④快取：revamp.css?v=27→28。⑤**未處理，等睿哥回覆**：「魔王降臨完後面不要再出現小魔王被撞擊」有兩種可能 —— (A) 魔王立繪整個從演出移除，(B) 立繪留著當背景、只拿掉撞擊閃光與怒吼震動。睿哥給的命運一擊設計稿裡魔王是看得見的，所以我沒有自作主張，已去信詢問。
- 2026-08-13｜Claude Code｜**v1.11 魔王降臨只播影片＋命運一擊改用睿哥提供的魔法陣**：①**魔王降臨拿掉「小圖撞擊」那套設計**（睿哥指定）：ACT2 原本是「播降臨影片 → 影片播完再把魔王立繪 `show` 上來 ＋ `impactFx` 撞擊閃光」，缺片時還會退回立繪的 `enter` 飛入動畫。現在整段只剩影片本身，魔王立繪改從 **ACT 3A** 才進場（`clash` 那段本來就會 `boss.classList.add("show")`）。②**降臨影片的順暢度**（睿哥：要順不要卡，交給我優化）三處：(a) `playStageClip()` 原本硬寫 `waitMediaReady(video, 1000)` —— **1.35MB 的降臨片在 4G 上一秒絕對載不完，於是整段被判定沒就緒直接跳過**，這就是「魔王降臨看不到」的根因；新增 `opts.readyMs`，降臨用 **8000ms**。(b) 新增 `opts.untilEnded`，改等 `ended` 事件而不是硬等 7100ms，起播晚了也不會被攔腰切掉（上限 9000ms 當保險）。(c) 新增 `prefetchArrivalClip()`，**在「選擇命運儀式」那一頁就開始抓**降臨片，玩家挑模式的那幾秒剛好拿來下載。③**命運一擊改用睿哥提供的魔法陣**（`IMG_2428` 暗版／`IMG_2427` 亮版）：兩張稿子是 1008×1792 的整頁 mock（含魔王、標題字、略過鈕），只取中間的魔法陣 —— 以 (500,990) 為心裁 760×760，再套半徑 295→350 的放射狀 alpha 遮罩，**同時去掉四角的魔王手與底部的「全員佔領螢幕」字樣**；縮到 640×640 輸出 WebP（暗 46KB／亮 92KB，PNG 版 536KB／705KB 太大不採用）放 `assets/deco/fate_circle_{dim,lit}.webp`。④**演出邏輯**：`.strike-gate` 每一幀寫入 `--p`（按住進度 0→1），CSS 全部吃這個值 —— 暗版永遠在、亮版 `opacity` 隨進度推到 0.6（不推滿，把亮度留給符文），四個符文小圓在 **22%／45%／68%／90%** 各自點亮，**3 秒到時四個全亮才揭曉最後一擊的獲勝者**；放開手 `--p` 歸零整組回暗版重新開始（這段 hold 邏輯本來就是這樣，只換了視覺）。**符文用羽化遮罩而不是硬邊 `clip-path`** —— 硬邊會變成「貼上一顆發光圓球」，羽化後才像那個符文自己亮起來；四個都是同一張亮版圖、同一個框只是各自遮罩，所以位置一定對得上，不必另外切圖。⑤**版面**：`.strike-gate` 底色由 `rgba(6,2,14,.93)` 壓淡到 `.78`／中心 `.72→.28`，讓魔王透出來（稿子裡魔王是看得見的）；倒數數字原本絕對置中會蓋住魔法陣中央的六芒星，改成跟著版面走的小字。舊的 `.strike-ring` 與 `#strike-ring` 全部移除。⑥快取：revamp.css?v=26→27、game.js?v=56→57；新素材網址帶 `?v=1`（**換圖時要記得 +1**，`assets/deco/` 不在 `gen_asset_versions.py` 的逐檔雜湊表裡）。⑦**實測**（393×760，用 VP8 webm 代打 mp4 讓本機 Chromium 播得動）：`--p` 逐段檢查 0／0.2／0.3／0.55／0.8／1 → 亮版 opacity `0／0.12／0.18／0.33／0.48／0.6`、符文 `0／0／1／2／3／4` 個亮起，截圖確認四個符文位置正確且邊緣融進暗版；真實按住實測 p 0.13→0.43→0.73，倒數 3→2→1，3 秒到自動完成；魔王戰 ACT 序列 `gather(boss:false)→arrival(**boss:false**)→clash→attack(boss:true)→fate→attack[FINAL]→victory`，**console error 0**。
- 2026-08-13｜Claude Code｜**v1.10.4 進入任何動畫前一律不顯示角色靜圖（睿哥指定）**：①**要求**：「進入『任何』動畫前不要跑出角色的大頭圖案」—— 撤掉 v1.10.2／v1.10.3 加的頭像頂替。②**選角舞台**：`videoPlayer.play()` 的揭幕 promise 改成**只有影片真的開演（`playing` 事件）才 resolve(true)**，載入中不再頂任何靜圖，舞台維持空的召喚陣；原本 260ms 就頂立繪的 `STILL_FALLBACK_MS` 換成 `REVEAL_GIVEUP_MS = 15000` 純保險（避免 pending 的 `play()` promise 把呼叫端永遠掛住，那正是 v1.10.1 修掉的原始 bug）。`updateModelPreview()` 改為**換角色時先把 `is-empty` 加回去**（不然慢速網路下會看到上一位角色的畫面配新角色的名字），揭幕改由新的 **`onShown` callback** 負責。③**`onShown` 放在 `activateVideo()` 而不是 `play()` 裡**：確定動畫 `playOnce()` 走的是另一條路徑，只掛在 `play()` 會導致「舞台還是 `is-empty` 隱藏著，確定動畫在裡面播完，玩家什麼都沒看到」。`showStill()`（整支缺檔的防呆路徑）也會呼叫，因為那時沒有動畫要等了、靜圖就是最終畫面。④**攻擊／FINAL 切入**：`.hero-cut-poster` 改回 `display:none`，也不再設頭像；影片元素維持「真的開播（`.is-playing`）前透明」。載入中就是深色底＋名牌，不頂立繪、不頂頭像、也不讓 video 自己的 poster 露出來（那會變成上一位角色殘留或半張解碼）。v1.10.3 修的「每輪開頭清掉 `is-playing`」保留 —— 那是「名字換人了圖還是上一位」的根因。⑤**保留 v1.10.2／v1.10.3 的頻寬修正**（那些與大頭圖無關）：預抓釋放連線與連兩支慢就停、三處小頭像不再載 280KB 立繪、poster 預抓改 low priority。⑥快取：revamp.css?v=25→26、videoPlayer.js?v=27→28、game.js?v=55→56。⑦**實測方法突破**：雲端 Chromium 沒有 H.264，先前只能驗到 fallback。這次用 Playwright 的 ffmpeg 把一張 poster 編成 VP8 webm，再攔截 `**/*.mp4` 以 `Content-Type: video/webm` 回傳，**第一次驗到「影片真的開播」那條路徑**。結果（393×760）：快速時點角色 → `is-empty:false`／`state:playing`／舞台可見，確定動畫與第二位待命片都正常接續；每支延遲 3 秒時，載入中 `is-empty` 維持 true、**完全沒有任何靜圖**，clip 一就緒就揭開；魔王戰切入序列 `野蠻人→騎士→FINAL 野蠻人`，`vidOp` 由 0 淡入 1、`posterShown` 全程 false，**console error 0**。⑧**副作用（已知並接受）**：網路慢時選角舞台會停在空的召喚陣直到動畫載好，這是「不要假圖」的直接代價。
- 2026-08-13｜Claude Code｜**v1.10.3 攻擊切入不再顯示上一位的半張 poster＋砍掉一堆搶頻寬的 280KB PNG**：①**症狀**（睿哥截圖）：魔王戰攻擊切入標的是「玩家 2 · 騎士」，畫面卻是**公主的 poster 只解碼了上面 40%，下面整片黑**。②**兩個原因疊在一起**：(a) `playAttackSequence()` 的 `is-playing` class **只加不移除** —— 第一位加上去之後就一直在，所以第二位以後影片元素從一開始就是可見的，會直接畫出上一位殘留的 poster；(b) `video.poster` 是每位角色一個新的 30KB 請求，4G 上還沒下載完就被畫出半張。③**修法**：重新啟用早就存在但被 `display:none` 停用的 `.hero-cut-poster` 圖層（當年停用是因為方形立繪比例不同會變形，**改用 `object-fit:cover` 就沒這問題**），每位角色把它設成**該角的選角頭像**（13KB、選角格已顯示過必定在快取裡）；影片元素改成**真的開播（`.is-playing`）之前保持透明**，並在迴圈每一輪開頭移除 `is-playing`。這樣切入層永遠是「正確角色的清楚特寫」，影片就緒才蓋上去。`playFinalBlow()` 同步處理。④**順手砍掉三處搶頻寬的 280KB PNG**：選角頁的玩家小圓點 `.pdot img`、戰鬥 HUD 小徽章 `.battle-hud-badge img`、舞台英雄列 `.stage-hero img`（顯示尺寸只有 58×64）原本都載 512×512 立繪（280KB），全改用 240×322 頭像（13KB）—— 4 人局等於在魔王戰現場少搶 1.1MB。`prefetchPosters()` 的 `fetchPriority` 由 **high 改 low**：poster 只是頂替用，搶在玩家正在等的攻擊影片前面反而害它更慢。⑤快取：revamp.css?v=24→25、game.js?v=54→55。⑥**實測**（393×760，2 人 公主＋騎士，魔王討伐）：act 序列 `gather→arrival→attack→fate→reveal→attack[FINAL]→victory`，兩次切入的 `#hero-cut-name` 與頂替圖**逐位一致**（公主→princess.jpg、騎士→knight.jpg），影片未就緒時 opacity 維持 0，截圖確認是滿版的正確角色特寫而非半張黑畫面，**console error 0**（另見下方 ref_battle 的 404）。⑦**發現一個既有問題，未處理，等睿哥指示**：ACT 3A「全軍突擊」播的 `assets/ref_battle_mobile.mp4` **被 `.gitignore` 排除**（第 11 行），所以**它從來沒有上過 GitHub Pages，手機版每場魔王戰都是 404**，只有睿哥的 Mac 本機看得到那段。要嘛把它轉成手機版納入版控，要嘛拿掉 ACT 3A 的影片改用純 CSS 演出 —— 這是素材與 repo 大小的決策，我沒有自作主張。
- 2026-08-13｜Claude Code｜**v1.10.2 頂替立繪改用頭像＋只站在召喚陣上＋預抓不再吃滿 4G**：睿哥回報三件事，一件是我上一版的副作用，一件是既有問題，一件不是 bug。①**「立繪整個蓋住石龍」**：`.vp-still` 原本是 `inset:0` 鋪滿整個框，但直式影片是以 `contain` 落在中央那一條（14 支裡 13 支是 720×1270，佔框寬 55%）。靜圖用 512×512 方形素材鋪滿，自然就壓到兩側石雕。改為與影片同一個框：`left:50%` + `aspect-ratio:720/1270` + `translateX(-50%)` + `object-fit:cover`，實測 393×760 下寬度佔比 55%、左右留白各 86px 完全對稱，與影片 footprint 一致。②**「立繪破圖成 ?、超級卡」**：頂替用的是 `assets/heroes/{id}.png`（280KB），在網路已經塞爆時等於再排一個 280KB 請求 —— 睿哥 11:14 的截圖是 PNG 只解碼出頂端一條、11:16 就變成破圖 `?`。改用**選角卡那張 240×322 頭像**（`portraits/{id}.jpg`，13KB），它在選角格上已經顯示過、必定在快取裡，**0 位元組就能立刻出現**；實測點角色後發出的請求只剩 14 張快取中的頭像與那一支 `wait/amazon.mp4`，不再有 PNG。③**預抓把 4G 吃光（超級卡的主因）**：`prefetchWaitClips()` 建立的 13 個 `preload="auto"` video **就算 canplay 了也會繼續把整支抓完，而且從來沒被釋放** —— 2.5s 一支往下推，等於逐漸疊出 13 條背景連線在搶頻寬。三處修正：(a) 新增 `releaseWaitClip()`（`removeAttribute("src")` + `load()`）真正中止下載；(b) 同時間最多留 2 支在背景緩衝，其餘立刻放掉；(c) **連續兩支慢（>2s 或逾時）就整串停掉**，這條線路撐不起預抓就別硬抓；另外 `yieldToActivePick()` 現在一偵測到玩家在等，會**立刻把整池背景連線收掉**，不是只暫停排隊。④**「野蠻人變成北極人」不是 bug**：`data.js` 是 `name: "野蠻人"`／`weapon: "鏈錘"`，截圖顯示「北極人 鏈鎚」且**下方選角卡仍是「野蠻人」**，網址列也有翻譯圖示 —— 是 Safari／Google 的網頁自動翻譯在改寫畫面文字。請睿哥在網址列點翻譯圖示選「顯示原文／不要翻譯這個網站」。⑤快取：revamp.css?v=23→24、videoPlayer.js?v=26→27、game.js?v=53→54。⑥**實測**（393×760，Playwright 攔截 `**/*.mp4` 延遲 3 秒）：靜圖幾何 55%／左右各 86px；四位玩家完整選角走到模式頁，有節流與無節流都通過，**console error 0**。**仍未在雲端驗證**：H.264 實際播放（本機 Chromium 不含專利編解碼器）。
- 2026-08-13｜Claude Code｜**v1.10.1 選角舞台不再停在空的召喚陣（待命／確定動畫沒顯示的根因）**：①**症狀**：睿哥手機 4G 上點了角色後，舞台一直是空的召喚陣（左右石雕龍＋`deco/empty_stage.jpg`），待命動畫與確定動畫都沒出現。②**根因不是影片壞掉，是「揭幕條件」寫錯**：`updateModelPreview()` 是 `await vp.play(...)` 之後才 `pedestal.classList.remove("is-empty")`，而 `is-empty` 會把整個 `.sprite-stage` 設成 `opacity:0; visibility:hidden`；`play()` 內部又是 `await target.play()` 才揭曉。**iOS 上 `video.play()` 的 promise 在影片真的開始播之前不會 resolve**，所以只要影片還在下載，這條鏈就整條卡住 → 舞台永遠是空的，而且**不會有任何 error**（本機 headless Chromium 因為沒有 H.264 會立刻 error 走 `showStill()` 的 fallback，所以在電腦上看不出來，這是先前沒抓到的原因）。③**修法**：`play()` 改為回傳新的 **reveal promise** —— 在「影片開播」**或**「立繪頂上（`STILL_FALLBACK_MS = 260ms`）」任一先發生時就 resolve，呼叫端因此一定會揭開舞台；影片是否已開演改用 **`playing` 事件**判定（不再靠 `play()` 的 promise），所以影片晚幾秒才就緒也會自動把立繪換成動畫。原本刻意不鋪立繪的理由（「避免先出現比例不同的靜圖」）用 260ms 延遲保留：快取熱的時候影片先開播，靜圖不會閃。④**確定動畫同樣補上**：`playOnce()` 原本 `readyState < 2` 就直接 `finish()`（畫面上什麼都沒有就跳過），改為**畫面上沒有任何影片在演時**先 `showStill()` 頂最多 900ms 再收；已經有待命片在演的情況維持原樣，不會用靜圖蓋掉正在播的動畫（新增 `hasVisibleVideo()` 判斷）。⑤**預抓讓路**：`prefetchWaitClips()` 每支之間插入 `yieldToActivePick()`，選角播放器處於 `loading`／`confirm-loading` 時暫停預抓（上限 6 秒防卡死）—— 預抓是照 `HEROES` 順序排隊的 13 支，跟玩家眼前那支搶頻寬，**龍騎士排最後最容易中**，正好就是睿哥截圖的角色。⑥**為什麼是今天才發生**：前一版把全域 `MEDIA_VERSION` 改成逐檔內容雜湊，等於**一次性把 70 支影片的網址全換掉**，手機快取全失效要重抓約 26MB，於是「影片還沒載完」這個一直存在的空窗被放大成「完全看不到動畫」。⑦快取：videoPlayer.js?v=25→26、game.js?v=52→53（沒動素材，MEDIA_VERSION／ART_VERSION 與逐檔雜湊表都不變）。⑧**實測**（360×640，用 Playwright 攔截 `**/*.mp4` 延遲 3 秒模擬 4G）：改前點龍騎士後 2.4 秒仍是 `is-empty=true`、立繪未顯示；改後 0.4 秒就 `is-empty=false` 並顯示 `dragon_knight.png`，截圖確認舞台是角色立繪而非空召喚陣；快速連點三位角色最後停在正確的龍騎士；四位玩家完整選角走到模式頁，**console error 0**。`js/rng.js` 未修改，SHA-256 仍為 `9ab55a96f19f162c1380a06cc7b3f2d496fb088aa393d570afeef7ef662f021a`。**本機無法驗證 H.264 播放**（雲端 Chromium 不含專利編解碼器），影片實際播放仍需睿哥手機確認。
- 2026-08-13｜Claude Code｜**v1.10 角色名稱修正／確定動畫可點擊跳過／魔王立繪與降臨影片換新**：①**7 個角色改名**（睿哥標紅字的那幾個；白字 7 個與 `data.js` 完全一致，可反向確認判讀正確）：`ranger` 遊俠→**暗夜精靈**、`orc_archer` 半獸人射手→**光之精靈**、`amazon` 女戰士→**野蠻人**、`dark_fighter` 暗黑武鬥家→**武鬥宗師**、`assassin` 黑袍刺客→**暗影刺客**、`dark_mage` 黑暗法師→**召喚師**、`dark_elf` 黑暗精靈→**巫毒薩滿**。只改 `name`，**`id` 一律不動**（70 條影片路徑、poster、音效、頭像全靠 id）；改名用「id 後緊接的 name」正則逐一取代並斷言每個只中 1 次。已確認舊名沒有寫死在其他檔案，`flavor`／`victory` 台詞也沒提到角色名。新名字最長 4 字（舊的最長 5 字），卡片版面只會更寬鬆。**睿哥另外傳圖確認「光之精靈的最後一擊」就是 v1.9 放進 `final/orc_archer.mp4` 的那支**，反向印證 v1.9 的配對正確。②**確定選擇動畫可點一下跳過**（睿哥：除非自己想看完，否則不該被迫等它跑完）：`videoPlayer.playOnce()` 新增第 5 個參數 `opts`，支援 `tapSkip`／`tapGraceMs`（預設 500ms）／`tapTarget`；`game.js` 的 `applyPick()` 傳入 `{ tapSkip: true, tapTarget: $("#screen-pick .model-pedestal") }`（用整個舞台座當觸控區，比影片框大好按）。**防誤觸**：寬限期從「影片真的開始播」起算，避免按「決定」那一下的殘留觸控立刻把動畫跳掉。badge 文案變成「鎖定中…　點一下跳過」。跳掉後自然回到既有流程 → 下一位玩家的待命動畫。③**魔王小圖換新**（睿哥放在 `角色圖/魔王降臨/IMG_2370.JPG`，1792×1008 純白底展翼魔王）：以「近白且低彩度」門檻去背（主體最亮處是高彩度粉紅火焰，不會被誤判），加半透明過渡帶柔化邊緣，殘留不透明近白像素 0、主體無破洞；裁到主體後為 1646×976（比例 1.686），輸出 `assets/boss_model_v3.png`（1000×593，593KB，新檔名本身即快取隔離，舊 v2 保留可回退）。**注意：舊立繪主體近正方（1.019），新的是 1.686 偏寬**，`.boss { width }` 若不動會變得又扁又小，因此基準由 `min(285px,67vw)` 放大為 `min(348px,94vw)`，`@media (max-height:760px)` 那條也由 `min(250px,62vw)` 放大為 `min(305px,87vw)`（**這條原本是漏掉會踩到的坑，多數手機都會命中**）。實測 360×640 下魔王佔舞台寬 85%／高 34%，位置與 DEMON LORD 標籤都正常。④**魔王降臨影片換新**：睿哥 19:42 又換了一支（`fec9a406`，1248×1664／6.04s，取代舊的 `b9451727`），轉 720 寬 CRF 29 為 `boss/arrival.mp4`（720×960／1.42MB，全片解碼無誤），poster 同步重製。⑤順手修掉 `videoPlayer.js` 裡 `primeStill()` 寫死的 `?v=2`，改為新增的 `ART_VERSION` 常數並與 `game.js` 對齊。⑥快取：**MEDIA_VERSION 13→14、ART_VERSION 3→4**、data.js?v=3、revamp.css?v=21、videoPlayer.js?v=24、game.js?v=51。⑦**實測**：14 個新名稱正確顯示在選角卡；跳過機制以獨立 player 實例驗證 —— 寬限期後點擊在媒體時間 1.95s／總長 3.04s 處立即收尾，寬限期內的點擊則被正確忽略且繼續播；魔王舞台截圖確認新立繪比例正確；console error 0。改前備份：`/private/tmp/heroes_fate_pre_v19/boss/`。
- 2026-08-13｜Claude Code｜**v1.9 睿哥修正的 3 支影片換上（龍騎士 attack／騎士 final／幽靈射手 final）**：①**用修改時間找出變動**：整個素材夾只有 3 個檔是今天（08-13）動過的 —— `攻擊魔王動畫/copy_6D2445BD-…mov`（08:09）、`最終戰勝者對戰動畫/…dc96988b….mp4`（08:13）、`…fb225175….mp4`（08:31）。**教訓：第一次只 `-iname "*.mp4"` 會漏掉 CapCut 匯出的 `.mov`**，兩個夾補上 `.mov` 後都剛好 14 檔，才對得起來。②**用「少了哪支」＋抽幀雙重確認角色**，不靠猜：final 夾少了 `34af6117` 與 `34A64C62`、attack 夾少了 `96AFB8AC`。抽幀比對頭像與攻擊片後確認 `dc96988b`=**knight**（銀色板甲＋直劍＋披風，與其頭像／攻擊片一致，結尾魔王倒地）、`fb225175`=**orc_archer**（黑髮精靈弓手＋綠皮甲＋金光弓，與現行 `final/orc_archer.mp4` 是同一角色同一套動作編排的高解析重算版；注意 `34af6117` 原本標記為 ranger，但 v1.8 互換後它服務的是 orc_archer，所以新片要換到 orc_archer）、`copy_6D2445BD`=**dragon_knight attack**（與現行同構圖）。③**新龍騎士攻擊片是乾淨的**：檢查右上角與最後 0.5s，**沒有 CapCut 浮水印、沒有黑幕片尾**，因此 v1.6 為裁浮水印而用的 `crop=iw*0.91:ih*0.89` 與 `-t 3.25` 都取消，畫面由被裁過的 720×1058 恢復為**完整 720×1080**。④**轉檔**：attack 用 CRF 29（820KB／3.08s），兩支 final 用 CRF 33（knight 2.03MB／720×1280、orc_archer 1.41MB／720×1080，來源 1152×1728 降到 720 寬）；三支全片解碼無誤。⑤**poster 同步重製**（400 寬），比例逐支核對與影片一致（0.6667／0.5618／0.6667）以防拉伸。⑥快取：**MEDIA_VERSION 12→13、ART_VERSION 2→3**、videoPlayer.js?v=23、game.js?v=50。⑦**實測**（360×640，2 人 騎士＋龍騎士，魔王討伐）：act 序列 `gather→arrival→clash→attack(attack/knight.mp4?v=13)→attack(attack/dragon_knight.mp4?v=13)→fate→reveal→attack[FINAL]`，poster 全部帶 `?v=3`；另以獨立 video 元素驗證三支新片皆 `loadeddata`＋可播（尺寸／時長／比例如上）、三張新 poster 皆載入成功，**console error 0**，manifest 仍 14 角 70 條引用零缺檔。改前備份：`/private/tmp/heroes_fate_pre_v19/`。**順帶發現（未處理，等睿哥指示）**：`final/ranger.mp4` 整支只有魔王、看不到英雄本人，語意上不像「英雄擊敗魔王」，可能也需要重做。
- 2026-08-13｜Claude Code｜**上 GitHub ＋ GitHub Pages，手機端不再依賴這台 Mac**：①**起因**：Remote Control 那條路實測不可靠 —— 睿哥手機的 `＋ New session` 預設開到**雲端 Linux 容器**（花 9 分鐘全機掃 `PROJECT_NOTES.md` 才回「讀不到」），之後 Devices 更直接變成「No recently connected devices」，而本機查證 `Claude.app` 從前一晚 20:04 起一直在跑、隧道回 200、`pmset` 不睡，**斷的是手機與 Mac 的配對層**。睿哥表示「不能隨時用電腦」，因此改走不依賴本機的路線。②**公開前安全檢查**：`grep` 命中的 `token` 全是 `playToken` 變數名與筆記裡的「request-token」字樣，**無金鑰**；唯一真的 PII 是 `tools/extract_sora_audio.py` 與 `tools/extract_character_bgm.py` 把 gmail 寫在 Drive 路徑裡。兩支改為讀環境變數（`HF_DRIVE_ROOT`／`HF_DRIVE_BGM`），未設則自動 glob `~/Library/CloudStorage/GoogleDrive-*/我的雲端硬碟` 並依序試「英雄命運抽（heroes fate)」與舊名「英雄旅途」—— **順手修掉它們原本還指向舊資料夾名的問題**；實測兩支都正確解析到改名後的路徑，`py_compile` 通過。③**建 repo**：`git init -b main`，`.gitignore` 排除 `assets/anim`、`assets/videos/{wait,confirm,victory}`、參考片與 `.DS_Store`／`__pycache__`，**476MB→73MB、265 檔、無單檔超過 10MB**；建立公開 repo `longxia7hao-dev/heroes-fate` 並 push。④**開 Pages**（`main` 分支根目錄），實測 10 項關鍵資源全部 HTTP 200 且 MIME 正確（html／js／css／json／mp4／jpg／mp3）。⑤**額外收穫**：Pages 對 Range 請求正確回 **206**，而本機 `python3 -m http.server` 回 **200** —— 那正是 v1.2.1 查到的 iOS Safari 音訊播不出來的根因。**所以手機測試改用 Pages 網址比用本機 server 更接近正確行為**。⑥`js/game.js` 的 `fillPhoneHint()` 由寫死的 cloudflared 隧道網址改為 Pages 永久網址，並新增 `github.io` 分支；game.js?v=48→49。⑦`CLAUDE.md` 與本檔補上新工作流：**收工必 push、動工先 pull、repo 是程式碼事實來源、雲端 session 做不到 ffmpeg 與讀 Drive**。
- 2026-08-13｜Claude Code｜**交接準備：新增專案層 `CLAUDE.md`**：睿哥要改用手機端 Claude Code 接手，但「動工前先讀 `PROJECT_NOTES.md`」這條規則原本只寫在 `~/.claude/CLAUDE.md`（本機使用者層）與雲端硬碟工作區的 `CLAUDE.md`，**雲端／手機 session 兩者都吃不到**，接手的 agent 不會知道要讀共享筆記。因此在專案根目錄新增 `CLAUDE.md`（會隨專案本身自動載入，跨裝置一致），內含閱讀順序、收工要更新本檔、專案鐵則（seed-first／改 rng.js 要先問並記雜湊／一局一位勝利者／無 build 無框架）、啟動指令、三個最常踩的坑（快取三層版本號、換影片要重製 poster、演出流程的 early return）與驗證方式。同時在 `AGENT_HANDOFF.md` 頂端加「⚠️ 先讀這段」標明其現況數字已過時、以本檔為準，並更新 60 秒摘要的規模（412MB→476MB，註明實際在用約 70MB）與版本（v0.94+／13 角 → v1.8／14 角）。**交接路線已定（睿哥決定）**：手機端 Claude Code 走 **Remote Control 接這台 Mac mini 的 session**（不是雲端沙箱），所以檔案、ffmpeg、Google Drive 素材夾、8888 server 與 cloudflared 隧道全部照用，也**不需要 git init**；已查證手機 App 顯示的 `longxia7haodeMac-mini` 就是本機、8888（8/3 起）與 cloudflared（8/5 起）常駐中、隧道回 200、`pmset` 為 `sleep 0`／`disksleep 0` 不會睡著斷線。**手機新開 session 時 cwd 要指到 `/Users/longxia7hao/Heroes_Fate`**，否則專案層 `CLAUDE.md` 不會自動載入（`~/.claude/CLAUDE.md` 的全域規則仍會載入）。另經睿哥同意，已把 `/Users/longxia7hao/Heroes_Fate` 補進 `~/.claude/CLAUDE.md` 的「已知採用 PROJECT_NOTES 機制的專案」清單。**注意：不要同時用電腦端與手機端 session 改同一個檔案。**
- 2026-08-13｜Claude Code｜**v1.8 命運審判補上最後一擊／首幀圖全面重製／弓箭手配對修正／提示位置**：①**「最後一擊動畫又沒出現」的真正根因（v1.7 那次診斷是錯的）**：睿哥的錄影 `IMG_2381.MP4` 逐格看下來，結果頁寫的是**「☠️ 命運審判」**，而流程是 怒吼→命運一擊(3→1)→揭曉→**直接進結果頁，完全沒有 final**。回頭讀 `presentBossRaid()` 才發現 `if (isDoom) { … showResultBoss(); return; }` 這段**排在 ACT 5.5 的 `playFinalBlow()` 之前** —— 也就是**命運審判模式從頭到尾根本沒播過 final**，跟檔案大小、網速都無關。修正：把 ACT 5.5 整段搬到 doom 的 early return 之前（`// === ACT 5.5: 「打敗魔王」專屬影片。命運審判也要播（先前誤在此之前就 return）===`）。v1.7 做的 CRF 33 重壓與 8000ms 等待仍然對 4G 有幫助，但不是這個 bug 的原因。②**「龍騎士跟聖騎士會先出現舊的角色圖」**：切入層在影片載入前顯示的是 poster 首幀，而 poster 從 v1.5／v1.6 換片後**從來沒重新產生過**，且網址沒有版本參數，手機永遠吃舊圖。已由現行影片**全數重製 43 張 poster**（attack／victory／final 各 14 ＋ boss/arrival，共 1.3MB），並新增 `ART_VERSION = "2"` 掛在立繪、選角頭像與所有 poster 網址上；另加 `prefetchPosters(kind, ids)`，開場就把 attack／final 的 poster 預抓進 `posterWarmRefs`，解決錄影中看到的 dark_mage 全黑首幀。改前備份：`/private/tmp/heroes_fate_pre_poster/`。③**聖騎士選角大頭照沒換到**：同上，`heroThumb()`／`heroImg()` 原本**完全沒有版本參數**，檔案其實已經是新的、只是手機快取住；補上 `?v=${ART_VERSION}` 後解決（index.html 內嵌的 `knight.png` 也一起補）。④**兩隻弓箭手 final／victory 配對顛倒**：做了 wait／attack／final／victory 四類的造型比對表，發現「紅眼暗色弓手」與「黑髮精靈弓手」兩種造型被錯split在 ranger 與 orc_archer 之間。已互換兩者的 `final` 與 `victory` 影片＋poster；結果 orc_archer 四類一致，ranger 的 attack／final／victory 一致（它的 wait 片是棕髮人類，沒有對應的戰鬥素材可配）。改前備份：`/private/tmp/heroes_fate_pre_archerswap/`。⑤**「點一下跳下一位」提示擋到角色名字**：`.cut-tap-hint` 由畫面中下移到**螢幕最左下角**（`bottom: calc(0.55rem + env(safe-area-inset-bottom))`、`left: max(0.7rem, env(safe-area-inset-left))`、字級 0.62rem、opacity 0.9），已在名牌與隊伍列之下。⑥**「角色攻擊動畫不會出現」的兩個手機端可能原因一併處理**：(a) 全螢幕可點層用 `pointerdown` 會被殘留觸控／滑動誤觸發，改為 `click` 並把防誤觸從 420ms 放寬到 **600ms**；(b) MEDIA_VERSION 12 讓手機重抓全部影片，而攻擊段只等 1.6–2.2s 就跳過，改為**第一支等 6000ms、其後 4500ms**，逾時也讓 poster 停 1200ms 再走而不是整段消失。⑦快取：revamp.css?v=20、videoPlayer.js?v=22、victoryFilm.js?v=12、game.js?v=48。⑧**實測（360×640，2 人 聖騎士＋龍騎士，命運審判／命運卡抽到加倍判決）**：act 序列 `gather→arrival→clash→attack(attack/paladin.mp4，有 can-tap)→attack(attack/dragon_knight.mp4，有 can-tap)→fate(命運一擊 3→1)→reveal→attack[FINAL: final/paladin.mp4，poster=final/paladin.jpg?v=2、1.3×、無 can-tap]→審判降下→結果頁`，結果頁「☠️ 命運審判／玩家 1／聖騎士」單一勝利者、懲罰鈕為「抽懲罰任務 ×2」，**console error 0**；截圖確認提示已在左下、龍騎士與聖騎士的切入首幀都是新造型。⑨`js/rng.js` 本次**未修改**，SHA-256 為 `9ab55a96f19f162c1380a06cc7b3f2d496fb088aa393d570afeef7ef662f021a`（此值自 v1.6 移除「雙重命運」後即為新基準，抽籤仍是均勻洗牌、seed-first）。**尚未在睿哥手機實機確認**：⑥的攻擊段等待放寬是否完全解決「攻擊動畫不出現」。
- 2026-08-12｜Claude Code｜**v1.7 final 片穩定顯示＋攻擊段可點擊跳過**：①**「有些角色最後一擊動畫沒顯示」的根因**：`playFinalBlow()` 只等 `waitMediaReady(video, 2600)`，而 final 片是 10s 級（原 1.8–3.95MB），4G 下大檔在 2.6 秒內載不完就 `return false` 直接整段跳過 —— 檔案越大越容易中（archmage 3.95MB、monk 3.82MB、dark_fighter 3.62MB）。三管齊下修正：**(a)** 14 支 final 全部由 CRF 30 重壓為 CRF 33（畫質在 360 寬手機看不出差別，已用 archmage 做 CRF 30/32/34 與 608 寬比對），總量 44MB→31MB，單支最大 3.95→2.37MB；`mobile/` 69MB→55MB。**(b)** 等待由 2600ms 放寬到 8000ms —— 等待期間畫面已是同比例的 final poster 首幀，不會空著；真的逾時也改為讓首幀停 1600ms 再走，不像以前整段憑空消失。**(c)** 命運淘汰模式補上 `prefetchFinalClip()`（原本只有魔王討伐有預抓）。②**攻擊輪播可點一下跳下一位**（睿哥要求：想看的角色就不點，讓它自然播完）：新增 `waitClipEndOrTap(video, tapTarget, timeoutMs)`，攻擊段改用它；`.hero-cut.show` 加 `pointer-events:auto`（「略過動畫」按鈕 z-index 30 仍在其上，整場略過不受影響），新增 `.cut-tap-hint`「點一下跳下一位」提示，只在 `.can-tap`（攻擊輪播）時淡入，final 段會移除該 class 不可點。**防誤觸**：影片開始後 420ms 內的點擊視為手指殘留而忽略，避免一次連跳好幾位。③快取：MEDIA_VERSION 11→12、revamp.css?v=18、videoPlayer.js?v=21、game.js?v=45。實測（3 人，命運卡抽到逆命轉為審判）：56ms 的點擊被忽略、騎士完整播完 3.37s；826ms 的點擊生效、大魔導師只播 0.7s 就跳走；沒點的僧侶自然播完 2.5s；14 支 final 全部 canplay 且解碼通過，無 console error。改前備份：`/private/tmp/heroes_fate_pre_v17/`。
- 2026-08-12｜Claude Code｜**v1.6 五項回報修正（頭像／CapCut／龍騎士舊圖／魔王降臨／音效開關／雙重命運）**：①**選角頭像漏更新**：上一版只處理影片，沒處理「選擇角色縮圖」夾。比對後只有 2 張是新的（`IMG_2325`=paladin、`IMG_2202`=dragon_knight，其餘 12 張未變），已重裁 240×322 覆蓋 `assets/heroes/portraits/`。②**龍騎士舊圖檔**：`assets/heroes/dragon_knight.png`（Codex ImageGen 的紅髮龍鎧）與新素材造型不符（新造型是灰髮龍角持龍砲），改由新的 wait 影片抽幀重製 512×512 立繪。③**CapCut 浮水印與片尾**：來源 5 支 `copy_*` 檔案有右上角浮水印，其中 `attack/dragon_knight`（96AFB8AC）末段 1.8s 還是 CapCut 黑幕片尾。全部以 `crop=iw*0.91:ih*0.89:0:ih*0.11` 裁掉右上角後重壓；dragon_knight 攻擊片另外 `-t 3.25` 砍掉黑幕。受影響 poster 一併重製。attack 夾只有這 1 支是 copy_ 來源，其餘 13 支 Sora 原生無浮水印。④**魔王降臨影片接上**：`角色圖/魔王降臨` 的 15s 原片裁前 7 秒（遠處雷擊→降落→走近→特寫）轉 720 寬 CRF 29 放 `assets/videos/mobile/boss/arrival.mp4`（1.31MB），ACT 2 改以 `playStageClip()` 全螢幕播放並掛 poster；播放失敗才退回原本的立繪 CSS 動畫。`playStageClip` 新增 `opts.poster`。⑤**首頁音效開關沒作用**：根因是開關判斷用 `status.ready`（需要 `currentVoice` 正在播放）。音樂還在載入、或被全域手勢解鎖搶先時，判斷永遠落在「開啟」分支，按了像沒反應。改為以模組內 `audioUserActivated`（本次載入是否已由使用者親手啟用過）判斷；實測連點四次為 開→關→開→關，圖示同步 🔊/🔇。⑥**移除「雙重命運」卡**：睿哥指出只該有一位勝利者，`FATE_CARDS` 刪除該卡（剩風平浪靜／逆命／加倍判決／命運仁慈），5000 次抽樣單局最多中選人數為 1。**因此 `js/rng.js` 雜湊改變**：`6944df46…` → `9ab55a96f19f162c…`，屬睿哥明確要求的規則變更，抽籤本身仍是均勻洗牌、seed-first。⑦快取：MEDIA_VERSION 10→11（強制手機重載所有影片）、rng.js?v=7、videoPlayer.js?v=20、game.js?v=42。360×640 實測 act 序列 `gather→arrival(boss/arrival.mp4)→clash→attack(dragon_knight)→attack(paladin)→fate→命運一擊→reveal→attack[FINAL:final/paladin]→victory→結果頁（單一勝利者 玩家2）`，攻擊切入無 CapCut 痕跡，無 console error；manifest 70 條引用零缺檔。
- 2026-08-12｜Claude Code｜**v1.5 素材夾改名＋打敗魔王專屬影片（final）＋等待片全面更新**：①**Drive 資料夾已改名**：`英雄旅途` → `英雄命運抽（heroes fate)`（同一個 folder id `1cXaJ5gi8JoRYglV8aYseM8vKYS0m1IQP`），筆記內所有素材來源路徑已更新；`角色圖/` 下新增兩個子夾「最終戰勝者對戰動畫」（14 支）與「魔王降臨」（1 支）。②**逐夾與筆記 UUID 比對，找出實際變動**：等待選擇 **14 支全部換新**、確定選擇 5 支新（orc_archer `08fdc282`、amazon `1c6bf49d`、archmage `454ae66a`、dragon_knight `4d94620c`、paladin `8ec5ac2f`）、攻擊魔王 2 支新（paladin `c9d7cb54`、dragon_knight `96AFB8AC`）、角色勝利 2 支新（dragon_knight `3a29b57a`、paladin `aad00432`）。全部抽幀視覺辨識後才對應角色；新等待片對照：`032c4ff7`=dark_fighter、`3845f6fd`=axeman、`3a641aa1`=orc_archer、`45996539`=archmage、`52b4cea5`=dark_elf、`6193d6e9`=amazon、`7ff58649`=ranger、`84ac1564`=assassin、`b836f708`=princess、`bc887302`=dark_mage、`c0c6f75e`=monk、`e8ecaf38`=knight、`ea3c5963`=dragon_knight、`f9a4b979`=paladin。③**新增 final 類別**：14 支「打敗魔王」影片轉為 720 寬 CRF 30（10.04–16.08s，共 44MB）放 `assets/videos/mobile/final/`，另抽 0.6s 首幀做 poster（14 張 456KB）；manifest 每角新增 `final` 欄位（共 70 條引用零缺檔），`videoPlayer.videoUrl` 支援 `final`（無片則回 null，不亂退回其他類別）。final 對照：`05fc7cf0`=dark_fighter、`0c29cc53`=archmage、`274d5b0b`=princess、`34af6117`=ranger、`44f58826`=assassin、`66846615`=paladin、`716f8a0d`=monk、`9b4aee01`=axeman、`a9c35676`=amazon、`ea9eaf37`=dark_elf、`01970212`=orc_archer、`34A64C62`=knight、`5779FAF9`=dark_mage、`EA47390D`=dragon_knight。④**流程改為睿哥指定順序**：每位英雄攻擊切入連播完 → 魔王怒吼 → **命運一擊全員按住 3 秒**（`holdMs` 由 1800ms 改 3000ms）→ **勝者的打敗魔王專屬影片**（新 `playFinalBlow()`，沿用切入層、1.3× 播放、等 `ended`、上限 14s、掛 final poster 防黑屏）→ 角色勝利短片 → 結果頁。命運淘汰模式同樣接上 final。魔王戰開場即以 `prefetchFinalClip()` 預抓勝者那一支（2–4MB），避免臨場等下載。⑤快取：manifest v13、videoPlayer.js?v=19、game.js?v=38。360×640 兩人實測 act 序列為 `gather→arrival→clash→attack(knight)→attack(dragon_knight)→fate→reveal→attack[FINAL:final/dragon_knight]→victory→結果頁`，畫面標籤顯示 `FINAL／終結一擊`，無 console error；37 支新轉檔全部完整解碼。`js/rng.js` 未修改，SHA-256 保持 `6944df46910819dfa324081462a041d38674f50c4494db65b76c3d150421d65c`。改前備份：`/private/tmp/heroes_fate_pre_v15/`。**待處理**：「魔王降臨」那支 15s 影片尚未使用（睿哥未指示用途）。
- 2026-08-11｜Codex｜**v1.4 使用者開始伴奏＋14 角選角 BGM 精華**：找到使用者 Google Drive `英雄命運抽（heroes fate)/伴奏/` 的 15 個有效來源（開始選單 1 首＋角色 14 首；資料夾內隨機無副檔名檔案為 HTML，未誤用），依角色語意一對一映射：皇家騎士→`knight`、聖騎士→`paladin`、精靈弓箭手→`ranger`、幽靈射手→`orc_archer`、斧頭男→`axeman`、女狂戰士→`amazon`、賽亞人→`dark_fighter`、暗影刺客→`assassin`、大法師→`archmage`、魔靈召喚師→`dark_mage`、巫毒薩滿→`dark_elf`、憎侶→`monk`、皇家公主→`princess`、龍騎士→`dragon_knight`。每首約 30.8s 原曲以能量窗分析擷取 12s 最具辨識度段落，輸出 `home_custom.mp3` 與 `assets/audio/heroes/bgm/{id}.mp3`，全部 44.1kHz stereo 96kbps、約 -14 LUFS、TP≤-1.5dB、段首 0.18s／段尾 0.28s 淡化；新增可重建的 `tools/extract_character_bgm.py`，來源原檔完全保留。`audioDirector.js` 新增 14 角 music map、`desiredMusic`、`playHeroMusic()`／`clearHeroMusic()` 與快速切角 request-token 防競態；點角色立即交叉淡入其 BGM，確定動畫期間延續，換到空白玩家回通用選角曲，離開選角恢復 mode／battle／home 場景曲；iPhone 首次解鎖與角色點擊同時發生時也會以最後要求的角色曲為準。現有 attack／victory 音效不覆蓋，`applyPick()`、confirm 1×／4200ms、`seedRun()` 皆未動。驗證：15 支新 MP3 全部完整解碼，14 角輸出零缺檔；快速 knight→princess 切歌狀態最終為 `hero:princess`，清空為 `pick`、進戰鬥為 `battle`；本機與公開隧道的首頁、v6/v37 腳本及 15 支新曲全數 HTTP 200／`audio/mpeg`。本環境沒有可連接圖形瀏覽器，故未做實際點擊聽感截圖回歸。指定 8888 server 仍在專案根目錄執行；`js/rng.js` 未修改，SHA-256 保持 `6944df46910819dfa324081462a041d38674f50c4494db65b76c3d150421d65c`。公開入口改用 `?build=character-bgm-20260811`。
- 2026-08-11｜Codex｜**v1.3 第 14 英雄「龍騎士」完整整合**：依 GDD 原始角色方向新增 `dragon_knight／龍騎士`，武器「龍焰長槍」、主色青焰×黑紅金龍鎧。以 Codex 內建 ImageGen 建立單一角色基準圖，再以 identity-preserve 編修產生確定、攻擊、勝利三個一致造型關鍵畫面；輸出 512×512 選角圖、240×322 portrait、wait／confirm／attack／victory 四支現行手機 MP4、attack／victory poster 與兩支專屬 MP3，全數走 `assets/videos/mobile/` 主路徑，未使用舊 `assets/anim` sheet。四支影片均為 H.264 High 3.1、720×1268、24fps、73 幀、3.041667s、yuv420p、BT.709 limited、faststart、無音軌，完整解碼通過；兩支 MP3 為 32kHz stereo 96kbps、2.34s、約 -18.3 LUFS。`data.js` 與 manifest 已加入新角色，manifest v12、data.js?v=2、videoPlayer.js?v=18、audioDirector.js?v=5、game.js?v=36；14 張卡形成完整 7×2，玩家上限仍刻意維持 13，讓滿場仍有一位候補。點角色只預覽，只有按「決定」才由 `applyPick()` 寫入鎖定；confirm 仍為 1×、4200ms 上限。驗證：14 角資料 ID 唯一、56 條影片引用零缺檔、10 個新資產本機與公開隧道皆 HTTP 200 且 MIME 正確、JS 語法／JSON／完整媒體解碼通過；指定 `python3 -m http.server 8888 --bind 0.0.0.0` 正在專案根目錄服務。公平抽樣 12,000 次龍騎士 5,962／騎士 6,038；`js/rng.js` 未修改，SHA-256 保持 `6944df46910819dfa324081462a041d38674f50c4494db65b76c3d150421d65c`。公開入口改用 `?build=hero14-dragon-knight-20260811`。
- 2026-08-11｜Codex｜**v1.2.3 音效開關圖示同步**：首頁音效按鈕與設定視窗的音效項目加入同一狀態圖示，開啟顯示 🔊、關閉顯示 🔇；首頁按鈕現在是真正的開關，完整音效已啟動時點一下可關閉，再點一次會在同一觸控手勢重新解鎖並開啟。按鈕文案同步顯示「點此關閉／點此開啟」，加入 `aria-pressed`，設定 checkbox、首頁圖示、Web Audio 實際 enabled 狀態保持一致。驗證：狀態測試確認 on=`🔊/aria-pressed true`、off=`🔇/aria-pressed false`，公開 HTML 與 game.js?v=35 均載入新版且 HTTP 200；JS 語法通過，8888 指定 server 正常，`js/rng.js` SHA-256 未變。公開入口改用 `?build=audio-toggle-icon-20260811`。
- 2026-08-11｜Codex｜**v1.2.2 選角確定動畫恢復原速**：依最新指示修正加入音樂後「按決定」的確定片被加速問題；`js/videoPlayer.js` 的 confirm 專用倍率由 3.2× 恢復為 1×，`js/game.js` 硬上限由 1150ms 放寬為 4200ms，讓 13 支 3.00–3.04s Sora confirm 片都能以原始速度完整播放後才交棒下一位。wait 預覽仍為 1×，attack／victory 維持既有 1.3×，音樂／角色／魔王聲效不變；角色仍只有按「決定」才寫入鎖定。驗證：播放狀態測試得到 `waitRate=1 / confirmRate=1 / finalState=holding`（由 ended 收尾）；僧侶與半獸人確定片完整解碼；公開入口、v17/v34 腳本、兩支抽測影片與 BGM 均 HTTP 200；三支相關 JS 語法通過，8888 仍由指定命令在專案根目錄服務。手機快取：videoPlayer.js?v=17、game.js?v=34；公開入口改用 `?build=confirm-natural-20260811`。`js/rng.js` 未修改，SHA-256 保持 `6944df46910819dfa324081462a041d38674f50c4494db65b76c3d150421d65c`。
- 2026-08-11｜Codex｜**v1.2.1 iPhone 完整音效修復**：①針對「只有舊嘟嘟聲、完整音效沒有播放」定位兩個根因：Python `http.server` 對 MP3 Range 請求回完整 200 而非 206，iPhone Safari 的 HTMLAudio BGM 可能無法啟播；舊解鎖流程又在 `AudioContext.resume()`／音檔播放真正成功前就標記 unlocked／current music，失敗後不會重試。②重寫 `js/audioDirector.js`：BGM 不再使用 HTMLAudio，改為 fetch 完整 MP3 → Web Audio 解碼 → `AudioBufferSourceNode` 無縫循環與 Gain 交叉淡入淡出，從而不依賴 HTTP Range；解鎖只有在 context 確實 running 後才成功，加入同一手勢靜音 primer、失敗重試、舊 Safari callback／新版 Promise 解碼相容、visibility suspend/resume 與可查詢錯誤狀態。③首頁新增「點一下開啟完整音效」可見按鈕；成功會變綠色「完整音效已開啟」，失敗則顯示錯誤供回報。pointerdown／touchend／click／keydown 及所有前進操作都會再次嘗試解鎖；總開關與音量設定仍沿用。④移除 `game.js`／`victoryFilm.js` 最後的 oscillator 舊嘟嘟聲，所有提示都改走真正的 named MP3 cue；音檔 revision 提升至 `?v=3`。⑤驗證：390×844 且強制 user-gesture autoplay 的本機測試，點啟動後狀態為 `unlocked=true / context=running / music=home / engine=webaudio-buffer`，切選角自動換 pick BGM；公開網址同樣成功。公開兩人魔王討伐完整走到結果，角色攻擊／勝利、魔王出場怒吼／倒下均實際 fetch、HTTP failure=0、runtime error=0；選角仍只在按「決定」後鎖定，兩次交棒約 1.016s／1.16s（1150ms 硬上限＋輪詢誤差）。快取：revamp.css?v=17、videoPlayer.js?v=16、audioDirector.js?v=4、victoryFilm.js?v=10、game.js?v=33。`js/rng.js` 未修改，SHA-256 保持 `6944df46910819dfa324081462a041d38674f50c4494db65b76c3d150421d65c`。
- 2026-08-11｜Codex｜**v1.2 場景配樂／角色原音／魔王聲效／華麗轉場**：①新增 `js/audioDirector.js`：第一次觸控統一解鎖 Web Audio（通過 iOS/Android autoplay 規則），主選單／人數用 home、選角／模式用 pick、戰鬥用 battle 三首原創循環 BGM，切場景以 680ms 交叉淡入淡出；頁面隱藏會暫停，返回會恢復。遊戲設定新增總音量／背景音樂／角色與戰鬥音效三組滑桿及總開關，存於 `localStorage hf_audio_v1`，勝利 fanfare 期間 BGM 自動降低約 6dB。②從 Google Drive 13 支攻擊與 13 支勝利 Sora 原片抽出獨立音軌，統一 loudness、預先烘焙 1.3× 節奏，於影片真正開始時同步播放；另有武器類型後備音效，總音訊包 44 檔／1.9MB。③加入 UI 點擊／鎖定／轉頁、命運卡、命運一擊、排序／配對／分隊、煙霧、揭曉、懲罰等 named cues；魔王降臨會先出場 stinger 再怒吼，反擊再次怒吼，倒下有獨立 defeat cue，勝利片使用角色原音＋fanfare。略過會立即停止 battle／hero 音效。④轉場加入短暫金紫色命運法陣、符印與四角框；模式卡 stagger 進場、結果頁 badge→角色→文案→按鈕逐層落地；命運卡翻面與每位攻擊斬線改為每次重新觸發；魔王登場時間與 JS 節拍同步、戰鬥鏡頭 transition 縮短、Sora 全畫面切入時暫停背後常駐動畫以降低手機 GPU 負擔，並完整尊重 reduced-motion。⑤選角鎖定依先前指示恢復為 `CONFIRM_RATE=3.2`、硬上限 1150ms；390×844 實測兩次交棒 1.002s／1.007s，點角色仍只進預覽且 party done=0，按「決定」後才 done=1。⑥驗證：44 支 MP3 全數可解碼；`python3 -m http.server 8888 --bind 0.0.0.0` 正在專案根目錄服務；390×844 兩人魔王討伐全流程 10.81s 到結果頁、無水平溢出、當次 HTTP failure=0；公開網址首頁與 `boss_roar.mp3` 皆 HTTP 200；同 Wi‑Fi 位址更新為 `192.168.68.52:8888`。快取：revamp.css?v=16、videoPlayer.js?v=16、audioDirector.js?v=3、victoryFilm.js?v=9、game.js?v=32。`js/rng.js` 未修改，SHA-256 保持 `6944df46910819dfa324081462a041d38674f50c4494db65b76c3d150421d65c`。
- 2026-08-05｜Claude Code｜**v1.1 遊戲性第二輪（命運審判／命運卡／今晚戰績／命運卷軸）**：①**命運審判（doom）**：新模式卡，抽的是「最倒楣的那個」；`rng.js` 的 boss 與 doom 共用同一段洗牌邏輯（`chosen` 陣列），演出沿用魔王討伐但文案改為「全員到齊——審判即將降下」「魔王降臨——它要挑一個人帶走」「命運降罪——…」，且**不播勝利短片**（語意不符），改以震動＋煙霧的「審判降下……」收尾後直接進結果頁；結果頁 badge 為「☠️ 命運審判」。②**命運卡**：每局開場翻一張（`#fate-card-layer`），5 張＝風平浪靜／雙重命運（抽兩人）／逆命（boss↔doom 互換）／加倍判決（懲罰抽 2 張不重複）／命運仁慈（本局免罰，按鈕變「本局免罰」並停用）。卡片同樣在 `seedRun()` 內以同一條 seed 抽出（seed-first），對所有玩家一視同仁；6000 次抽樣驗證 5 人被抽中次數仍均等（1419–1448）。可在設定關閉。③**今晚戰績**：`localStorage` `hf_night_stats_v1` 累積每局（模式、卡片、中選者），結果頁「今晚戰績」顯示局數、封號（命運寵兒／今晚最衰／隱形人）、玩家榜與熱門角色，可一鍵清除。命運寵兒只計非審判中選，避免與最衰矛盾。④**命運卷軸**：結果頁按鈕以 canvas 720×1080 繪出可截圖分享的圖卡（模式、勝者立繪或勝利片首幀、玩家與角色、命運卡、判決內容、日期），提供下載 PNG。快取：rng.js?v=6、revamp.css?v=15、game.js?v=30。360×640 實測：審判流程與文案正確、命運仁慈使懲罰鈕停用、雙重命運抽出「玩家2＋玩家3」、加倍判決抽出兩張不重複懲罰、戰績封號口徑正確、卷軸圖卡正常產出、設定存檔生效、無 console error。驗證用的固定卡片測試碼已還原。
- 2026-08-04｜Claude Code｜**v1.0 遊戲性擴充（命運一擊／命運淘汰／命運分隊／懲罰任務）**：①**命運一擊**：`playFateStrike()` 在魔王討伐與命運淘汰的揭曉前插入全螢幕蓄力層（`#strike-gate`），全員按住約 1.8s 才揭曉，放開會中斷重來，沒人按 9s 自動放行；純儀式，結果仍由 `seedRun()` 事先決定。設定可關閉（`opts.strike`）。②**命運淘汰（survival）**：`rng.js` 新增模式，洗牌順序即淘汰順序、最後一位為生還者；`presentSurvival()` 每輪一位出局並即時更新 HUD，最後接勝利短片；結果頁列出淘汰順序（由後往前）。6000 次抽樣分布均勻（5 人各約 20%）。③**命運分隊（teams）**：`rng.js` 洗牌後輪流發牌，人數差最多 1；模式卡上直接選 2／3／4 隊，隊數大於人數會自動收斂；結果頁以紅／藍／綠／金四色分區列出。④**懲罰任務**：結果頁「抽懲罰任務」按鈕，題庫預設 10 則、可在設定頁自由編輯（存 localStorage `hf_doom_list_v1`），抽選用 `crypto.getRandomValues`，與勝負 RNG 完全分離。快取：rng.js?v=2、revamp.css?v=14、game.js?v=26。360×640 實測：分隊 3 隊結果正確、淘汰賽 HUD 逐位減少並正確產出生還者與淘汰順序、命運一擊按住蓄力至完成後才揭曉、懲罰任務可抽出並顯示。
- 2026-08-04｜Claude Code｜**v0.99.1 poster 真的顯示＋選角預抓 13 支待選片**：v0.99 加了 poster 卻仍全黑，根因是 `.hero-cut-video` 的 CSS 是 `opacity:0`、只有 `.hero-cut.is-playing` 才變 1，影片元素本身不可見，poster 自然也看不到；`victoryFilm` 同理（`video.hidden` 要等 canplay 才解除）。修法：`.hero-cut-video` 改為恆 `opacity:1`（移除 `is-playing` 的 opacity 規則），`victoryFilm` 設完 poster／src 後立刻 `video.hidden = false`。另外針對「選角卡卡的」新增 `prefetchWaitClips()`：在人數頁（`show("count")` → `warmPickAssets`）就用隱藏 `<video preload="auto">` **依序**把 13 支待選片抓進快取（一支就緒才抓下一支，避免同時開 13 條連線），之後點任何角色都直接從快取播。快取：revamp.css?v=12、victoryFilm.js?v=8、game.js?v=24。實測：13 支 wait 於人數頁全部預抓完成；連點三個角色的切換耗時 103ms／33ms／32ms；`.hero-cut-video` 在 `show` 未 `is-playing` 時 computed opacity 已是 1；魔王戰攻擊切入直接顯示影片、無 console error。
- 2026-08-04｜Claude Code｜**v0.99 全面提速＋消除黑畫面**：睿哥回報卡頓、有些動畫跑不出來還黑屏。三管齊下：①**影片減重**：52 支 mobile 影片全部由 CRF 24 重壓為 CRF 29（維持 720 寬 / 24fps / faststart / 無聲，`preset slow`），`mobile/` 47MB→25MB（-47%）；wait 6.9→3.5MB、confirm 12→6.2MB、attack 15→7.7MB、victory 14→7.4MB。以騎士攻擊片做 720/608 × CRF 27/29/31 比對，360 寬手機上 CRF 29 與原檔幾乎無差別。`ref_battle_mobile.mp4` 重壓後反而變大，維持原檔。②**首幀 poster 消除黑屏**：為 attack／victory 各 13 支抽 0.5s 幀輸出 400 寬 JPEG 放 `assets/videos/poster/{attack,victory}/`（共 828KB），`playAttackSequence` 與 `victoryFilm` 在設 src 前先給 `video.poster`，載入中顯示同比例首幀而非黑底。③**節奏加快**：新增 `CLIP_RATE = 1.3`（game.js／videoPlayer.js 各一份），確定選擇、攻擊切入、勝利短片皆以 1.3× 播放（仍完整播完不截斷，動作自然）；`PICK_CONFIRM_MAX_MS` 3600→3000、攻擊切入逾時 4200→3400；演出等待 gather 900→560、arrival 820→520、參考戰鬥片 2350→1500、怒吼 1200→760、揭曉 900→620、排序／配對段 700→480、850/420→560/280、500→340；勝利字幕節拍改除以 1.3。快取：MEDIA_VERSION 9→10、videoPlayer.js?v=15、victoryFilm.js?v=7、game.js?v=23。360×640 實測：攻擊片 `rate=1.3`、poster 正確掛上、兩支攻擊間隔由約 3.5s 縮為 2.5s、勝利片 `rate=1.3` 且 poster 生效、manifest 52 個引用無缺檔、無 console error。改前備份：`/private/tmp/heroes_fate_pre_v099/`。
- 2026-08-04｜Claude Code｜**v0.98.8 攻擊段改為連續播放**：睿哥要求打魔王時只把各角色的攻擊動畫接續播完，不要中間插「打到魔王」的演出（割裂感）。原本 ACT 3B 是每位一輪：`playHeroCut` 開全螢幕切入 → 播完 → `stopHeroCut` 收掉 → 回舞台做 `stage.shake`／`boss.hurt`／`impactFx`／`wait(80)` → 再開下一位。改為新的 `playAttackSequence(players, sources)`：切入層只開一次（`show`），迴圈內只換 `video.src` 與角色名／編號／HUD／橫幅，播完 `ended` 直接接下一支，全部結束才 `stopHeroCut`；移除 shake／hurt／impactFx／間隔等待（保留每位切換時的 beep）。原 `playHeroCut` 已無呼叫者，一併移除（-54 行）。快取：game.js?v=22。360×640 三人實測：`hero-cut` 的 `show` 從 35.5s 一路維持到 46.3s，期間 src 依序 knight → monk → princess，中途沒有回到舞台，無 console error。
- 2026-08-04｜Claude Code｜**v0.98.7 攻擊切入徹底移除方形立繪＋預載攻擊片**：睿哥回報攻擊切入仍會先變比例。根因是 v0.98.5 保留的 `is-fallback` 後備：手機 4G 下 attack 影片（約 1MB）在 760ms 內載不完 → `played=false` → 顯示 512×512 方形立繪（上下留黑），影片追上後才變直式，看起來就是「先改變比例再正常」。修法：①`.hero-cut-poster` 改 `display:none !important`（永不顯示），移除 `is-fallback` 的視覺效果與 `playHeroCut` 內的加 class。②`waitMediaReady` 等待由 480/760ms 放寬為 1400/2200ms。③新增 `prefetchAttackClips()`：`resolveAttackSources` 取得對照後立刻用隱藏 `<video preload="auto">` 把所有參戰者的攻擊片抓下來，`cleanupStageMedia({release:true})` 時由 `clearAttackPrefetch()` 釋放。快取：revamp.css?v=11、game.js?v=21。360×640 兩人實測：`hero-cut-poster` 全程 `display:none`，兩段切入都在 `show` 後約 79ms 直接進 `is-playing`，network 顯示兩支 attack 影片在開場就以 206 串流預抓，無 console error。
- 2026-08-04｜Claude Code｜**v0.98.6 選角與結果頁都不再先顯示立繪**：①選角預覽：`videoPlayer.play()` 開頭原本會 `primeStill(id)` 先鋪方形立繪，導致「先寬扁再變正常」；移除該呼叫（`showStill` 仍保留給無片／播放失敗的後備）。同時 `updateModelPreview` 把 `pedestal.classList.remove("is-empty")` 移到 `await vp.play()` 之後，讓影片就緒前維持空舞台，就緒才揭開。②結果頁勝者頭像改播角色勝利動畫：`index.html` 的 `.result-model` 加 `#result-video`（保留 `#result-img` 當後備），新增 `setResultPortrait(heroId)`／`stopResultPortrait()`，三種模式的結果頁都改用；`show()` 離開結果頁時停止並釋放影片。CSS 加 `#screen-result .result-model.has-video` 切換 video／img 顯示，兩者共用 `contain`＋`padding: 4% 4% 9%`。踩坑：一開始沿用 `waitMediaReady()` 判斷就緒，但它內部會檢查 `state.skip`，玩家按過「略過動畫」後 `state.skip` 仍為 true 而直接失敗，導致永遠退回立繪；結果頁改用不綁 skip 的獨立 canplay 等待。快取：revamp.css?v=10、videoPlayer.js?v=14、game.js?v=20。360×640 實測：選角點角色時 `.vp-still` 全程 `hidden`、直接播動畫；結果頁 `has-video=true`、顯示 `victory/knight.mp4`（720×1268）、立繪 opacity 0；回主選單後影片 src 已釋放，無 console error。
- 2026-08-04｜Claude Code｜**v0.98.5 攻擊切入不再先閃方形立繪**：睿哥回報「每次攻擊都先壓縮比例才回正常」。根因是攻擊切入在影片就緒前會先鋪 `#hero-cut-poster`＝`assets/heroes/{id}.png`（512×512 方形），與其後的 720×1268 直式影片比例不同，所以看起來先扁再正常（v0.98.3 把它從 cover 改 contain 只是換一種扁法）。改為 `.hero-cut-poster` 預設 `opacity:0`，只有影片載不動（`played=false`）時 `playHeroCut` 才加 `.is-fallback` 讓它現身當後備；`stopHeroCut` 一併清除該 class。快取：revamp.css?v=9、game.js?v=16。360×640 兩人實測：poster 全程 `opacity:0`，class 直接由 `hero-cut` → `hero-cut show is-playing`，兩位角色的攻擊切入都是直接播影片、比例正確，無 console error。
- 2026-08-04｜Claude Code｜**v0.98.4 未選角時舞台留空**：`updateModelPreview` 原本在沒有選擇時會 fallback 到第一個未被選的角色（所以一進選角就直接顯示騎士）；改為沒有 `selectedHeroId` 也沒有該玩家已鎖定角色時，標題顯示「選擇角色」、武器與台詞清空、`.model-pedestal` 加 `is-empty`、暫停預覽影片。空舞台圖取自睿哥示意圖中央區（`crop=278:465:206:419` → 556×930），存 `assets/deco/empty_stage.jpg`（37KB），由 `#screen-pick .model-pedestal.is-empty` 以 `background-size: auto 100%` 置中鋪上，同時隱藏 `.sprite-stage`。鎖定一位後切到下一位玩家會回到空舞台；按「上一位」回到已鎖定的玩家仍顯示其角色。快取：revamp.css?v=8、game.js?v=15。360×640 實測四種狀態（初始空／預覽公主／鎖定後玩家 2 空／上一位回玩家 1 顯示公主）皆正確，無 console error。
- 2026-08-04｜Claude Code｜**v0.98.3 移除勝者重複攻擊＋修攻擊切入前的壓扁**：①`presentBossRaid` 的 ACT 5 揭曉後那段 `playHeroCut`（勝者再攻擊一次）整段移除，全員輪播完 → 揭曉 → 直接進勝利片。②攻擊切入的暫代立繪 `.hero-cut-poster` 原本 `object-fit: cover` + `transform: scale(1.12)`，把 512×512 方形立繪撐滿直式全螢幕，看起來被壓扁，影片就緒後才恢復；改為 `object-fit: contain`＋`transform: none`。快取：revamp.css?v=7、game.js?v=14。360×640 兩人實測 act 序列為 gather→arrival→clash→attack(amazon)→attack(ranger)→fate→reveal→victory，reveal 後不再出現攻擊影片，poster 全程 `contain`，無 console error。
- 2026-08-04｜Claude Code｜**v0.98.2 選角動畫框加石雕龍側柱**：依睿哥示意圖，在選角動畫框左右空白處加石雕龍。素材取自睿哥提供的合成截圖（688×1504），裁 `crop=190:478:14:421` 後放大 2× 並 `unsharp` 銳化，輸出 `assets/deco/stone_dragon_l.jpg`（380×956，44KB）與水平鏡像的 `_r.jpg`；`#screen-pick .model-pedestal::before`（z-index 1，在影片層之下）以 `background-position: left/right center`＋`background-size: auto 100%` 貼上，兩隻龍面向中央。同時把 `#screen-pick :is(.vp-video,.vp-still)` 的紫色底改為 `transparent`，否則影片元素會蓋住石雕。快取 revamp.css?v=6。402×660（接近實機含 Safari UI）效果最接近示意圖；360×640、375×812 亦正常，無 console error。註：框越高影片越寬、左右空白越窄，石雕露出的比例會跟著變小。
- 2026-08-04｜Claude Code｜**v0.98.1 結果頁勝者圖等比例＋兩處呆等修正**：①結果頁 `#screen-result .result-model` 的勝者圖原本 `object-fit: cover` + `object-position: center 42%`，方形立繪被裁切又被盾形圓弧切掉下緣，看起來像壓扁；改為 `object-fit: contain`＋`padding: 4% 4% 9%`＋`box-sizing:border-box`，並給 `.result-model` 補 `flex:0 0 auto` 與 `aspect-ratio:1/1` 防止父層壓縮高度。375×812 實測框 193×193 正方、人物完整不裁切。②修兩處「影片載不動就呆等」：`playHeroCut` 未成功播放時由空等 3.2s 改為最多 0.9s；`videoPlayer.playOnce` 在 `readyState<2` 時直接 `finish()`，不再等滿 3600ms 硬上限。快取：revamp.css?v=5、videoPlayer.js?v=13、game.js?v=13。2 人魔王討伐實機驗證結果頁正常、無 console error。
- 2026-08-03｜Claude Code｜**v0.98 動畫全長播放＋勝利片補齊**：①**確定選擇動畫不再加速**：`videoPlayer.playOnce` 移除 `CONFIRM_TARGET_MS`／`MAX_CONFIRM_RATE` 倍速計算，一律 `playbackRate=1` 播完整段；`PICK_CONFIRM_MAX_MS` 940→3600、`playOnce` 預設 maxMs 940→3600（改由 `ended` 收尾，硬上限只是保險）。②**魔王戰每位角色的攻擊動畫完整播完**：新增 `waitClipEnd(video, timeoutMs)`（等 `ended`／逾時／skip），`playHeroCut` 播放成功時改等它；`perCutMs` 由 `clamp(430..900, 4700/人數)` 改為固定 3200 且不再乘 `fast`，揭曉後勝者的重擊切入也由 820ms 改為同一值。360×640 實測三段攻擊各約 4.0s（3.04s 影片＋載入收尾）。③**修勝利片開頭被拉寬**：`victoryFilm` 原本先鋪 `assets/heroes/{id}.png`（512×512 方形，用 contain 會佔滿寬度看起來被拉寬）再切影片；改為開場不顯示 still，只有缺片或載入失敗才拿它當後備，實測勝利片一開始就是 720×1268 直式正常比例。④**匯入更新後的角色勝利動畫**：來源夾由 9 支增為 13 支，新增 4 支經抽幀辨識為 `02cdf1ec`=princess、`8777d01b`=ranger、`a35d2e9c`=dark_elf、`ba783ecd`=monk，轉 720 寬 mobile 規格放 `mobile/victory/`（ranger 覆蓋舊的專案內裁切版），manifest 全部 13 角 victory 補齊、`refs` 52 個無缺檔。快取：videoPlayer.js?v=12、victoryFilm.js?v=6、game.js?v=12、MANIFEST_VERSION 11、MEDIA_VERSION 9。實測 4 人魔王討伐全流程通過、略過後媒體釋放、無 console error；公開網址四支新勝利片皆 HTTP 200。改前備份：`/private/tmp/heroes_fate_pre_v098/`。註：兩支弓手勝利片造型不同（`38a9eef7` 紅眼深膚＝orc_archer、`8777d01b` 黑髮尖耳＝ranger），如需對調告知即可。
- 2026-08-03｜Claude Code｜**v0.97 縮圖選角版面＋攻擊魔王動畫**：①首頁移除「2～13 人／公平隨機／免輸入名字」chips 與「◇ 手機連線方式」摺疊區（`fillPhoneHint` 仍在但找不到節點即跳過）。②選角版面由 U 字左右 rail 改為「動畫框在上、縮圖選單在下」：`index.html` 移除 `#hero-rail-*`，改用單一 `#hero-grid`；`renderHeroGrid()` 改渲染單一網格（7 欄 × 2 列＝13 角），名稱在圖片下方。縮圖來源改為雲端「選擇角色縮圖」13 張頭像，裁 3:4（`crop=iw:iw*1.34:0:ih*0.015`）輸出 240×322 JPEG，放 `assets/heroes/portraits/`。踩坑：`<img width height>` 的 presentational hint 會蓋掉 `aspect-ratio`，導致卡片高 341px、動畫框被壓成 0；`.hero-card img` 補 `height:auto` 才正確；`css/game.css` 原本沒有版本參數，改為 `?v=2` 才吃得到新規則。③魔王戰英雄切入改用雲端「攻擊魔王動畫」：13 支轉 720 寬 mobile 規格放 `assets/videos/mobile/attack/`，`manifest.json` 每角新增 `attack` 欄位，`videoUrl` 支援 `attack`（缺片退 confirm→wait），`game.js` 的 `resolveConfirmSources` 更名 `resolveAttackSources` 並改取 attack。④勝利者維持用「角色勝利動畫」（`mobile/victory/`），無片者 fallback 順序改為 attack→confirm→wait。攻擊影片 UUID→角色：`1587a5f2`=ranger（造型與遊俠待選片不同，蒙面暗色射手，待睿哥確認）、`1f9496b7`=monk、`4fe31390`=orc_archer、`62e2a9ae`=princess、`73b0aded`=assassin、`79a2459a`=dark_mage、`85a35951`=knight、`98f11503`=paladin、`ac594548`=dark_elf、`b85537c0`=dark_fighter、`daef7dde`=archmage、`fabf11a7`=amazon、`fc11f013`=axeman。快取：game.css?v=2、revamp.css?v=4、videoPlayer.js?v=11、game.js?v=11、MANIFEST_VERSION 10。375×812 與 360×640 實測：首頁無多餘文字、13 縮圖 7×2 無水平溢出與文字截斷、選角預覽正確、4 人魔王討伐依序播 attack 影片、勝利播 `victory/knight.mp4`、略過後回結果頁且媒體釋放、無 console error。仍缺勝利片：`dark_elf`／`monk`／`princess`。改前備份：`/private/tmp/heroes_fate_pre_v097/`。
- 2026-08-03｜Claude Code｜**v0.96 依 Google Drive 分類重新載入影片**：來源＝`我的雲端硬碟/英雄命運抽（heroes fate)/角色圖/`（Drive folder `1EZhAFT1mTkOagNvm2iqkqDV3hIjAFfNd`，本機已同步）。以 md5 比對確認「角色確定選擇動畫」12 支、「角色勝利動畫」9 支與現行專案完全相同（victory 仍缺 `dark_elf`/`monk`/`princess`；`ranger` 勝利片為專案內裁切版，來源夾沒有）。**「角色等待選擇動畫」13 支全部是新片**，逐支抽幀視覺辨識對應角色後，轉為 720 寬 / 24fps / H.264 High / yuv420p / faststart / 無聲，覆蓋 `mobile/wait/{id}.mp4`；wait 目錄 26MB→7.0MiB，每支 3.041667s（原 15s）。UUID→角色對照：`0a5fe782`=dark_mage、`1815a8fe`=dark_elf、`22574e3b`=orc_archer、`65b38f1d`=ranger、`762608e9`=assassin、`76e4cf28`=archmage、`7ce3cf5e`=knight、`981ae2c8`=monk、`ca2ab338`=amazon、`eaaf94dd`=princess、`f3d356e0`=dark_fighter、`f42d9b2c`=axeman、`f4470026`=paladin。同時修正 v0.95.2 的錯置：確定選擇夾內的 `d0ce2bbf`（僧侶綠光施法）本來就是 **confirm**，已轉檔覆蓋 `mobile/confirm/monk.mp4`，manifest 的 monk.wait 改回 `mobile/wait/monk.mp4`，刪除 `monk_v2.mp4`。快取版本 MANIFEST_VERSION 8→9、MEDIA_VERSION 7→8、`index.html` 的 videoPlayer.js?v=9→10。8888 實測：13 支 wait 全數 200、manifest 36 個引用無缺檔、4 人選角→魔王討伐→勝利片→結果全流程通過，略過後所有 video 釋放、無 console error。改前備份：`/private/tmp/heroes_fate_pre_v096/`。註：`assets/videos/wait/`（168MB 舊 15s 原始檔）已與現行待選片無關，可另行封存。
- 2026-08-01｜Codex｜**v0.95.3 選角首載與完整倍速鎖定**：13 張選角卡由 512px PNG（3,867,085 bytes）改用 192px JPEG 縮圖（163,738 bytes，-95.77%），在人數頁預熱 manifest、預設 wait／confirm 與卡片縮圖；首次影片未就緒時使用同尺寸靜態立繪承接，選角頁淡入由 380ms 縮至 120ms。鎖定不再於 820ms 截斷原 3 秒影片，而是依實際長度約 4.00× 完整播放，正常約 760ms 結束、940ms 硬上限後切下一位。390×844 實測騎士與僧侶皆於 800ms 檢查點完成換人／進模式，無水平溢出、無 console error/warn；只點角色仍不鎖定，`js/rng.js` 雜湊保持 `d10ab10983e39ffad5c47e3c602171bd4cfda4f7a8ba6185202c969d3507b3e3`。手機公開網址同步更新為當時的 `charity-potentially-precisely-observations.trycloudflare.com`（已失效）並驗證首頁與新縮圖皆為 HTTP 200。
- 2026-08-01｜Codex｜**v0.95.2 僧侶待選片更新**：改用使用者提供的 3.04s 直式 Sora 僧侶影片，轉為 720×1278 / 24fps / H.264 High / yuv420p / faststart / 無聲行動版（1.00 MiB）；manifest 改指向 `assets/videos/mobile/wait/monk_v2.mp4`，另拆 manifest 與其他影片的快取版本，只讓僧侶新片重載。
- 2026-08-01｜Codex｜**v0.95.1 選角動畫加速**：待選 Sora 影片改為雙緩衝直接切換，新影片就緒前保留前一支待選影片，不再先顯示方形靜態圖後壓扁／放大；點角色後同時預載其 confirm，confirm 播放後立即預載下一位默認 wait；「決定」演出上限 820ms，正式版實測 883ms 完整進入下一位可選狀態。
- 2026-08-01｜Codex｜**v0.95 整體改版**：全站改為紫黑×金色暗黑皇家視覺，重做首頁、人數、U 字選角、模式海報卡、結果與 safe-area；選角須點角色後按「決定」才鎖定，離開選角會釋放影片。
- 2026-08-01｜Codex｜魔王討伐改為全螢幕六幕演出：淨空王座戰場、透明魔王降臨、Sora 主戰鬥影片、每位已選英雄 Sora 攻擊切入、種子結果揭曉、全畫面勝利短片；略過會立即停播並釋放所有媒體。
- 2026-08-01｜Codex｜公平 RNG 保持 `js/rng.js` seed-first：開局先 `seedRun()` 完整決定結果，後續影片、粒子、排牌只負責演出，不能影響結果。
- 2026-08-01｜Codex｜將 36 支 Sora 角色片與 1 支參考戰鬥片做 faststart/H.264 行動版，總體積 273.22 MiB → 48.85 MiB（-82.12%）；13 wait / 13 confirm / 10 victory 全數解碼驗證通過。
- 2026-08-01｜Codex｜用 `python3 -m http.server 8888 --bind 0.0.0.0` 及手機視窗完整回歸：魔王、順位、配對三模式通過；成功驗證 Sora 攻擊影片不殘留、略過後 video src 全部釋放、360×640 無水平溢出。
- 2026-08-01｜Grok｜匯出 `AGENT_HANDOFF.md` 完整交接（架構／啟動／角色表／模組／Bug／待辦）
- 2026-07-30｜Grok｜手機白屏：須用區網 IP 非 127.0.0.1；server bind 0.0.0.0；主畫面顯示手機網址；啟動錯誤保護
- 2026-07-30｜Grok｜選角 U 字版面（左右底繞中央全畫面）；等待片 contain 全身+紫底無黑頻；手機 safe-area
- 2026-07-30｜Grok｜修勝利黑屏：字幕與影片同框、片尾凍結最後一幀、VICTORY 疊字；不中途 destroy
- 2026-07-30｜Grok｜修跨局選角：回主選單／編成／重新選角會 clearPicks，上一場角色可再選
- 2026-07-30｜Grok｜重修選角：決定才鎖定、確定動畫等 canplay、全隊檢查才進模式、狀態列提示、鎖定中徽章
- 2026-07-30｜Grok｜修最後一位選角卡死（confirm 硬上限＋finally 進模式）；半獸人確定片 15s→3s；選角影片改 cover 去黑框
- 2026-07-30｜Grok｜**v0.94**：接入雲端 13 角色 Sora 影片（等待/確定/勝利）；重命名角色；選角播 wait、決定播 confirm、勝利播 victory；人數上限 13
- 2026-07-30｜Grok｜**v0.93 動畫大修**：Imagine 產 4 格 walk/attack sprite sheet → 裁切居中 → 全 12 角；棄用光流/軟遮罩（鬼影與假肢）
- 2026-07-30｜Grok｜**Imagine 管線**：全 12 角 walk/attack 改用 `image_edit` 從底圖重生關鍵幀並裝入 `assets/anim/`（取代軟遮罩）；video 工具此環境不可用
- 2026-07-30｜Grok｜逐幀走路8幀＋揮刀6幀（軟遮罩肢體循環，含國王）；選角播 walk、勝利播 attack；frameAnimator 硬切幀無鬼影
- 2026-07-30｜Grok｜大修動畫：廢除破圖切肢/鬼影交叉淡入；待機改整圖絲滑呼吸；勝利改乾淨關鍵幀＋鏡頭推拉（約6秒短片感）
- 2026-07-30｜Grok｜修卡頓：交叉淡入＋乒乓待機；補國王/龍騎士動作幀（先前缺 sheet 導致靜止）
- 2026-07-30｜Grok｜改為真・肢體分鏡動畫：sprite sheet 待機循環 + 勝利 6 幀短片；移除灰色平面旋轉；需 http://127.0.0.1:8888 開啟
- 2026-07-30｜Grok｜勝者專屬約 6 秒分鏡（12 角不同 FX）＋ Three.js 可拖曳旋轉 3D 立繪建模
- 2026-07-30｜Grok｜①強化魔王討伐分幕（集結→降臨→參考片→輪切→怒吼煙霧→揭示→勝利）②拿掉名字輸入③12 角 Q 版建模選角＋轉台預覽
- 2026-07-30｜Grok｜初版可玩原型完成
