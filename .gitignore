# ── 大型原始素材：只留遊戲實際在用的，其餘保留在本機與 Google Drive ──
# 舊高碼率原片（現行 manifest 不使用，約 272MB）
assets/videos/wait/
assets/videos/confirm/
assets/videos/victory/
# 已停用的 sprite sheet 動畫（約 128MB）
assets/anim/

# 參考片（高碼率原片，程式沒有引用）
assets/ref_battle.mp4
# 注意：ref_battle_mobile.mp4 不在這裡 —— 它是 ACT 3A「全軍突擊」正式使用的手機版短片
# （js/game.js 的 playStageClip 有引用），必須納入版控，否則 GitHub Pages 上會 404。

# macOS / Python
.DS_Store
._*
__pycache__/
*.pyc
